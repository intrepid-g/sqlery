# Doc Ingest — Conflict Detection Report

Mode: merge | Docs: 3 (2 SPEC, 1 DOC) | Generated: 2026-06-10
Precedence applied: GSD-CONTEXT.md (SPEC, 0) > PLAN.md (SPEC, 1) > sqlery-vs-pgque.md (DOC, 2)
Cycle detection: clean (GSD-CONTEXT → PLAN → sqlery-vs-pgque, no back-edges)

## Conflict Detection Report

### BLOCKERS (0)

(none — no locked ADR contradictions in the ingest set, and no ingested locked decision
contradicts an existing locked decision in PROJECT.md Key Decisions)

### WARNINGS (1)

[WARNING] Python syntax convention (3.13+) vs project floor (3.10+)
  Found: GSD-CONTEXT.md "Conventions for execution" requires "Python 3.13+ syntax";
    the project constraint is Python 3.10+ minimum (PROJECT.md Constraints, CLAUDE.md,
    and pyproject.toml `requires-python = ">=3.10"`, verified 2026-06-10)
  Impact: literal 3.13-only syntax (e.g. PEP 695 generics, 3.12+ f-string forms) in
    library source would break the published 3.10 floor and CI (tests 3.11–3.13). This is
    a standing project constraint, not a locked decision in a <decisions> block, so it is
    not a blocker — but the two cannot both be honored literally.
  → Confirm the interpretation before routing: treat the convention as "modern syntax
    within the 3.10 floor" (built-in generics, `X | None` — already project style), OR
    explicitly raise requires-python (a breaking packaging change, out of milestone scope)

### INFO (8)

[INFO] Auto-resolved: SPEC > DOC on partition config defaults
  Note: GSD-CONTEXT.md locked decision #1 and PLAN.md set interval "1 day", retention
    "30 days", premake 7, maintenance tick 5 min; sqlery-vs-pgque.md Appendix B.7 sketched
    "1 hour" / "2 hours" / premake 6. SPEC wins; DOC values recorded as superseded in
    intel/context.md.

[INFO] Auto-resolved: SPEC > DOC on partitioning feature flag
  Note: GSD-CONTEXT.md locked decision #8 makes partitioning default-on for PG with no
    flag (only SQLERY_PG_NOTIFY is flagged); sqlery-vs-pgque.md Appendix B.7 proposed an
    opt-in `SQLERY_PG_PARTITIONED=False`. SPEC wins; the flag does not exist in the
    synthesized config contract.

[INFO] Auto-resolved: SPEC > DOC on pg_partman
  Note: GSD-CONTEXT.md locked decision #2 and PLAN.md ("Why hand-rolled") reject
    pg_partman (no extension dependency; no invariant-checked drop); sqlery-vs-pgque.md
    Appendix A.3/B.6 offered pg_partman create-ahead as an option. SPEC wins; hand-rolled
    `core/partitioning.py` only.

[INFO] Auto-resolved: SPEC > DOC on migration numbering
  Note: PLAN.md and GSD-CONTEXT.md number the partition cutover 0029 (0028 = partial
    index, 0030 = staging); sqlery-vs-pgque.md Appendix B names it
    `0028_partition_queued_job.py`. SPEC wins; verified against the repo — latest existing
    migration is 0027, so 0028/0029/0030 are correct.

[INFO] Auto-resolved: SPEC > DOC on migration SQL and index columns
  Note: sqlery-vs-pgque.md Appendix A/B SQL uses `INCLUDING IDENTITY` + `SET DEFAULT
    nextval(...)`, a non-idempotent rename, copy-before-historical-partitions, and an
    `(priority DESC, scheduled_at)` index sketch. PLAN.md Step 8 / Step 1 (rev 2026-06-10,
    senior review findings #1–#3 folded in) supersede all of these: NOT INCLUDING
    IDENTITY, `ADD GENERATED BY DEFAULT AS IDENTITY` + setval, guarded idempotent rename,
    historical partitions before the copy, trailing index column `created_at`.

[INFO] Stale CLAUDE.md tech-stack note: Django >= 4.2
  Note: project CLAUDE.md says "Django >= 4.2" but pyproject.toml requires `django>=5.2`
    (verified 2026-06-10), which PLAN.md Step 7 depends on for CompositePrimaryKey. The
    Phase 4 premise holds; the CLAUDE.md tech-stack section is out of date, not a plan
    conflict.

[INFO] Milestone sequencing: drop-in compatibility deferred again
  Note: PROJECT.md names the drop-in compatibility milestone as the "strongest candidate"
    for the next cycle (and `.planning/MILESTONE-drop-in-compatibility.md` exists
    untracked); this ingest creates `partition-bloat-elimination` instead. The locked
    decision "Drop-in compatibility is a permanent first-class feature" concerns the
    feature's permanence, not its sequencing — no contradiction, but the deferral should
    be reflected when ROADMAP.md/PROJECT.md are regenerated.

[INFO] Composite PK / FK demotion vs the public-API-stability constraint
  Note: PROJECT.md requires `@job`, `enqueue`, `Queue` to remain stable — none are touched.
    However Phase 4's composite PK makes `QueuedJob.pk` a tuple and demotes two FKs;
    `QueuedJob` is re-exported (alias `TaskExecution`), so third-party `.pk` usage could
    feel it. Covered by locked decision #4 (accepted trade-off) and Phase 4's mandatory
    blast-radius audit (zero unaddressed hits is an acceptance criterion). Also noted:
    sqlery-vs-pgque.md flags PgQue licensing (Apache-2.0 vs MIT) as open if SQL is copied
    rather than re-derived — reference artifacts stay outside this repo.

---

## Resolution Log (2026-06-10)

[RESOLVED] Python syntax convention (3.13+) vs project floor (3.10+)
  Decision (user): RAISE the floor — `requires-python = ">=3.13"`. The packaging change
  is in-scope for this milestone (added as an explicit early task: pyproject bump,
  CI matrix drops 3.11/3.12, PROJECT.md constraint updated). 3.13+ syntax is then
  permitted throughout the new code.
