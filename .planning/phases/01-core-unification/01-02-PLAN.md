---
phase: 01-core-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/worker_claiming.py
  - src/sqlery/django_sqlery/executor.py
  - src/sqlery/django_sqlery/backend.py
  - src/sqlery/django_sqlery/admin.py
  - src/sqlery/django_sqlery/daemon_worker.py
  - src/sqlery/django_sqlery/worker_process.py
  - src/sqlery/django_sqlery/worker_registry.py
  - src/sqlery/django_sqlery/management/commands/run_jobs.py
  - src/sqlery/django_sqlery/management/commands/rqworker.py
  - src/sqlery/django_sqlery/management/commands/run_scheduled_tasks.py
  - src/sqlery/triggers.py
  - src/sqlery/lambda_handler.py
  - src/sqlery/executor.py
  - src/sqlery/compat/scheduler.py
  - tests/chaos/test_worker_chaos.py
autonomous: true
requirements: [UNIF-01, UNIF-02]
tags: [unification, dead-code-marking]

must_haves:
  truths:
    - "`django_sqlery/worker_claiming.py` is a comment-and-date stub that re-exports from `sqlery.core.claiming`"
    - "`django_sqlery/executor.py` is a comment-and-date stub that re-exports from `sqlery.core.worker`"
    - "Every in-repo caller imports from `sqlery.core.claiming` and `sqlery.core.worker` directly (not from the deprecated paths)"
    - "All Django-mode tests still pass after the cutover"
  artifacts:
    - path: "src/sqlery/django_sqlery/worker_claiming.py"
      provides: "Dated-stub with __getattr__ lazy re-export from sqlery.core.claiming"
      contains: "# DEPRECATED 2026-05-13"
    - path: "src/sqlery/django_sqlery/executor.py"
      provides: "Dated-stub with __getattr__ lazy re-export from sqlery.core.worker"
      contains: "# DEPRECATED 2026-05-13"
  key_links:
    - from: "src/sqlery/django_sqlery/backend.py"
      to: "sqlery.core.claiming"
      via: "direct import of `claim_next_job_with_queue_priority`"
      pattern: "from sqlery.core.claiming import"
    - from: "src/sqlery/django_sqlery/worker_process.py"
      to: "sqlery.core.claiming + sqlery.core.worker"
      via: "direct imports replacing local relatives"
      pattern: "from sqlery.core.(claiming|worker) import"
---

<objective>
Retire `django_sqlery/worker_claiming.py` and `django_sqlery/executor.py` as duplicate sources of truth. Update every in-repo caller to import from `sqlery.core.claiming` and `sqlery.core.worker`. Convert the retired files to dated stubs per the project dead-code policy (CLAUDE.md / [feedback_dead_code]).

Purpose: Satisfy UNIF-01 and UNIF-02 — single canonical implementation of claiming and execution logic.
Output: Two dated stubs, ~13 caller files updated, zero behavior change in Django mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-unification/01-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md

<interfaces>
Canonical exports (already exist — DO NOT re-create):

`src/sqlery/core/claiming.py` exports:
- `claim_next_job_with_queue_priority(...)` — atomic claim helper.
- `get_node_id()` — worker node identifier helper.
- `release_job(...)` — release a held job back to ready.

`src/sqlery/core/worker.py` exports:
- `JobExecutor` (likely aliased as `TaskExecutor` for backward compat — verify and re-alias if needed).
- `_current_job_var: contextvars.ContextVar` — current job context for nested enqueues.

Stub pattern (from STRUCTURE.md, locked in CONTEXT.md):
```
# DEPRECATED 2026-05-13 — moved to sqlery.core.{X}. Remove after 2027-05-13.
# See .planning/phases/01-core-unification/01-CONTEXT.md and feedback_dead_code memory.
from sqlery.core.{X} import *  # noqa: F401,F403

def __getattr__(name):
    import sqlery.core.{X} as _canon
    return getattr(_canon, name)
```

Caller inventory (verified 2026-05-13 via grep):
- `src/sqlery/django_sqlery/backend.py:22` — `from .worker_claiming import claim_next_job_with_queue_priority`
- `src/sqlery/django_sqlery/admin.py:15` — `from .executor import TaskExecutor`
- `src/sqlery/django_sqlery/daemon_worker.py:45` — `from sqlery.django_sqlery.executor import TaskExecutor`
- `src/sqlery/django_sqlery/worker_process.py:18-19` — `from .executor import TaskExecutor` + `from .worker_claiming import claim_next_job_with_queue_priority, release_job`
- `src/sqlery/django_sqlery/worker_registry.py:11` — `from .worker_claiming import get_node_id`
- `src/sqlery/django_sqlery/management/commands/run_jobs.py:9`
- `src/sqlery/django_sqlery/management/commands/rqworker.py:24`
- `src/sqlery/django_sqlery/management/commands/run_scheduled_tasks.py:4`
- `src/sqlery/triggers.py:5` — `from .executor import TaskExecutor` (this is `src/sqlery/executor.py` top-level shim)
- `src/sqlery/lambda_handler.py:67` — same
- `src/sqlery/executor.py:7` — top-level shim `from sqlery.django_sqlery.executor import TaskExecutor` (this file ALSO becomes a dated stub re-exporting from `sqlery.core.worker`)
- `src/sqlery/compat/scheduler.py:31` — `from sqlery.django_sqlery.executor import _current_job_var as _django_job_var`
- `tests/chaos/test_worker_chaos.py:31` — `from sqlery.django_sqlery.executor import TaskExecutor`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify canonical exports + alias `TaskExecutor` in core/worker.py if missing</name>
  <files>src/sqlery/core/worker.py, src/sqlery/core/claiming.py</files>
  <read_first>
    - src/sqlery/core/worker.py (full file)
    - src/sqlery/core/claiming.py (full file)
    - src/sqlery/django_sqlery/executor.py (lines 1-100; identify exported names — TaskExecutor, _current_job_var, any module-level helpers callers use)
    - src/sqlery/django_sqlery/worker_claiming.py (lines 1-100; identify exported names — claim_next_job_with_queue_priority, release_job, get_node_id, any others)
  </read_first>
  <action>
    1. In `src/sqlery/core/worker.py`: verify `JobExecutor` exists. If callers use `TaskExecutor`, add a module-level alias `TaskExecutor = JobExecutor` (or rename `JobExecutor` to `TaskExecutor` and add reverse alias — pick whichever matches existing public API; `TaskExecutor` is the historic name per CLAUDE.md "Backward compatibility aliases").
    2. Verify `_current_job_var` is exported from `core/worker.py` at module level (it is the contextvar callers rely on). If it lives elsewhere, move or re-export.
    3. In `src/sqlery/core/claiming.py`: verify `claim_next_job_with_queue_priority`, `release_job`, `get_node_id` are all present and exported. The CONTEXT.md notes claiming.py was promoted from the django version — names should match. If any name is missing, port the function body from `django_sqlery/worker_claiming.py` (DO NOT re-implement; copy the verified working code).
    4. Add an `__all__` list to both files capturing the public exports the stubs will re-export.
    5. Do NOT change any function signatures or behavior.
  </action>
  <verify>
    <automated>python -c "from sqlery.core.worker import TaskExecutor, _current_job_var; from sqlery.core.claiming import claim_next_job_with_queue_priority, release_job, get_node_id; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All five symbols (`TaskExecutor`, `_current_job_var`, `claim_next_job_with_queue_priority`, `release_job`, `get_node_id`) importable from `sqlery.core.*` directly.
    - `__all__` defined in both core/worker.py and core/claiming.py.
    - `git diff src/sqlery/core/worker.py src/sqlery/core/claiming.py` shows no behavioral change to existing functions (only alias additions or `__all__`).
  </acceptance_criteria>
  <done>Canonical exports stable and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Update all in-repo callers to import from sqlery.core.*</name>
  <files>src/sqlery/django_sqlery/backend.py, src/sqlery/django_sqlery/admin.py, src/sqlery/django_sqlery/daemon_worker.py, src/sqlery/django_sqlery/worker_process.py, src/sqlery/django_sqlery/worker_registry.py, src/sqlery/django_sqlery/management/commands/run_jobs.py, src/sqlery/django_sqlery/management/commands/rqworker.py, src/sqlery/django_sqlery/management/commands/run_scheduled_tasks.py, src/sqlery/triggers.py, src/sqlery/lambda_handler.py, src/sqlery/compat/scheduler.py, tests/chaos/test_worker_chaos.py</files>
  <read_first>
    - Each file in `<files>` (read each once; extract the existing import lines to rewrite).
    - The `<interfaces>` block above for the verified caller inventory and line numbers.
  </read_first>
  <action>
    For each listed file, rewrite the deprecated-path imports to canonical-core imports. Specific substitutions:

    - `from .worker_claiming import X` (in `django_sqlery/*.py`) → `from sqlery.core.claiming import X`
    - `from sqlery.django_sqlery.worker_claiming import X` → `from sqlery.core.claiming import X`
    - `from .executor import TaskExecutor` (in `django_sqlery/*.py`) → `from sqlery.core.worker import TaskExecutor`
    - `from sqlery.django_sqlery.executor import TaskExecutor` → `from sqlery.core.worker import TaskExecutor`
    - `from sqlery.django_sqlery.executor import _current_job_var as _django_job_var` (in `compat/scheduler.py:31`) → `from sqlery.core.worker import _current_job_var as _django_job_var`
    - In `src/sqlery/triggers.py:5` and `src/sqlery/lambda_handler.py:67`: `from .executor import TaskExecutor` resolves to `src/sqlery/executor.py` (top-level shim). Change to `from sqlery.core.worker import TaskExecutor`.
    - In `tests/chaos/test_worker_chaos.py:31`: `from sqlery.django_sqlery.executor import TaskExecutor` → `from sqlery.core.worker import TaskExecutor`.

    Do NOT remove the commented-out historical lines (e.g. `# from .executor import TaskExecutor  # moved to top-level`). They are intentional dead-code markers per project policy.

    After the edits, run `grep -rnE 'from (\.|sqlery\.django_sqlery\.)(worker_claiming|executor)' src/ tests/ --include='*.py'` and verify the only remaining hits are:
    1. Commented-out lines (start with `#`).
    2. The two stub files themselves (Task 3 will create them).

    Run the Django-mode test suite as a regression gate.
  </action>
  <verify>
    <automated>test -z "$(grep -rnE '^[[:space:]]*from (\.|sqlery\.django_sqlery\.)(worker_claiming|executor) import' src/ tests/ --include='*.py' | grep -v 'django_sqlery/worker_claiming.py\|django_sqlery/executor.py')" && uv run pytest tests/ -x -q --no-header -k "not chaos_postgresql"</automated>
  </verify>
  <acceptance_criteria>
    - Grep gate above returns empty (no live imports from the deprecated paths outside the stub files themselves).
    - `uv run pytest tests/ -x -q --no-header` passes with no NEW failures vs the pre-edit baseline.
    - `git diff --stat` shows changes only to the 12 listed files (no incidental edits).
    - Each modified file's old import line is replaced, not deleted-and-rewritten differently (keep import grouping/order stable per `ruff`).
  </acceptance_criteria>
  <done>All live callers route through sqlery.core.*; test suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Convert django_sqlery/{worker_claiming,executor}.py and src/sqlery/executor.py to dated stubs</name>
  <files>src/sqlery/django_sqlery/worker_claiming.py, src/sqlery/django_sqlery/executor.py, src/sqlery/executor.py</files>
  <read_first>
    - src/sqlery/django_sqlery/worker_claiming.py (full file — preserve any unique helpers not yet in core)
    - src/sqlery/django_sqlery/executor.py (full file — preserve any unique helpers not yet in core)
    - src/sqlery/executor.py (already a tiny shim; just needs date-stamp)
    - .planning/codebase/STRUCTURE.md (stub-file convention)
  </read_first>
  <action>
    Replace the bodies of all three files with the dated-stub pattern. Use removal date `2027-05-13` (12 months from 2026-05-13 per CONTEXT.md open question resolution).

    Before replacing each file: re-scan it for any function or constant NOT yet present in `sqlery.core.*`. If you find any, STOP and surface to the user — Task 1 should have caught this. (Defensive check; expected outcome: nothing remaining.)

    Stub template for `django_sqlery/worker_claiming.py`:
    ```
    """DEPRECATED 2026-05-13 — moved to sqlery.core.claiming.

    This module is a backward-compatibility stub for any out-of-repo importers
    during the deprecation window. Remove after 2027-05-13.

    Policy: comment-and-date marking per CLAUDE.md and feedback_dead_code memory.
    See .planning/phases/01-core-unification/01-CONTEXT.md.
    """
    from sqlery.core.claiming import *  # noqa: F401,F403
    from sqlery.core.claiming import (  # explicit re-export for type checkers
        claim_next_job_with_queue_priority,
        release_job,
        get_node_id,
    )

    def __getattr__(name):
        import sqlery.core.claiming as _canon
        try:
            return getattr(_canon, name)
        except AttributeError as e:
            raise AttributeError(
                f"{name!r} not found in deprecated sqlery.django_sqlery.worker_claiming; "
                "this module is retired — see sqlery.core.claiming"
            ) from e
    ```

    Stub template for `django_sqlery/executor.py`: same pattern, target `sqlery.core.worker`, re-export `TaskExecutor` and `_current_job_var`.

    Stub template for `src/sqlery/executor.py`: same pattern, target `sqlery.core.worker`, re-export `TaskExecutor`.

    DO NOT delete the files (per dead-code policy). DO NOT keep the original function bodies (single source of truth violation).
  </action>
  <verify>
    <automated>python -c "import sqlery.django_sqlery.worker_claiming as a, sqlery.django_sqlery.executor as b, sqlery.executor as c; assert a.claim_next_job_with_queue_priority is __import__('sqlery.core.claiming', fromlist=['x']).claim_next_job_with_queue_priority; assert b.TaskExecutor is __import__('sqlery.core.worker', fromlist=['x']).TaskExecutor; assert c.TaskExecutor is b.TaskExecutor; print('OK')" && grep -l "DEPRECATED 2026-05-13" src/sqlery/django_sqlery/worker_claiming.py src/sqlery/django_sqlery/executor.py src/sqlery/executor.py</automated>
  </verify>
  <acceptance_criteria>
    - All three files contain the `# DEPRECATED 2026-05-13` header and a `Remove after 2027-05-13` line.
    - Each file's line count drops below 30 (sanity: was 531/685/short — now stubs).
    - `python -c "from sqlery.django_sqlery.worker_claiming import claim_next_job_with_queue_priority"` succeeds AND returns the same function object as `from sqlery.core.claiming import claim_next_job_with_queue_priority`.
    - Same identity check for `TaskExecutor` from both deprecated paths.
    - `uv run pytest tests/ -x -q --no-header` still passes (regression).
  </acceptance_criteria>
  <done>Three dated stubs in place; canonical implementations are the single source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none for this plan) | Pure refactor; no new untrusted-input paths |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Stub `__getattr__` | accept | Module is internal-only; lazy re-export is a stdlib pattern |
</threat_model>

<verification>
- Grep returns no live imports from `django_sqlery.{worker_claiming,executor}` or `sqlery.executor` outside the three stubs and commented historical lines.
- All three stubs carry the dated-header.
- Identity check: deprecated import path returns the same callable object as the canonical path.
- Full Django test suite passes with no new failures.
</verification>

<success_criteria>
1. UNIF-01 satisfied: `django_sqlery/worker_claiming.py` delegates to `core/claiming.py`.
2. UNIF-02 satisfied: `django_sqlery/executor.py` delegates to `core/worker.py` JobExecutor.
3. No in-repo caller imports from the deprecated paths.
4. Dead-code policy honored: files retained as dated stubs, not deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-unification/01-02-SUMMARY.md`.
</output>
