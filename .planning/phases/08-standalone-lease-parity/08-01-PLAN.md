---
phase: 08-standalone-lease-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/core/models.py
  - src/sqlery/tables.py
  - alembic/env.py
  - alembic/versions/20260608_0015_add_daemon_lease.py
autonomous: true
requirements: [LEASE-01, LEASE-02]
must_haves:
  truths:
    - "A DaemonLease SQLModel exists in core/models.py mirroring Django's fields plus a version field for SQLite CAS"
    - "A date-prefixed Alembic migration chained from head 20260514_0014 creates the sqlery_daemon_lease table with an index on expires_at"
    - "Running 'alembic upgrade head' creates the sqlery_daemon_lease table on a fresh SQLite database"
    - "SQLModel.metadata.create_all produces the sqlery_daemon_lease table (model registered on metadata)"
  artifacts:
    - path: "src/sqlery/core/models.py"
      provides: "DaemonLease SQLModel (table sqlery_daemon_lease)"
      contains: "class DaemonLease"
    - path: "src/sqlery/tables.py"
      provides: "DAEMON_LEASE table-name constant"
      contains: "DAEMON_LEASE"
    - path: "alembic/versions/20260608_0015_add_daemon_lease.py"
      provides: "Migration creating sqlery_daemon_lease with expires_at index"
      contains: "create_table"
    - path: "alembic/env.py"
      provides: "DaemonLease import so autogenerate/metadata sees the table"
      contains: "DaemonLease"
  key_links:
    - from: "src/sqlery/core/models.py"
      to: "SQLModel.metadata"
      via: "class DaemonLease(SQLModel, table=True)"
      pattern: "class DaemonLease\\(SQLModel, table=True\\)"
    - from: "alembic/versions/20260608_0015_add_daemon_lease.py"
      to: "20260514_0014"
      via: "down_revision chaining"
      pattern: "down_revision = '20260514_0014'"
---

<objective>
Establish the standalone lease schema foundation: a `DaemonLease` SQLModel that
mirrors Django's `DaemonLease` field-for-field (plus a `version` column for
SQLite CAS), a centralized `DAEMON_LEASE` table-name constant, and a
date-prefixed Alembic migration that creates the `sqlery_daemon_lease` table
chained from the current head.

Purpose: Standalone/SQLAlchemy currently has NO lease table at all — leader
election silently inherits the ABC fake default. This plan creates the physical
table and ORM model so Plan 02 can implement real atomic claim/renew/release
against it. (LEASE-01, LEASE-02)

Output: `DaemonLease` model in `core/models.py`, `DAEMON_LEASE` constant in
`tables.py`, `DaemonLease` registered in `alembic/env.py`, and migration
`20260608_0015_add_daemon_lease.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-standalone-lease-parity/08-CONTEXT.md
@.planning/phases/08-standalone-lease-parity/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DaemonLease SQLModel and DAEMON_LEASE table constant</name>
  <files>src/sqlery/core/models.py, src/sqlery/tables.py</files>
  <read_first>
    - src/sqlery/core/models.py (lines 1-130) — the file being modified; copy the
      SQLModel field/Config style and the `version` field precedent verbatim. The
      `version` field on `QueuedJob` (lines 113-114) is the exact CAS-field pattern
      to reuse. Top-of-file imports (`Field, SQLModel`, `datetime`, `timedelta`,
      `UTC`) at lines 7-16 already cover everything needed — add NO new imports.
    - src/sqlery/core/models.py (lines 260-299) — the `Worker` SQLModel; copy its
      `__tablename__` + field declaration style.
    - src/sqlery/django_sqlery/models.py (lines 1191-1213) — the semantic source of
      truth `DaemonLease` Django model to mirror field-for-field.
    - src/sqlery/tables.py (full file, 1-9) — the file being modified; follow the
      existing `QUEUED_JOB`/`SCHEDULED_TASK`/`WORKER`/`REGISTRY` constant style.
  </read_first>
  <action>
    In `src/sqlery/core/models.py`, add a new class `DaemonLease(SQLModel, table=True)`
    with `__tablename__ = "sqlery_daemon_lease"` and a one-line docstring describing it
    as the DB-backed per-queue scheduler/daemon ownership lease for standalone mode.
    Declare fields mirroring Django's `DaemonLease` exactly: `queue_name: str` =
    `Field(max_length=255, primary_key=True)`; `daemon_id: str` =
    `Field(max_length=255, description="daemon_{node_id}_{pid}")`; `node_id: str` =
    `Field(max_length=255)`; `pid: int`; `acquired_at: datetime`; `expires_at: datetime` =
    `Field(index=True)`. Add the SQLite-CAS delta column `version: int` =
    `Field(default=0, description="Version counter for optimistic locking (SQLite CAS)")`,
    copying the exact wording from `QueuedJob.version` (models.py:113-114). Do NOT add a
    `Relationship` (leases stand alone). Place the class near the other table models;
    add NO new imports (existing top-level imports suffice). Follow CLAUDE.md: top-level
    imports only, modern `X | None` / built-in generic syntax, succinct PEP 257 docstring.

    In `src/sqlery/tables.py`, add the module-level constant
    `DAEMON_LEASE = "sqlery_daemon_lease"` alongside the existing constants, matching
    the `QUEUED_JOB`/`WORKER` style.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && python -c "from sqlery.core.models import DaemonLease; from sqlery.tables import DAEMON_LEASE; assert DaemonLease.__tablename__ == 'sqlery_daemon_lease'; assert DAEMON_LEASE == 'sqlery_daemon_lease'; cols = {c.name for c in DaemonLease.__table__.columns}; assert {'queue_name','daemon_id','node_id','pid','acquired_at','expires_at','version'} <= cols, cols; pk = [c.name for c in DaemonLease.__table__.primary_key.columns]; assert pk == ['queue_name'], pk; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from sqlery.core.models import DaemonLease` imports without error.
    - `DaemonLease.__tablename__ == "sqlery_daemon_lease"`.
    - `DaemonLease` table columns are exactly the seven: `queue_name`, `daemon_id`,
      `node_id`, `pid`, `acquired_at`, `expires_at`, `version`.
    - Primary key is `queue_name` only.
    - `expires_at` column has `index=True` (an index is registered for it).
    - `version` column default is `0`.
    - `from sqlery.tables import DAEMON_LEASE` equals `"sqlery_daemon_lease"`.
    - No inline/function-local imports were added (top-of-file only).
  </acceptance_criteria>
  <done>
    DaemonLease SQLModel exists with all seven columns and queue_name PK; DAEMON_LEASE
    constant exported; the verify command prints OK. (LEASE-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Register DaemonLease in alembic env and add create-table migration</name>
  <files>alembic/env.py, alembic/versions/20260608_0015_add_daemon_lease.py</files>
  <read_first>
    - alembic/env.py (lines 1-30) — the file being modified; line 18 imports the model
      set (`from sqlery.core.models import QueuedJob, ScheduledTask, JobRegistry, Worker`)
      that populates `target_metadata = SQLModel.metadata`. `DaemonLease` MUST be added
      to this import for autogenerate/metadata parity.
    - alembic/versions/20260514_0014_add_shutting_down_status.py (full file) — current
      head; confirms `revision = '20260514_0014'`, the header layout, and that it imports
      a constant from `sqlery.tables`. The new migration's `down_revision` MUST be
      `'20260514_0014'`.
    - alembic/versions/20250101_0007_tag_lock.py (full file) — the create_table analog;
      copy its `op.create_table(...)` + `PrimaryKeyConstraint` shape. Plain
      `create_table`/`create_index` need NO `batch_alter_table` (batch mode is only for
      SQLite ALTERs).
    - src/sqlery/tables.py — to import `DAEMON_LEASE` (added in Task 1) in the migration,
      matching how 20260514_0014 imports `QUEUED_JOB`.
  </read_first>
  <action>
    In `alembic/env.py`, extend the existing import on line 18 so it also imports
    `DaemonLease` (i.e. `from sqlery.core.models import QueuedJob, ScheduledTask,
    JobRegistry, Worker, DaemonLease`). This keeps `SQLModel.metadata` aware of the
    table for autogenerate. Do not change `target_metadata`.

    Create `alembic/versions/20260608_0015_add_daemon_lease.py`. Header per repo
    convention: module docstring naming it "Add daemon lease table"; `revision =
    '20260608_0015'`; `down_revision = '20260514_0014'`; `branch_labels = None`;
    `depends_on = None`. Import `sqlalchemy as sa`, `from alembic import op`, and
    `from sqlery.tables import DAEMON_LEASE`.

    `upgrade()` calls `op.create_table(DAEMON_LEASE, ...)` with columns matching the
    model exactly: `queue_name` `sa.String(length=255)` not-null; `daemon_id`
    `sa.String(length=255)` not-null; `node_id` `sa.String(length=255)` not-null; `pid`
    `sa.Integer()` not-null; `acquired_at` `sa.DateTime()` not-null; `expires_at`
    `sa.DateTime()` not-null; `version` `sa.Integer()` not-null with `server_default='0'`;
    and `sa.PrimaryKeyConstraint('queue_name')`. Then
    `op.create_index('ix_sqlery_daemon_lease_expires_at', DAEMON_LEASE, ['expires_at'])`
    to mirror the Django `db_index=True` on `expires_at`.

    `downgrade()` drops the index then the table:
    `op.drop_index('ix_sqlery_daemon_lease_expires_at', table_name=DAEMON_LEASE)` then
    `op.drop_table(DAEMON_LEASE)`.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && SQLERY_DATABASE_URL="sqlite:///$(mktemp -t lease_mig_XXXX.sqlite)" python -m alembic upgrade head && SQLERY_DATABASE_URL="sqlite:///$(mktemp -t lease_head_XXXX.sqlite)" python -c "import sqlalchemy as sa, os; from alembic.config import Config; from alembic.script import ScriptDirectory; sd = ScriptDirectory.from_config(Config('alembic.ini')); head = sd.get_current_head(); assert head == '20260608_0015', head; print('HEAD OK', head)"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/env.py` line 18 import includes `DaemonLease`.
    - The migration file defines `revision = '20260608_0015'` and
      `down_revision = '20260514_0014'`.
    - `alembic upgrade head` succeeds against a fresh SQLite DB and the current Alembic
      head resolves to `20260608_0015` (single linear head, no branch).
    - After upgrade, table `sqlery_daemon_lease` exists with columns `queue_name`,
      `daemon_id`, `node_id`, `pid`, `acquired_at`, `expires_at`, `version`, primary key
      on `queue_name`, and an index `ix_sqlery_daemon_lease_expires_at` on `expires_at`.
    - `downgrade()` removes the index then the table.
    - The migration imports `DAEMON_LEASE` from `sqlery.tables` (no hardcoded table name).
  </acceptance_criteria>
  <done>
    Migration 20260608_0015 chains from head, creates sqlery_daemon_lease with the
    expires_at index, and `alembic upgrade head` resolves to a single linear head.
    (LEASE-02)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → database | DDL executes against the operator's DB; table/column names must be controlled (constant-sourced, never user input) |
| ORM model → metadata | Model registration feeds `SQLModel.metadata`/Alembic autogenerate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | migration DDL | mitigate | Table/index names sourced from the `DAEMON_LEASE` constant and literal strings only; no user/runtime input reaches DDL. |
| T-08-02 | Denial of Service | branched Alembic head | mitigate | `down_revision = '20260514_0014'` keeps a single linear head; verify step asserts head resolves to `20260608_0015` (prevents multi-head upgrade ambiguity that can block deploys). |
| T-08-03 | Information Disclosure | lease columns | accept | Lease rows hold only `daemon_id`/`node_id`/`pid` operational identifiers — no secrets or PII; no encryption needed. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (alembic, sqlmodel already in pyproject; per CLAUDE.md "no new dependencies"). N/A. |
</threat_model>

<verification>
- `python -c "from sqlery.core.models import DaemonLease"` succeeds with all seven columns and `queue_name` PK.
- `from sqlery.tables import DAEMON_LEASE == "sqlery_daemon_lease"`.
- `alembic upgrade head` on a fresh SQLite DB creates `sqlery_daemon_lease` with the `expires_at` index; current head resolves to `20260608_0015` (single linear head).
- `alembic/env.py` imports `DaemonLease` so `SQLModel.metadata` carries the table.
</verification>

<success_criteria>
- DaemonLease SQLModel mirrors Django's `DaemonLease` fields plus a `version` column. (LEASE-01)
- DAEMON_LEASE constant added to `tables.py`.
- Date-prefixed migration `20260608_0015_add_daemon_lease.py` chains from head and creates the table with the `expires_at` index. (LEASE-02)
- Migration and model both reference the table only via the constant / `__tablename__`.
</success_criteria>

<output>
Create `.planning/phases/08-standalone-lease-parity/08-01-SUMMARY.md` when done.
</output>
