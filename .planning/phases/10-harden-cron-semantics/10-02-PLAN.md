---
phase: 10-harden-cron-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/fastapi_sqlery/config.py
  - src/sqlery/django_sqlery/settings.py
autonomous: true
requirements: [CRON-03]
must_haves:
  truths:
    - "scheduler_jitter_seconds is readable via get_config('scheduler_jitter_seconds', 0) in both Django and standalone modes"
    - "The default value is 0 (jitter off) in both config default sets"
    - "The standalone key is overridable via an environment variable"
  artifacts:
    - path: "src/sqlery/fastapi_sqlery/config.py"
      provides: "scheduler_jitter_seconds default in StandaloneConfig + env-var loading"
      contains: "scheduler_jitter_seconds"
    - path: "src/sqlery/django_sqlery/settings.py"
      provides: "SCHEDULER_JITTER_SECONDS default in DEFAULTS"
      contains: "SCHEDULER_JITTER"
  key_links:
    - from: "scheduler (Plan 03)"
      to: "get_config jitter key"
      via: "get_config lookup"
      pattern: "scheduler_jitter_seconds"
---

<objective>
Add the optional `scheduler_jitter_seconds` config key (default `0`, jitter off) to BOTH config default sets so the scheduler (Plan 03) can read it via `get_config` consistently in Django and standalone modes. This is the config surface for CRON-03; Plan 03 consumes it to apply a bounded enqueue delay.

Purpose: PROJECT.md locked jitter default to `0`. `get_config(key, default)` resolves against the active config (Django `DEFAULTS`/`DJANGO_SQL_JOBS` or `StandaloneConfig._config`), so the key must exist in both default sets to be a real, documented knob rather than a magic string.

Output: The new key in `StandaloneConfig` defaults (with env-var loading) and in the Django `DEFAULTS` dict, both defaulting to `0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-harden-cron-semantics/10-CONTEXT.md
@.planning/phases/10-harden-cron-semantics/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- **New config key `scheduler_jitter_seconds`** (default `0`):
  - Standalone: added to `StandaloneConfig._config` in `src/sqlery/fastapi_sqlery/config.py`, with an env-var mapping (`SQLERY_SCHEDULER_JITTER_SECONDS`) parsed as a float in `_load_from_env`.
  - Django: added to the `DEFAULTS` dict in `src/sqlery/django_sqlery/settings.py` under the scheduler settings section, as `SCHEDULER_JITTER_SECONDS: 0`.
- **Key-name resolution note:** `get_config` looks up the literal key passed to it. Plan 03 will call `get_config('scheduler_jitter_seconds', 0)`. Confirm in this plan which literal key string `get_config` resolves in each mode (standalone keys are the literal dict keys in `_config`; Django resolution maps through `get_setting`/`DJANGO_SQL_JOBS`). Document the exact key string the scheduler must pass in the SUMMARY so Plan 03 uses the right literal.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduler_jitter_seconds default + env loading to StandaloneConfig</name>
  <files>src/sqlery/fastapi_sqlery/config.py</files>
  <read_first>
    - src/sqlery/fastapi_sqlery/config.py lines 18-67 (the `_config` defaults dict — add the key in the Daemon/scheduler settings area near DAEMON_CHECK_INTERVAL :31)
    - src/sqlery/fastapi_sqlery/config.py lines 72-112 (`_load_from_env`: the `env_mappings` dict and the int/bool type-conversion block — add a float conversion for the jitter key)
    - src/sqlery/fastapi_sqlery/config.py lines 114-124 (get/set/all — confirm get returns the literal key)
  </read_first>
  <action>
    In `StandaloneConfig.__init__`, add `'scheduler_jitter_seconds': 0` to the `_config` dict near the Daemon settings block (after `'DAEMON_CHECK_INTERVAL': 10,` :31). In `_load_from_env`, add `'SQLERY_SCHEDULER_JITTER_SECONDS': 'scheduler_jitter_seconds'` to `env_mappings`, and extend the type-conversion logic so this key is parsed as a `float` (the existing block converts a set of keys to `int`; add a parallel branch that does `float(env_value)` for `'scheduler_jitter_seconds'`). Keep the literal key name lowercase `scheduler_jitter_seconds` (this is the exact string Plan 03 passes to `get_config`). Default stays `0` (jitter off, PROJECT.md locked). No new imports (`os` already top-level).
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && uv run --active python -c "from sqlery.fastapi_sqlery.config import StandaloneConfig; c=StandaloneConfig(); assert c.get('scheduler_jitter_seconds')==0, c.get('scheduler_jitter_seconds'); import os; os.environ['SQLERY_SCHEDULER_JITTER_SECONDS']='2.5'; c2=StandaloneConfig(); v=c2.get('scheduler_jitter_seconds'); assert v==2.5 and isinstance(v,float), v; del os.environ['SQLERY_SCHEDULER_JITTER_SECONDS']; print('STANDALONE JITTER OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `StandaloneConfig().get('scheduler_jitter_seconds')` returns `0` by default.
    - Setting `SQLERY_SCHEDULER_JITTER_SECONDS=2.5` yields `2.5` as a float.
    - Verify prints `STANDALONE JITTER OK`.
    - `grep -n "scheduler_jitter_seconds" src/sqlery/fastapi_sqlery/config.py` returns at least two hits (default dict entry + env mapping).
  </acceptance_criteria>
  <done>Standalone config exposes scheduler_jitter_seconds (default 0, env-overridable as float).</done>
</task>

<task type="auto">
  <name>Task 2: Add SCHEDULER_JITTER_SECONDS default to Django DEFAULTS</name>
  <files>src/sqlery/django_sqlery/settings.py</files>
  <read_first>
    - src/sqlery/django_sqlery/settings.py lines 8-53 (the DEFAULTS dict; add the key under the "Scheduler settings" comment block near `CHECK_INTERVAL_SECONDS` :11)
    - src/sqlery/django_sqlery/settings.py lines 200+ (the `get_setting(name, default)` resolver — confirm how get_config -> get_setting resolves a key against DEFAULTS and DJANGO_SQL_JOBS, so the SUMMARY can state the exact literal key Plan 03 passes)
  </read_first>
  <action>
    Add `"SCHEDULER_JITTER_SECONDS": 0,` to the `DEFAULTS` dict under the Scheduler settings section (next to `"CHECK_INTERVAL_SECONDS": 60,` :11). Default `0` (jitter off). Match the existing UPPER_SNAKE_CASE convention used by every other Django setting key. Then determine the exact literal key string that `get_config('scheduler_jitter_seconds', 0)` resolves to in Django mode by tracing `get_config` -> the Django Config adapter -> `get_setting`: if Django resolution upper-cases or maps the key, the scheduler in Plan 03 must pass whatever string round-trips correctly in BOTH modes. Record the resolved mapping in the SUMMARY. If the two modes require different literal key strings, note that Plan 03 must normalize (the simplest fix: have the Django Config adapter accept the lowercase `scheduler_jitter_seconds` and map it to `SCHEDULER_JITTER_SECONDS`, OR confirm get_setting is case-insensitive — verify, do not assume). Do NOT change get_config/get_setting behavior unless the trace proves the key cannot resolve in both modes; if a mapping change is required, make the minimal change and document it.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && DJANGO_SETTINGS_MODULE=tests.settings uv run --active python -c "import django; django.setup(); from sqlery.django_sqlery.settings import DEFAULTS, get_setting; assert DEFAULTS.get('SCHEDULER_JITTER_SECONDS')==0, DEFAULTS.get('SCHEDULER_JITTER_SECONDS'); print('DJANGO DEFAULT OK; get_setting resolves:', get_setting('SCHEDULER_JITTER_SECONDS', 0))"</automated>
  </verify>
  <acceptance_criteria>
    - `DEFAULTS['SCHEDULER_JITTER_SECONDS'] == 0`.
    - `get_setting('SCHEDULER_JITTER_SECONDS', 0)` resolves to `0` (the default) without error.
    - Verify prints `DJANGO DEFAULT OK; get_setting resolves: 0`.
    - The SUMMARY records the exact literal key string Plan 03 must pass to `get_config` so it resolves in BOTH Django and standalone modes (e.g. confirm whether get_config is given `scheduler_jitter_seconds` and the Django adapter upper-cases it, or whether the scheduler must branch).
  </acceptance_criteria>
  <done>Django DEFAULTS exposes SCHEDULER_JITTER_SECONDS (default 0); the cross-mode key resolution is documented for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env/settings -> scheduler delay | The jitter value controls a sleep/offset before enqueue; an unbounded or attacker-controlled value could become a covert delay/DoS channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service (covert delay channel) | scheduler_jitter_seconds value | mitigate | Default `0` (off). The value is config-driven (operator-set settings/env), not request-derived, so it is not attacker-controllable across the trust boundary. Plan 03 must BOUND the applied delay to `random.uniform(0, jitter)` and treat non-positive/None as disabled — enforced as an acceptance criterion in Plan 03. This plan only defines the default; it introduces no delay itself. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; config-dict edits only. No install task, no slopcheck/human checkpoint required. |
</threat_model>

<verification>
- `scheduler_jitter_seconds` default `0` resolvable via `get_config` in standalone mode.
- `SCHEDULER_JITTER_SECONDS` default `0` resolvable via `get_setting` in Django mode.
- Standalone env override parses as float.
- The exact literal key string for cross-mode `get_config` access is documented in the SUMMARY.
- `cd /Users/gabriel/Documents/GitHub/sqlery-public && uv run --active black --check src/sqlery/fastapi_sqlery/config.py src/sqlery/django_sqlery/settings.py` passes.
</verification>

<success_criteria>
- The jitter knob exists in both config default sets, defaulting to `0` (CRON-03 surface).
- The standalone key is env-overridable as a float.
- Plan 03 has a documented, verified literal key string to read in both modes.
</success_criteria>

<output>
Create `.planning/phases/10-harden-cron-semantics/10-02-SUMMARY.md` when done, recording the exact literal key string(s) `get_config` resolves in each mode and any get_setting case-handling discovered.
</output>
