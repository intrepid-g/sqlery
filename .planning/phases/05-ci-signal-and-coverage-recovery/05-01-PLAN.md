---
phase: 05-ci-signal-and-coverage-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_django_backend.py
  - tests/unit/test_worker.py
  - tests/chaos/test_property_based.py
  - .github/workflows/test.yml
autonomous: false
requirements: [TEST-01, TEST-02]
tags: [ci, tests, collection, signal]

must_haves:
  truths:
    - "The default non-postgres suite collects and runs without the known collection-error debt distorting the result"
    - "The postgres CI rail is not broken by avoidable collection-time failures"
    - "Broken or low-value test debt is either fixed or explicitly isolated with an auditable rationale"
    - "Changes keep scope narrow and do not add broad new test infrastructure"
  artifacts:
    - path: "tests/unit/test_django_backend.py"
      provides: "Current failing/fragile Django-backend unit coverage hotspot"
    - path: "tests/unit/test_worker.py"
      provides: "Fork lifecycle hotspot noted in prior verification"
    - path: "tests/chaos/test_property_based.py"
      provides: "Collection-debt hotspot affecting CI truthfulness"
    - path: ".github/workflows/test.yml"
      provides: "Default and postgres test-rail behavior"
  key_links:
    - from: "03-VERIFICATION.md failing-test evidence"
      to: "live tests and CI workflow"
      via: "Phase 5 should prove the current tree is honest, not just inherit old verification text"
      pattern: "test_property_based.py"
---

<objective>
Restore clean, trustworthy execution of the existing test rails with the smallest possible code change set. Fix or isolate the specific failing/collection-debt tests that make CI lie, and make sure the postgres rail fails only on real regressions.
</objective>

<context>
@.planning/phases/05-ci-signal-and-coverage-recovery/05-CONTEXT.md
@.planning/phases/05-ci-signal-and-coverage-recovery/05-RESEARCH.md
@.planning/phases/03-testing-ci/03-VERIFICATION.md
@.planning/v0.21-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the live red/green state of the default and postgres rails</name>
  <files>(measurement only)</files>
  <read_first>
    - .github/workflows/test.yml
    - pyproject.toml
    - tests/unit/test_django_backend.py
    - tests/unit/test_worker.py
    - tests/chaos/test_property_based.py
  </read_first>
  <action>
    Run the smallest set of commands that proves the current state of the two rails:
    - `uv run pytest tests/unit/test_django_backend.py tests/unit/test_worker.py --no-cov -q`
    - `uv run pytest -m postgres --collect-only -q`
    - `uv run pytest tests/ -m "not postgres" --collect-only -q`

    Record exactly which failures are still live on the current tree. Do not assume the older verifier is still perfectly current; use its findings only as a starting hypothesis.
  </action>
  <acceptance_criteria>
    - The current tree's live failures are recorded from command output, not inferred from old planning docs.
    - The executor can name which failures are still real and which prior gaps are already closed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix or explicitly isolate the small set of test/collection hotspots</name>
  <files>tests/unit/test_django_backend.py, tests/unit/test_worker.py, tests/chaos/test_property_based.py</files>
  <read_first>
    - tests/unit/test_django_backend.py
    - tests/unit/test_worker.py
    - tests/chaos/test_property_based.py
    - .planning/phases/03-testing-ci/03-VERIFICATION.md
    - .planning/v0.21-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Make the smallest concrete changes that remove false-negative signal:
    - if a test is correct but missing DB/test harness setup, add the exact mark, fixture, or monkeypatch it needs
    - if a test is stale against the current code shape, update it to the real public/internal interface
    - if `tests/chaos/test_property_based.py` is still not realistically repairable in this phase, keep it as an explicit module-level skip stub with a current rationale instead of letting it break collection

    Do not add new broad harness code. Prefer deleting misleading assumptions, tightening setup, or preserving an explicit dated stub over building a large replacement in this phase.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_django_backend.py` and `tests/unit/test_worker.py` no longer contain the specific false-negative failures reproduced in Task 1.
    - `tests/chaos/test_property_based.py` no longer breaks collection.
    - Any intentional skip/defer state is explicit in the source file and auditable from comments or module-level markers.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Tighten CI so rails fail for real regressions, not avoidable noise</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml
    - tests/chaos/test_property_based.py
    - .planning/phases/03-testing-ci/03-VERIFICATION.md
  </read_first>
  <action>
    Update `.github/workflows/test.yml` only if Task 1 shows the workflow still encodes misleading behavior.

    Concrete target state:
    - the default rail runs `-m "not postgres"`
    - the postgres rail runs `-m postgres`
    - neither rail depends on accidental collection success from a broken file
    - there is no hidden tolerance that masks genuine failures

    If the current workflow already meets that target once the test files are fixed or stubbed, leave the workflow unchanged.
  </action>
  <acceptance_criteria>
    - CI workflow semantics match the source-level reality after Task 2.
    - No new broad CI complexity is introduced.
    - The postgres rail is not made green by ignoring meaningful regressions.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verifies the rails are honest on a PR run</name>
  <what-built>
    Narrow test fixes or auditable stubs for the specific false-negative hotspots, plus any minimal workflow cleanup required to keep rails truthful.
  </what-built>
  <how-to-verify>
    1. Push the branch and open or update a PR.
    2. Confirm the default rail runs without collection noise from the addressed files.
    3. Confirm the postgres rail does not fail on the property-based collection debt.
    4. Confirm any remaining failures are real behavioral regressions, not harness slop.
  </how-to-verify>
  <resume-signal>Type "approved" once the PR rails reflect real failures only, or describe the remaining misleading behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI rail ↔ test collection | collection-time debt can make CI look broken even when the product is fine |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | CI signal | mitigate | fix or explicitly isolate the small set of collection/failure hotspots instead of letting them silently distort the rails |
| T-05-01-02 | Repudiation | test debt status | mitigate | any intentional skip/defer state must be explicit in source, not hidden in CI tolerances |
</threat_model>

<verification>
- Reproduction commands from Task 1 give a before/after record.
- Default and postgres rails no longer fail for avoidable collection noise.
</verification>
