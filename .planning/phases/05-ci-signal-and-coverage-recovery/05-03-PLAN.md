---
phase: 05-ci-signal-and-coverage-recovery
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .github/workflows/test.yml
  - tests/test_core_standalone.py
  - docs/TESTING.md
  - .planning/STATE.md
autonomous: false
requirements: [TEST-04]
tags: [ci, standalone, django-free, proof]

must_haves:
  truths:
    - "The standalone-no-Django confidence claim is proven by automation, not left as a human green-observation reminder"
    - "The CI job shows exactly what contract is being proven: core import/runtime behavior without Django installed"
    - "The proof path stays small and readable; no heavyweight new harness is introduced"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "standalone-no-Django job behavior"
    - path: "tests/test_core_standalone.py"
      provides: "explicit django-free proof target"
    - path: "docs/TESTING.md"
      provides: "documentation of what the standalone-no-Django job proves"
  key_links:
    - from: "standalone-no-django CI job"
      to: "tests/test_core_standalone.py"
      via: "explicit automated import/runtime proof"
      pattern: "standalone-no-django"
---

<objective>
Turn the lingering standalone-no-Django confidence gap from a human-verification story into an explicit automated proof that runs in CI and is documented clearly enough that maintainers know what it does and does not guarantee.
</objective>

<context>
@.planning/phases/05-ci-signal-and-coverage-recovery/05-CONTEXT.md
@.planning/phases/05-ci-signal-and-coverage-recovery/05-RESEARCH.md
@.planning/v0.21-MILESTONE-AUDIT.md
@tests/test_core_standalone.py
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit what the current standalone-no-Django job actually proves</name>
  <files>(measurement only)</files>
  <read_first>
    - .github/workflows/test.yml
    - tests/test_core_standalone.py
    - .planning/v0.21-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Read the current job definition and `tests/test_core_standalone.py`, then write down the exact current contract:
    - which command the CI job runs
    - whether it proves pure importability, limited runtime behavior, or both
    - which parts still relied on human observation rather than machine assertions
  </action>
  <acceptance_criteria>
    - The current proof contract is explicitly known before editing anything.
    - Any remaining human-only assurance is identified concretely.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make the CI proof explicit and automated</name>
  <files>.github/workflows/test.yml, tests/test_core_standalone.py</files>
  <read_first>
    - .github/workflows/test.yml
    - tests/test_core_standalone.py
    - .planning/phases/01-core-unification/01-03-PLAN.md
  </read_first>
  <action>
    Update the standalone-no-Django proof path so the CI job clearly runs an explicit automated assertion sequence.

    Concrete target state:
    - the CI job logs the exact django-free proof command(s)
    - `tests/test_core_standalone.py` contains the assertions needed for the current promise and no ambiguous manual interpretation
    - the job does not depend on someone visually "observing green" to satisfy the milestone truth

    Prefer simple subprocess/import assertions over adding a custom harness.
  </action>
  <acceptance_criteria>
    - The standalone-no-Django job proves its contract through explicit automated assertions.
    - `tests/test_core_standalone.py` is the clear source-of-truth test file for that contract.
    - No manual-only checkpoint remains in the implementation path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update docs and planning state to remove the stale human-proof story</name>
  <files>docs/TESTING.md, .planning/STATE.md</files>
  <read_first>
    - docs/TESTING.md
    - .planning/STATE.md
    - .planning/v0.21-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Update `docs/TESTING.md` to explain what the standalone-no-Django job proves.

    Update `.planning/STATE.md` only if its project-state narrative still describes this confidence claim as pending human verification. Keep the edit narrowly scoped to the stale status text; do not rewrite unrelated state.
  </action>
  <acceptance_criteria>
    - Documentation no longer implies the django-free proof depends on a human green observation.
    - Any stale state text about this proof gap is corrected if still present.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human confirms the automated proof is understandable</name>
  <what-built>
    A clearer standalone-no-Django CI proof path, associated assertions in `tests/test_core_standalone.py`, and updated docs/state language.
  </what-built>
  <how-to-verify>
    1. Read the standalone-no-Django job in `.github/workflows/test.yml`.
    2. Read `tests/test_core_standalone.py`.
    3. Confirm you can answer, in one sentence, what the job proves without relying on planning-doc interpretation.
    4. Confirm the docs no longer describe this as a human-only confidence step.
  </how-to-verify>
  <resume-signal>Type "approved" if the proof is explicit and understandable, or describe what is still ambiguous.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI job name ↔ actual proof | a job can sound meaningful while proving very little |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | standalone-no-Django confidence | mitigate | make the automated assertion path explicit in both CI and tests |
| T-05-03-02 | Repudiation | milestone truth | mitigate | remove stale human-verification language once the proof is machine-enforced |
</threat_model>

<verification>
- The standalone-no-Django proof is explicit in CI, explicit in tests, and explicit in docs.
</verification>
