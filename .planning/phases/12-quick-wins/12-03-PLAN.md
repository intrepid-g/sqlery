---
phase: 12-quick-wins
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/test.yml
  - .planning/PROJECT.md
autonomous: true
requirements:
  - R11
must_haves:
  truths:
    - "`requires-python = \">=3.13\"` in pyproject.toml"
    - "CI matrix python-version list contains only '3.13' (3.11 and 3.12 removed)"
    - "standalone-no-django job uses Python 3.13 (not 3.10)"
    - "Old Python classifiers for 3.10, 3.11, 3.12 are commented out in pyproject.toml"
    - "PROJECT.md Constraints section reflects 3.13+ minimum"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated requires-python and classifiers"
      contains: "requires-python = \">=3.13\""
    - path: ".github/workflows/test.yml"
      provides: "Updated CI matrix dropping 3.11 and 3.12; standalone job updated to 3.13"
      contains: "python-version: ['3.13']"
    - path: ".planning/PROJECT.md"
      provides: "Updated Constraints section"
      contains: "3.13+"
  key_links:
    - from: "pyproject.toml requires-python"
      to: ".github/workflows/test.yml matrix"
      via: "must stay aligned — floor and CI matrix must agree"
      pattern: "3\\.13"
---

<objective>
Raise the library's Python floor from 3.10 to 3.13 across three files:
1. `pyproject.toml` — bump `requires-python` and prune obsolete classifiers
2. `.github/workflows/test.yml` — drop 3.11 and 3.12 from both the `test` job matrix and the `standalone-no-django` job
3. `.planning/PROJECT.md` — update the Constraints section

Purpose: The Python-floor decision was user-approved on 2026-06-10 (INGEST-CONFLICTS.md Resolution Log,
D11-equivalent). After this plan, new code written in v0.24.0 may use 3.13+ syntax and standard
library features without a compat shim. CI can assume 3.13 as the minimum interpreter.

Output: Three updated files. No source code or test changes. Independently shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-quick-wins/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump requires-python and prune classifiers in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml lines 1-30 (requires-python, classifiers list)
    - .planning/phases/12-quick-wins/12-CONTEXT.md section "Decisions" (Python floor user decision)
    - .planning/REQUIREMENTS.md R11 (acceptance criteria: `requires-python = ">=3.13"`)
  </read_first>
  <action>
    In `pyproject.toml`:

    1. Line 10 — comment out `requires-python = ">=3.10"` and add beneath it:
       `requires-python = ">=3.13"`

    2. In the `classifiers` list (lines 15-29), comment out the lines for 3.10, 3.11, and 3.12:
       - `"Programming Language :: Python :: 3.10",`
       - `"Programming Language :: Python :: 3.11",`
       - `"Programming Language :: Python :: 3.12",`

       Leave the following classifiers active (do NOT comment out):
       - `"Programming Language :: Python :: 3",`
       - `"Programming Language :: Python :: 3.13",`
       - `"Programming Language :: Python :: 3.14",`

    Do NOT change any `[project.optional-dependencies]`, `[tool.*]`, or `[project.scripts]` sections.
    Do NOT change the `version` field.

    After editing, verify:
    - `grep -v "^#" pyproject.toml | grep "requires-python"` returns exactly `requires-python = ">=3.13"`
    - `grep -v "^#\|^ *#" pyproject.toml | grep "3\.10\|3\.11\|3\.12"` returns no classifier lines
      (note: `[tool.pytest.ini_options]` has `pythonpath` which contains no version string; that is safe)
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && python3 -c "
import tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
rp = data['project']['requires-python']
assert rp == '>=3.13', f'expected >=3.13 got {rp}'
classifiers = data['project']['classifiers']
bad = [c for c in classifiers if any(v in c for v in ['3.10', '3.11', '3.12'])]
assert not bad, f'stale classifiers found: {bad}'
print('pyproject-ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `data['project']['requires-python'] == '>=3.13'` (verified by tomllib parse above)
    - No classifiers containing `3.10`, `3.11`, or `3.12` remain active in the file
    - Old `requires-python = ">=3.10"` line is commented out, not deleted
    - Old classifier lines for 3.10/3.11/3.12 are commented out, not deleted
    - `uv run python -c "import tomllib"` succeeds (tomllib is stdlib in 3.11+; test environment has 3.13)
  </acceptance_criteria>
  <done>
    pyproject.toml carries `requires-python = ">=3.13"` and classifiers list 3.13 and 3.14 only
    (3.10–3.12 lines commented out).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI matrix and standalone-no-django job to Python 3.13</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml lines 1-25 (matrix definition — `python-version: ['3.11', '3.12', '3.13']`)
    - .github/workflows/test.yml lines 164-219 (standalone-no-django job — uses Python 3.10)
    - .planning/phases/12-quick-wins/12-CONTEXT.md section "Decisions" (R11 acceptance: CI matrix drops 3.11/3.12)
  </read_first>
  <action>
    In `.github/workflows/test.yml`:

    1. At line 20 (the `python-version` matrix entry):
       Comment out `python-version: ['3.11', '3.12', '3.13']`
       Add beneath it: `python-version: ['3.13']`

    2. In the `standalone-no-django` job (around line 178):
       Comment out `python-version: '3.10'`
       Add beneath it: `python-version: '3.13'`

    Do NOT alter any step names, run commands, service definitions, or other matrix keys
    (`django-version`, etc.). Do NOT change any `uv run pytest` commands.

    The comment at line 20 `# CI matrix (plan 03-07, TEST-11):` may be left as-is or updated
    to note the Phase 12 change — at the executor's discretion.

    After editing:
    - `grep -v "^#\|^ *#" .github/workflows/test.yml | grep "python-version.*3\.11\|python-version.*3\.12\|python-version.*3\.10"` returns no active lines
    - `grep -v "^#\|^ *#" .github/workflows/test.yml | grep "python-version.*3\.13"` returns at least 2 lines (matrix + standalone job)
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && python3 -c "
import yaml
with open('.github/workflows/test.yml') as f:
    data = yaml.safe_load(f)
matrix_versions = data['jobs']['test']['strategy']['matrix']['python-version']
assert matrix_versions == ['3.13'], f'unexpected matrix: {matrix_versions}'
standalone_version = data['jobs']['standalone-no-django']['steps'][2]['with']['python-version']
assert standalone_version == '3.13', f'unexpected standalone version: {standalone_version}'
print('ci-matrix-ok')
" 2>&1 || echo "yaml parse check — install pyyaml or verify manually with grep"</automated>
  </verify>
  <acceptance_criteria>
    - `python-version: ['3.11', '3.12', '3.13']` line is commented out
    - `python-version: ['3.13']` is active in the `test` job matrix
    - `python-version: '3.10'` line in `standalone-no-django` is commented out
    - `python-version: '3.13'` is active in the `standalone-no-django` job
    - No other YAML structure is altered (django-version matrix, steps, service containers)
    - `grep -c "3\.11\|3\.12\|3\.10" .github/workflows/test.yml` may return matches (commented-out lines are acceptable); grep on active YAML parse returns 0
  </acceptance_criteria>
  <done>
    CI matrix runs only Python 3.13. The standalone-no-django import guard also uses Python 3.13.
    3.11 and 3.12 lines are commented out, not deleted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md Constraints section to reflect 3.13+ floor</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 94-101 (Constraints section — already has a commented-out 3.10 line and an active 3.13 line per the doc ingest, but verify the current state before editing)
  </read_first>
  <action>
    In `.planning/PROJECT.md` at the Constraints section (around lines 94-101):

    Read the current state of the Constraints section. The doc ingest already added:
      `<!-- - **Python version**: 3.10+ minimum (uses X | None union syntax) -->`
      `- **Python version**: 3.13+ minimum (...) — floor raised 2026-06-10 ...`

    If the section already has the 3.13 constraint active and 3.10 commented out, this task is
    a no-op — confirm and document in the SUMMARY.

    If the section still has `3.10+` active (not yet commented out), apply the same pattern:
    comment out the 3.10 line and add/verify the 3.13 line is active with the full description:
    `- **Python version**: 3.13+ minimum (requires-python = ">=3.13") — floor raised 2026-06-10 (user decision, INGEST-CONFLICTS.md Resolution Log); ships in v0.24.0 Phase 12 (pyproject bump + CI matrix drops 3.11/3.12)`

    Do NOT change any other section of PROJECT.md.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && grep -v "^<!--\|^ *<!--\|^ *-->" .planning/PROJECT.md | grep -c "3\.13" | grep -v "^0$" && echo "constraint-ok"</automated>
  </verify>
  <acceptance_criteria>
    - The active (non-commented) Constraints section line references Python 3.13+
    - The old Python 3.10 constraint line (if present) is commented out
    - No other sections of PROJECT.md are modified
    - `grep "3\.13" .planning/PROJECT.md` returns at least one active line in the Constraints section
  </acceptance_criteria>
  <done>
    PROJECT.md Constraints section actively states Python 3.13+ as the floor, consistent with
    pyproject.toml and the CI matrix.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---|---|---|
| `pyproject.toml` | Updated config | `requires-python = ">=3.13"`; classifiers for 3.10/3.11/3.12 commented out |
| `.github/workflows/test.yml` | Updated CI config | Matrix drops 3.11/3.12; standalone-no-django job updated from 3.10 to 3.13 |
| `.planning/PROJECT.md` | Updated planning doc | Constraints section confirms Python 3.13+ floor |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml `requires-python` → pip/uv resolver | Downstream installs on Python < 3.13 will now receive a resolver error instead of installing silently and failing at import time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Denial of Service | Dropping 3.11/3.12 from CI — existing users on those versions get no CI coverage signal | accept | User-approved decision (INGEST-CONFLICTS.md). Downstream users on 3.11/3.12 will receive a packaging resolver error pointing them to upgrade. |
| T-12-03-02 | Tampering | pyproject.toml classifiers inconsistent with requires-python — could mislead pip resolver | mitigate | Task 1 acceptance criteria verify via tomllib parse that no 3.10/3.11/3.12 classifiers remain active |
| T-12-03-SC | Tampering | pip/cargo installs (slopcheck) | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['requires-python']=='>=3.13'; print('ok')"` prints `ok`
2. `grep -v '^#\|^ *#' .github/workflows/test.yml | grep "python-version.*3\.11\|python-version.*3\.12\|python-version.*3\.10"` returns empty
3. `grep "python-version.*3\.13" .github/workflows/test.yml | grep -v "^#\|^ *#"` returns at least 2 matches
4. `grep -v "<!--" .planning/PROJECT.md | grep "3\.13"` returns at least 1 active line in Constraints
</verification>

<success_criteria>
- `pyproject.toml`: `requires-python = ">=3.13"` active; 3.10/3.11/3.12 classifier lines commented out
- `.github/workflows/test.yml`: matrix is `['3.13']` only; `standalone-no-django` uses 3.13
- `.planning/PROJECT.md`: Constraints section references 3.13+ as the active floor
- Old lines are commented out, not deleted, in all three files (per project convention)
- No source code or test files modified in this plan
</success_criteria>

<output>
Create `.planning/phases/12-quick-wins/12-03-SUMMARY.md` when done
</output>
