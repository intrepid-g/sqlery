---
phase: 03-testing-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/migrations/0022_delete_daemonlease_alter_jobregistry_metadata_and_more.py
  - src/sqlery/django_sqlery/migrations/0023_restore_daemonlease.py
  - tests/test_d_02_07_1_regression.py
autonomous: false
requirements: [TEST-01, TEST-02]
tags: [migrations, django, regression, foundation]

must_haves:
  truths:
    - "`python manage.py makemigrations --check` reports no model drift after the fix"
    - "`pytest tests/test_models.py::TestScheduledTask::test_scheduled_task_creation -x` passes from a clean SQLite (the canonical D-02-07-1 reproducer)"
    - "`pytest tests/integration/test_modes.py -x` is no longer blocked by setup_databases crashing on `sqlery_daemon_lease already exists`"
    - "A regression test invokes pytest-django's `setup_databases` from a clean SQLite and asserts no IntegrityError/OperationalError"
  artifacts:
    - path: "src/sqlery/django_sqlery/migrations/0022_delete_daemonlease_alter_jobregistry_metadata_and_more.py"
      provides: "Migration whose filename matches its operations (DeleteModel either present, or filename intent reconciled per chosen fix path)"
    - path: "src/sqlery/django_sqlery/migrations/0023_restore_daemonlease.py"
      provides: "No-op (operations = []) preserving the migration-graph node for already-deployed users — file is NOT deleted (CLAUDE.md backward-compat)"
    - path: "tests/test_d_02_07_1_regression.py"
      provides: "Regression test that fails if the duplicate-CreateModel bug reappears"
      min_lines: 30
  key_links:
    - from: "0023_restore_daemonlease.py"
      to: "migration graph"
      via: "operations=[]; dependencies preserved"
      pattern: "operations\\s*=\\s*\\[\\s*\\]"
    - from: "tests/test_d_02_07_1_regression.py"
      to: "pytest-django setup_databases"
      via: "django.test.utils.setup_databases against a tmpdir SQLite"
      pattern: "setup_databases"
---

<objective>
Fix D-02-07-1 (deferred from Phase 2): pytest-django's `setup_databases` crashes with `sqlery_daemon_lease already exists` because migration `0022_delete_daemonlease_...` advertises a DeleteModel via filename but its `operations` list contains only AlterField calls. The previously-committed `0023_restore_daemonlease.py` then attempted an unconditional CreateModel('DaemonLease'), colliding with the still-existing table from `0020_daemon_lease.py`.

Per CONTEXT decision B (targeted duplicate-CreateModel deletion) and the RESEARCH root-cause analysis: turn `0023_restore_daemonlease.py` into a true no-op (operations=[]) while preserving the file so deployed databases that already applied it still have a graph node to reference. Leave `0022` alone unless the no-op-`0023` route fails pytest-django, in which case fall back to the RESEARCH "alternative path": add an explicit `DeleteModel('DaemonLease') + CreateModel('DaemonLease')` pair inside `0023` so the state machine has a clean delete+recreate matching `0022`'s filename intent.

Purpose: Unblock every integration-test plan in Phase 3 (and Phase 2's TEST-01/02 success criteria).
Output: Edited migration(s) + a regression test that locks the bug closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testing-ci/03-CONTEXT.md
@.planning/phases/03-testing-ci/03-RESEARCH.md
@.planning/phases/02-execution-modes/deferred-items.md
@CLAUDE.md

<interfaces>
From RESEARCH (verified file reads):
- `src/sqlery/django_sqlery/migrations/0020_daemon_lease.py:12-27` — CreateModel('DaemonLease'). Table exists.
- `src/sqlery/django_sqlery/migrations/0022_*.py:13-73` — `operations = [...]` lists 11 AlterFields, NO DeleteModel.
- `src/sqlery/django_sqlery/migrations/0023_restore_daemonlease.py:4-57` — currently a conditional RunPython; lines 38-53 contain the original unconditional CreateModel (commented out).

Reproducer command (per deferred-items.md:22-24):
  `pytest tests/test_models.py::TestScheduledTask::test_scheduled_task_creation -x`

Backward-compat constraint (CLAUDE.md): users may already have `0023` applied. Do not delete the file — replace its operations with `[]` and keep the `dependencies` line intact so the migration graph still resolves.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce 0023_restore_daemonlease.py to a no-op migration</name>
  <files>src/sqlery/django_sqlery/migrations/0023_restore_daemonlease.py</files>
  <read_first>
    - src/sqlery/django_sqlery/migrations/0023_restore_daemonlease.py (entire file)
    - src/sqlery/django_sqlery/migrations/0020_daemon_lease.py (confirm CreateModel content)
    - src/sqlery/django_sqlery/migrations/0022_delete_daemonlease_alter_jobregistry_metadata_and_more.py (confirm operations list lacks DeleteModel)
    - .planning/phases/02-execution-modes/deferred-items.md (full deferred entry for D-02-07-1)
  </read_first>
  <action>
    Replace the body of `0023_restore_daemonlease.py` with a minimal `migrations.Migration` whose `operations = []`. Keep:
    - The `dependencies` list exactly as-is (points back to `0022_*`).
    - The class name `Migration`.
    - A module docstring explaining: "No-op migration — preserves graph node for users who applied earlier conditional-RunPython versions. The original Create-if-missing logic is unnecessary because 0020 created the table and 0022 never deleted it (filename intent vs operations list mismatch). See `.planning/phases/03-testing-ci/03-01-PLAN.md` for the full root cause."

    Per CLAUDE.md feedback_dead_code policy, keep the existing commented-out CreateModel block at lines 38-53 but ADD a date marker: `# #CLEANUP 2026-05-14: dead — remove after Phase 4`.

    Run `python manage.py makemigrations --check --dry-run` after editing — it must report "No changes detected".
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && uv run python -c "import django; import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','tests.settings'); django.setup(); from django.core.management import call_command; call_command('makemigrations','--check','--dry-run')"</automated>
  </verify>
  <acceptance_criteria>
    - `0023_restore_daemonlease.py` contains `operations = []`.
    - `dependencies` line unchanged.
    - `makemigrations --check` reports no changes.
    - The dead commented block carries the `#CLEANUP 2026-05-14` marker.
  </acceptance_criteria>
  <done>Migration reduced to no-op; graph still resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression test for D-02-07-1 (setup_databases on clean SQLite)</name>
  <files>tests/test_d_02_07_1_regression.py</files>
  <read_first>
    - tests/conftest.py
    - tests/integration/conftest.py (for fixture conventions)
    - tests/test_models.py (canonical reproducer)
  </read_first>
  <action>
    Create `tests/test_d_02_07_1_regression.py` with two tests:

    1. `test_scheduled_task_creation_does_not_trip_daemon_lease()` — directly invokes the canonical reproducer logic (import `ScheduledTask`, create one, assert no `OperationalError`/`IntegrityError` about `sqlery_daemon_lease`). Use `@pytest.mark.django_db`. This is the executable equivalent of `pytest tests/test_models.py::TestScheduledTask::test_scheduled_task_creation -x`.

    2. `test_setup_databases_from_clean_sqlite()` — uses `subprocess.run` to spawn a fresh Python that:
       - sets `DJANGO_SETTINGS_MODULE=tests.settings` with a tmpdir SQLite path
       - calls `django.setup()` then `django.test.utils.setup_databases(verbosity=0, interactive=False)`
       - asserts return code 0 and that stderr contains no `already exists` substring

    Both tests must pass on a fresh checkout (no preexisting `*.sqlite3` files). Use `tmp_path` fixture for DB isolation.
  </action>
  <verify>
    <automated>uv run pytest tests/test_d_02_07_1_regression.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists with both tests, >= 30 lines.
    - Both tests pass on clean SQLite.
    - Removing Task 1's fix causes `test_setup_databases_from_clean_sqlite` to fail (manual verification: revert 0023, re-run, expect failure).
  </acceptance_criteria>
  <done>Regression locked in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the wider integration suite is unblocked</name>
  <what-built>
    Migration 0023 reduced to a no-op (Task 1). Regression test added (Task 2).
  </what-built>
  <how-to-verify>
    1. Run `uv run pytest tests/integration/test_modes.py -x` — expect setup_databases to succeed (whether downstream tests all pass is a separate concern; we only need the "already exists" crash gone).
    2. Run `uv run pytest tests/test_models.py::TestScheduledTask::test_scheduled_task_creation -x` — must pass.
    3. Run `uv run pytest tests/test_d_02_07_1_regression.py -v` — both regression tests green.
    4. Confirm `python manage.py makemigrations --check` (against `tests.settings`) reports no drift.

    If pytest-django still trips on something else (per RESEARCH Pitfall #1: setup_databases ≠ manage.py migrate), execute the RESEARCH fallback inline: edit `0023` to contain `DeleteModel('DaemonLease') + CreateModel('DaemonLease')` instead of empty operations. Re-run all four checks.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe the remaining failure mode.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration graph state ↔ runtime DB schema | A mismatch causes silent data-shape drift across deployed databases |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | migration 0023 | mitigate | Preserve file (not delete) so deployed databases retain a graph node; operations=[] is idempotent |
| T-03-02 | Repudiation | migration 0022 filename vs operations | accept | Leaving 0022 as-is; filename misleading but harmless once 0023 is no-op. Document in 0023 docstring. |
</threat_model>

<verification>
- `makemigrations --check` clean.
- Reproducer test passes.
- Subprocess `setup_databases` test passes.
- Integration `test_modes.py` no longer crashes in setup.
</verification>

<success_criteria>
1. pytest-django's `setup_databases` succeeds from a clean SQLite.
2. The canonical D-02-07-1 reproducer passes.
3. Migration graph resolves without DeleteModel ↔ filename complaints from `makemigrations --check`.
4. No deployed-DB users break (0023 file preserved, dependencies intact).
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci/03-01-SUMMARY.md`.
</output>
