---
phase: 03-testing-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
  - pyproject.toml
autonomous: false
requirements: [TEST-11, TEST-12]
tags: [ci, github-actions, postgres, markers]

must_haves:
  truths:
    - "`.github/workflows/test.yml` triggers on push/PR to `main` (not `master`)"
    - "`pytest -m postgres` is a recognized invocation (no `PytestUnknownMarkWarning`)"
    - "A new CI job (or matrix axis) runs `pytest -m postgres` against the existing `postgres:15` service container"
    - "The default SQLite CI job deselects `-m postgres` so PG-only tests do not run there"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Main-branch triggers + postgres CI job scaffold (empty selector OK until 03-07 expands tagged tests)"
      contains: "branches: [ main ]"
    - path: "pyproject.toml"
      provides: "`postgres` marker registered in [tool.pytest.ini_options].markers"
      contains: "postgres:"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "postgres:15 service container (existing block lines 18-30)"
      via: "new job step `pytest -m postgres`"
      pattern: "pytest -m postgres"
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "pytest marker registry"
      via: "`postgres: requires PostgreSQL service container`"
      pattern: "postgres:"
---

<objective>
Two foundation edits that unblock the rest of Wave 2/3:

1. TEST-12 — flip CI triggers from `master` to `main`. One-line two-word edit, identified by RESEARCH as still NOT fixed (`.github/workflows/test.yml:5-7`).
2. TEST-11 scaffold — register the `@pytest.mark.postgres` marker (CONTEXT decision A; ASSUMED-but-collision-free per RESEARCH) AND add the CI plumbing to run `pytest -m postgres` against the existing `postgres:15` service container. The actual tagging of tests happens in Plan 03-07; this plan only builds the rails.

Purpose: Unblock Plans 03-03..08 from operating on the wrong branch and from running unmarked tests in a job that can't reach Postgres.
Output: Edited workflow + pyproject marker registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testing-ci/03-CONTEXT.md
@.planning/phases/03-testing-ci/03-RESEARCH.md
@.github/workflows/test.yml
@pyproject.toml

<interfaces>
From RESEARCH (verified):
- `.github/workflows/test.yml:5-7`: `branches: [ master ]` (in both `push` and `pull_request`).
- `.github/workflows/test.yml:18-30`: existing `postgres:15` service container — reuse, do not duplicate.
- `pyproject.toml:139-145`: `[tool.pytest.ini_options]` block; only `slow` marker registered.
- `pytest_collection_modifyitems` pattern in `tests/integration/conftest.py:95-109` for engine-based skip (template for postgres marker routing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `postgres` pytest marker</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (full [tool.pytest.ini_options] block)
  </read_first>
  <action>
    In `pyproject.toml`'s `[tool.pytest.ini_options]` table, extend the `markers` list to include:
    ```
    "postgres: requires a running PostgreSQL service (skipped on SQLite-only jobs)"
    ```
    Keep `slow` and any other existing markers untouched. Do not change `testpaths`, `DJANGO_SETTINGS_MODULE`, or filter warnings unless they're already there.

    Verify locally: `uv run pytest --markers | grep -E '^@pytest.mark.postgres'` returns a hit.
  </action>
  <verify>
    <automated>uv run pytest --markers 2>&1 | grep -q "@pytest.mark.postgres" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` lists `postgres` in the markers array.
    - `pytest --markers` shows the new marker with the description.
    - No other pyproject changes (diff confined to the markers list).
  </acceptance_criteria>
  <done>Marker registered.</done>
</task>

<task type="auto">
  <name>Task 2: Switch CI triggers from `master` to `main` AND add `postgres` CI job step</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (entire file)
  </read_first>
  <action>
    1. Replace both occurrences of `branches: [ master ]` at lines 5-7 with `branches: [ main ]`.

    2. Locate the existing job that already provisions the `postgres:15` service container (the one running `test_atomic_claiming.py` / `test_atomic_scheduler.py` against PG, per RESEARCH). Add a NEW step (after the existing PG test step) named `Run @pytest.mark.postgres suite`:
       ```yaml
       - name: Run @pytest.mark.postgres suite
         env:
           SQLERY_TEST_PG_URL: postgresql://postgres:postgres@localhost:5432/postgres
         run: uv run pytest -m postgres -v --tb=short
       ```
       It is OK if this step initially finds zero tests — Plan 03-07 tags them. Use `--strict-markers` is NOT required because pyproject registers it. Use `pytest -m postgres` and explicitly allow empty collection: pass `|| true` ONLY if pytest returns exit code 5 (no tests collected). Prefer: `bash -c 'uv run pytest -m postgres -v --tb=short; rc=$?; [ $rc -eq 0 ] || [ $rc -eq 5 ]'`. Document that the `|| code 5` exception will be removed in 03-07 once tests are tagged.

    3. In the SQLite-only CI jobs (the default test matrix and the `standalone-no-django` job from Phase 1), ensure `pytest` invocations include `-m "not postgres"` so PG-only tests are skipped. If the existing `pytest` step uses `pytest tests/`, change to `pytest tests/ -m "not postgres"`. Leave coverage flags untouched.

    Do not refactor other jobs; do not bump action versions. Keep YAML diff minimal and idiomatic.
  </action>
  <verify>
    <automated>grep -E "^on:|branches:.*main" .github/workflows/test.yml | grep -vE "master" && ! grep -E "branches:.*master" .github/workflows/test.yml && grep -q "pytest -m postgres" .github/workflows/test.yml && grep -qE '\-m "not postgres"|-m '"'"'not postgres'"'"'' .github/workflows/test.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both push and pull_request triggers on `main`.
    - At least one job step invokes `pytest -m postgres` against the PG service container.
    - All SQLite-mode pytest invocations include `-m "not postgres"`.
    - No regression in existing jobs (diff is purely additive + branch rename).
    - No `master` token remains under triggers.
  </acceptance_criteria>
  <done>Triggers fixed; PG job rail in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies CI is green on a `main`-targeted PR</name>
  <what-built>
    Pytest `postgres` marker registered (Task 1). CI triggers flipped to `main` and a `pytest -m postgres` step added to the PG job; SQLite jobs deselect `-m postgres` (Task 2).
  </what-built>
  <how-to-verify>
    1. Push a branch and open a PR targeting `main`. Confirm the workflow runs (it would not have on the old `master` trigger).
    2. In the PG job logs, confirm the `Run @pytest.mark.postgres suite` step runs and reports "no tests ran" (exit code 5 tolerated) — that's expected until 03-07.
    3. In the SQLite job logs, confirm `-m "not postgres"` is in the pytest invocation.
    4. Confirm no existing CI job regressed.
  </how-to-verify>
  <resume-signal>Type "approved" if CI runs on `main` and the postgres step is visible.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI workflow ↔ default branch | Triggers on wrong branch = no CI signal at all |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Repudiation | CI green claim on `master` | mitigate | Trigger flipped to `main`; PR opening proves it |
| T-03-04 | DoS | empty pytest run | accept | Exit code 5 tolerated only until 03-07 tags tests |
</threat_model>

<verification>
- `pytest --markers` lists `postgres`.
- Workflow yaml validates and lists `main` triggers.
- PR against `main` actually triggers CI.
</verification>

<success_criteria>
1. TEST-12 closed: CI runs on `main`.
2. TEST-11 scaffolded: `pytest -m postgres` rail exists; SQLite jobs deselect it.
3. No regressions in unrelated jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci/03-02-SUMMARY.md`.
</output>
