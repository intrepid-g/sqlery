---
phase: 03-testing-ci
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/unit/test_sqlalchemy_backend_sync.py
  - tests/unit/test_django_backend.py
autonomous: true
requirements: [TEST-08, TEST-09]
tags: [unit-tests, backends, django, sqlalchemy]

must_haves:
  truths:
    - "`tests/unit/test_sqlalchemy_backend_sync.py` exercises every method of `fastapi_sqlery/backend.py` (sync `SQLAlchemyBackend`)"
    - "`tests/unit/test_django_backend.py` exercises every method of `django_sqlery/backend.py` (`DjangoBackend`)"
    - "SQLAlchemy tests use a temp-file SQLite (NOT `:memory:`) so WAL paths exercise; sessions cleaned up per-test"
    - "Django tests use `@pytest.mark.django_db` and depend on the 03-01 migration fix to load fixtures cleanly"
    - "Per-module coverage of both backends ≥ 80% when running just these two files"
  artifacts:
    - path: "tests/unit/test_sqlalchemy_backend_sync.py"
      provides: "Method-by-method tests of sync SQLAlchemyBackend"
      min_lines: 200
    - path: "tests/unit/test_django_backend.py"
      provides: "Method-by-method tests of DjangoBackend"
      min_lines: 200
  key_links:
    - from: "tests/unit/test_sqlalchemy_backend_sync.py"
      to: "fastapi_sqlery.backend.SQLAlchemyBackend"
      via: "direct instantiation against temp-file SQLite engine"
      pattern: "SQLAlchemyBackend\\("
    - from: "tests/unit/test_django_backend.py"
      to: "django_sqlery.backend.DjangoBackend"
      via: "Django ORM via pytest-django fixtures"
      pattern: "@pytest.mark.django_db"
---

<objective>
TEST-08 (sync `SQLAlchemyBackend`) and TEST-09 (`DjangoBackend`) — RESEARCH shows the sync SQLAlchemy backend has ZERO unit tests today, and `DjangoBackend` has only TTL-related indirect coverage. Both backends implement 30+ methods of the `DatabaseBackend` ABC. The unit suites here directly call each method against a real (but minimal) DB and assert state.

Distinct from Plan 03-03: 03-03 covers framework-agnostic core with a FakeBackend; 03-04 covers the real backends with real DB engines. Together they prove the ABC contract is both implementable (03-04) and substitutable (03-03).

Purpose: Direct unit coverage for TEST-08 and TEST-09.
Output: Two test modules under `tests/unit/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testing-ci/03-CONTEXT.md
@.planning/phases/03-testing-ci/03-RESEARCH.md
@src/sqlery/fastapi_sqlery/backend.py
@src/sqlery/django_sqlery/backend.py
@src/sqlery/compat/__init__.py
@tests/test_sqlalchemy_async_backend.py
@CLAUDE.md

<interfaces>
From `src/sqlery/fastapi_sqlery/backend.py` (RESEARCH: 891 LOC, sync, ZERO unit tests):
- `SQLAlchemyBackend` implements DatabaseBackend; uses SQLModel sessions. Required init: an engine. Use `create_engine("sqlite:///<tmp_path>/db.sqlite")` per-test (NOT `:memory:` — RESEARCH Pitfall #5).
- Methods include enqueue_job, claim_job, mark_running/complete/failed, update_heartbeat, register_worker, mark_worker_dead, acquire/renew/release_lease, get_due_scheduled_tasks, registry ops, cleanup ops.

From `src/sqlery/django_sqlery/backend.py` (RESEARCH: 903 LOC, partial via TTL only):
- `DjangoBackend` uses Django ORM. Tests must run under pytest-django with `@pytest.mark.django_db`. Plan 03-01 must land first (setup_databases must work).

Reference: `tests/test_sqlalchemy_async_backend.py` is the pattern for the ASYNC sibling; mirror its structure for the sync variant but without `pytest.mark.asyncio`.

CLAUDE.md fork safety: backend tests do NOT fork; they test the synchronous method surface only. Fork lifecycle is in 03-03/03-06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/unit/test_sqlalchemy_backend_sync.py</name>
  <files>tests/unit/test_sqlalchemy_backend_sync.py</files>
  <read_first>
    - src/sqlery/fastapi_sqlery/backend.py (enumerate every public method)
    - src/sqlery/fastapi_sqlery/database.py (engine factory, session helpers)
    - src/sqlery/core/models.py (QueuedJob, WorkerProcess, ScheduledTask, DaemonLease SQLModel definitions)
    - tests/test_sqlalchemy_async_backend.py (pattern reference)
  </read_first>
  <action>
    Preamble (parallel-execution safety): If `tests/unit/__init__.py` does not exist, create it (empty). Do not import from `tests/unit/conftest.py` — that file is owned by Plan 03-03 and may not exist yet when this plan executes in parallel. This task must be self-contained w.r.t. fixtures.

    Create `tests/unit/test_sqlalchemy_backend_sync.py`. Structure:

    ```python
    # fixtures
    @pytest.fixture
    def sync_backend(tmp_path):
        from sqlalchemy import create_engine
        from sqlmodel import SQLModel
        from sqlery.fastapi_sqlery.backend import SQLAlchemyBackend
        engine = create_engine(f"sqlite:///{tmp_path}/db.sqlite", connect_args={"check_same_thread": False})
        SQLModel.metadata.create_all(engine)
        yield SQLAlchemyBackend(engine=engine)
        engine.dispose()
    ```

    Test classes (one per method group; mirror `test_sqlalchemy_async_backend.py`):
    - `TestEnqueueAndClaim`: enqueue → claim returns the job; double-claim returns None; claim respects queue filter.
    - `TestStatusTransitions`: mark_running, mark_complete, mark_failed (with traceback string).
    - `TestRetryAndTTL`: retry increments retry_count; TTL expiry sets a state.
    - `TestWorkerLifecycle`: register_worker, update_heartbeat, mark_worker_dead, list_alive_workers.
    - `TestLeaseLifecycle`: acquire_lease, renew, release; contention test (two acquires; only one wins).
    - `TestScheduledTasks`: get_due_scheduled_tasks honors next_run_at; advance_next_run mutates correctly.
    - `TestRegistry`: add to running/finished/failed registries; range queries.
    - `TestCleanup`: TTL-based cleanup removes expired jobs.

    Coverage target: ≥ 80% of `src/sqlery/fastapi_sqlery/backend.py`.

    Use `@pytest.mark.parametrize` to reduce duplication. Mark zero tests `postgres` here (this is SQLite-only unit coverage); Plan 03-07 owns the PG mirror.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sqlalchemy_backend_sync.py -v --cov=src/sqlery/fastapi_sqlery/backend --cov-report=term --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test classes present.
    - Coverage of `fastapi_sqlery/backend.py` ≥ 80%.
    - Runs in < 15 seconds.
    - Uses temp-file SQLite (not `:memory:`).
  </acceptance_criteria>
  <done>Sync SQLAlchemyBackend fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: tests/unit/test_django_backend.py</name>
  <files>tests/unit/test_django_backend.py</files>
  <read_first>
    - src/sqlery/django_sqlery/backend.py (enumerate every public method)
    - src/sqlery/django_sqlery/models.py (Django models)
    - tests/conftest.py (Django test settings)
    - tests/test_ttl_retention.py (existing DjangoBackend partial coverage — find gaps, do not duplicate)
  </read_first>
  <action>
    Create `tests/unit/test_django_backend.py`. Mirror the 8 test classes from Task 1 but with `@pytest.mark.django_db` on each test and using Django ORM constructs (`QueuedJob.objects.create(...)`).

    Implementation notes:
    - Use `pytest-django`'s `django_db` mark; the harness in 03-01 ensures setup_databases works.
    - `DjangoBackend()` reads from Django settings — no constructor args.
    - For optimistic-locking SQLite branch: include a test that simulates a concurrent `version` mismatch by directly UPDATEing the row outside the backend, then asserts the backend raises `ConcurrentModificationError`.
    - For Postgres-specific `SELECT FOR UPDATE SKIP LOCKED` branch: mark one test `@pytest.mark.postgres` and gate it on `SQLERY_TEST_PG_URL` being set (skip otherwise). Plan 03-07 will tag this for the PG CI job.

    Coverage target: ≥ 80% of `src/sqlery/django_sqlery/backend.py` from this file alone.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_django_backend.py -v --cov=src/sqlery/django_sqlery/backend --cov-report=term --cov-fail-under=80 -m "not postgres"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 method-group test classes present.
    - Coverage of `django_sqlery/backend.py` ≥ 80% (excluding postgres-only branches).
    - At least one `@pytest.mark.postgres` test exists for the SKIP LOCKED branch.
    - `ConcurrentModificationError` is explicitly asserted in at least one test.
  </acceptance_criteria>
  <done>DjangoBackend fully unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| backend method ↔ DB schema | Schema drift (D-02-07-1 class) can mask backend test failures by failing setup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | optimistic-locking race | mitigate | Explicit ConcurrentModificationError assertion in DjangoBackend tests |
| T-03-08 | Repudiation | "all 30+ methods covered" claim | mitigate | Coverage threshold gates the claim; 80% per-file |
</threat_model>

<verification>
- Both files pass independently.
- Coverage thresholds met.
- Postgres-marked tests skip cleanly when `SQLERY_TEST_PG_URL` unset.
</verification>

<success_criteria>
1. TEST-08 closed: sync SQLAlchemyBackend has dedicated unit suite with ≥80% coverage.
2. TEST-09 closed: DjangoBackend has dedicated unit suite with ≥80% coverage (sans PG-only branches).
3. ConcurrentModificationError path covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci/03-04-SUMMARY.md`.
</output>
