---
phase: 03-testing-ci
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - tests/unit/test_webhooks.py
autonomous: true
requirements: [TEST-10]
tags: [unit-tests, webhooks, hmac, retry]

must_haves:
  truths:
    - "`tests/unit/test_webhooks.py` exists and exercises HMAC signing, retry/backoff, HTTP delivery, and event filtering in `src/sqlery/webhooks.py`"
    - "HTTP client is mocked (no real network calls; project lacks `requests` in pyproject.toml deps)"
    - "Per-module coverage of `src/sqlery/webhooks.py` ≥ 80% from this file alone"
  artifacts:
    - path: "tests/unit/test_webhooks.py"
      provides: "Focused unit tests for webhooks.py"
      min_lines: 150
  key_links:
    - from: "tests/unit/test_webhooks.py"
      to: "unittest.mock"
      via: "patch('sqlery.webhooks.requests') or patch the module-level HTTP client object"
      pattern: "mock.patch.*webhooks"
---

<objective>
TEST-10 — RESEARCH confirms `src/sqlery/webhooks.py` has ZERO test files. The module imports `requests` (which is NOT in `pyproject.toml` deps — flagged for Phase 4 CLEAN-04). The unit test must mock the HTTP client to avoid both real network calls and a hard dependency on a non-declared package.

Purpose: Direct unit coverage for TEST-10 (HMAC, retry/backoff, HTTP delivery).
Output: One test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-testing-ci/03-CONTEXT.md
@.planning/phases/03-testing-ci/03-RESEARCH.md
@src/sqlery/webhooks.py
@CLAUDE.md

<interfaces>
From RESEARCH:
- `webhooks.py` is 269 LOC; uses `requests` (undeclared); has HMAC signing, retry with exponential backoff, HTTP POST delivery, event filtering.
- Plan note: do not "fix" the missing `requests` dependency here — that's CLEAN-04 in Phase 4. Mock the HTTP layer instead.

Mocking strategy: `unittest.mock.patch('sqlery.webhooks.requests')` — if `requests` is imported at module top-level, this patches it cleanly; if imported lazily inside a function, patch the function's namespace. Verify by reading the file before writing tests.

CLAUDE.md: SSRF protection is SEC-02 (Phase 4) — out of scope here. Test what currently exists, not what should exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/unit/test_webhooks.py</name>
  <files>tests/unit/test_webhooks.py</files>
  <read_first>
    - src/sqlery/webhooks.py (entire file — find every public function and the `requests` import site)
  </read_first>
  <action>
    Create `tests/unit/test_webhooks.py` with test classes:

    - `TestHMACSigning`: deterministic signature given a known secret + body; signature header name matches spec; different bodies produce different sigs; empty body still signs cleanly.
    - `TestHTTPDelivery`: successful POST (200) marks delivery successful; HTTP 4xx is recorded as failure (does NOT retry per HTTP spec — verify the policy); HTTP 5xx triggers retry path; connection error triggers retry.
    - `TestRetryBackoff`: retry attempts capped at configured max; backoff durations are non-decreasing; second-and-later attempts use the next backoff value (mock `time.sleep` to verify the durations passed without actually sleeping).
    - `TestEventFiltering`: webhook fires for subscribed event types; silently skipped for unsubscribed; wildcard subscription if supported.
    - `TestPayloadShape`: JSON body contains the documented fields (job_id, status, timestamp, etc. — read from webhooks.py to learn the exact contract).

    Mocking — LOCKED to stdlib only:
    - Use `unittest.mock.patch` (or `MagicMock`) directly — `pytest-mock` is intentionally not a dep (`pyproject.toml` does not include it; do not add it).
    - Patch `sqlery.webhooks.requests` (or the underlying HTTP call site) to return Mock responses with `.status_code` and `.text`.
    - Patch `time.sleep` to record call args without sleeping.

    Coverage target: ≥ 80% of `src/sqlery/webhooks.py` from this file alone.

    Mark any test that exercises the SSRF code path (if present) as `@pytest.mark.xfail(reason="SEC-02 in Phase 4")`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_webhooks.py -v --cov=src/sqlery/webhooks --cov-report=term --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - All five test classes present.
    - No real network calls (verify by patching `requests` globally for the file — failing patch would raise during test collection).
    - Coverage of `src/sqlery/webhooks.py` ≥ 80%.
    - No new pyproject dependency added.
    - No `pytest-mock` / `pytest_mock` import anywhere in the file.
  </acceptance_criteria>
  <done>webhooks.py directly unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| webhook outbound HTTP | Untrusted destination URL — but SSRF mitigation is SEC-02 (Phase 4) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | HMAC signature scheme | mitigate | Deterministic-signature test pins the algorithm |
| T-03-10 | Information disclosure | SSRF risk in webhooks.py | transfer | Tracked as SEC-02 in Phase 4; out of scope here |
</threat_model>

<verification>
- File passes; coverage ≥ 80%.
- Zero network access during test run.
</verification>

<success_criteria>
1. TEST-10 closed.
2. SSRF risk explicitly flagged via xfail markers (handoff to Phase 4 SEC-02).
3. No new runtime dep introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci/03-05-SUMMARY.md`.
</output>
