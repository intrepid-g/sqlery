---
phase: 02-execution-modes
plan: 08
type: execute
wave: 5
depends_on: [02-06, 02-07]
files_modified:
  - src/sqlery/fastapi_sqlery/subprocess_executor.py
  - src/sqlery/core/triggers.py
  - src/sqlery/core/signature.py
  - src/sqlery/django_sqlery/signature.py
  - src/sqlery/django_sqlery/views.py
  - src/sqlery/fastapi_sqlery/triggers.py
  - src/sqlery/fastapi_sqlery/app.py
  - src/sqlery/fastapi_sqlery/lambda_handler.py
  - src/sqlery/core/lambda_core.py
  - tests/integration/test_modes.py
  - tests/integration/test_lambda_django.py
  - tests/integration/test_lambda_standalone.py
autonomous: true
requirements: [SMOD-02, SMOD-03, DMOD-04, SMOD-04, DMOD-06, SMOD-06]
tags: [standalone-impl, http-trigger, lambda, async-e2e]

must_haves:
  truths:
    - "SMOD-02: standalone subprocess executor exists and the (subprocess, standalone, sqlite) matrix cell passes"
    - "SMOD-03: `core/triggers.py::handle(envelope)` is a pure function; Django view and FastAPI route both delegate to it"
    - "SMOD-03: (http-trigger, standalone, sqlite) matrix cell passes"
    - "DMOD-04: `tests/integration/test_lambda_django.py` imports `sqlery.lambda_handler`, invokes with a fake EventBridge dict, asserts a job lifecycle"
    - "SMOD-04: `src/sqlery/fastapi_sqlery/lambda_handler.py` exists with NO Django imports; smoke test passes"
    - "DMOD-06 + SMOD-06: (async, django, sqlite) and (async, standalone, sqlite) matrix cells pass — AsyncWorker E2E"
    - "Existing top-level `src/sqlery/triggers.py` (147-line strategy file) is LEFT LIVE per CONTEXT open-question 3; new `core/triggers.py` is a distinct HTTP-receiver surface"
  artifacts:
    - path: "src/sqlery/fastapi_sqlery/subprocess_executor.py"
      provides: "Standalone subprocess execution: spawns `python -m sqlery.core.worker_runner` with standalone config"
    - path: "src/sqlery/core/triggers.py"
      provides: "Pure-function HTTP trigger receiver (handle(envelope) -> result)"
      contains: "def handle"
    - path: "src/sqlery/core/signature.py"
      provides: "Framework-agnostic HMAC sign/verify (moved from django_sqlery/signature.py)"
    - path: "src/sqlery/django_sqlery/signature.py"
      provides: "Dated stub re-exporting from core.signature (Phase 1 stub policy)"
    - path: "src/sqlery/fastapi_sqlery/triggers.py"
      provides: "FastAPI POST /trigger route adapting core.triggers.handle"
    - path: "src/sqlery/fastapi_sqlery/lambda_handler.py"
      provides: "Standalone Lambda entry point — no Django imports"
      contains: "def handler"
    - path: "src/sqlery/core/lambda_core.py"
      provides: "Mode-agnostic claim+execute helper used by both Lambda handlers"
  key_links:
    - from: "src/sqlery/fastapi_sqlery/lambda_handler.py"
      to: "src/sqlery/core/lambda_core.py"
      via: "imports + calls the mode-agnostic helper; explicit no-Django guarantee"
      pattern: "from sqlery.core.lambda_core"
    - from: "src/sqlery/django_sqlery/views.py"
      to: "src/sqlery/core/triggers.py::handle"
      via: "trigger_view builds envelope from request, calls handle, returns JsonResponse"
      pattern: "core\\.triggers\\.handle|triggers\\.handle"
    - from: "src/sqlery/fastapi_sqlery/triggers.py"
      to: "src/sqlery/core/triggers.py::handle"
      via: "FastAPI POST /trigger handler calls handle"
      pattern: "core\\.triggers\\.handle"
---

<objective>
Land the 6 net-new mode implementations + their E2E tests in one plan. Each of these items is unblocked by Wave 1–4 (migrations, async backends, AsyncWorker, test harness). Per CONTEXT decisions D (pure-core trigger) and E (Lambda smoke-only), this is bounded scope — no LocalStack, no SAM. The trigger refactor is structural (file moves + dated stubs) but does NOT rewrite existing logic; it lifts signature verification and dispatch into `core/triggers.py` and adds two framework-thin adapters.

Purpose: Close every remaining Phase-2 requirement.
Output: 4 new implementation modules, 2 new test modules, 2 dated stubs, harness rows turned from skip to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@src/sqlery/django_sqlery/signature.py
@src/sqlery/django_sqlery/views.py
@src/sqlery/django_sqlery/http_trigger_middleware.py
@src/sqlery/lambda_handler.py
@src/sqlery/fastapi_sqlery/app.py
@src/sqlery/core/worker_runner.py

<interfaces>
<!-- Envelope shape for core.triggers.handle (CONTEXT decision D) -->
@dataclass
class TriggerEnvelope:
    body: bytes
    headers: dict[str, str]   # at minimum: X-Sqlery-Signature, X-Sqlery-Timestamp
    payload: dict             # parsed JSON; keys: action ('process_queue'|'process_scheduled'), queue_name?, task_id?

@dataclass
class TriggerResult:
    status_code: int          # 200 success, 401 bad-sig, 409 idempotency hit, 400 bad-payload
    body: dict                # {'ok': True, 'job_ids': [...]} or {'error': '...'}

def handle(envelope: TriggerEnvelope) -> TriggerResult: ...

<!-- Standalone Lambda handler shape (RESEARCH §E, §5) -->
def handler(event: dict, context) -> dict:
    from sqlery.compat import initialize, get_backend
    initialize(database_url=os.environ["SQLERY_DATABASE_URL"])
    from sqlery.core.lambda_core import process_event
    return process_event(event, get_backend())
# NOTE: must NOT import django, django_sqlery, or call setup_django anywhere in the import chain.

<!-- AsyncWorker harness rows -->
For mode='async', the harness fixture spins up an AsyncWorker in a background asyncio task,
enqueues a job, awaits status='finished', then signals the worker to shut down with deadline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMOD-02 — Standalone subprocess executor</name>
  <files>src/sqlery/fastapi_sqlery/subprocess_executor.py, tests/integration/test_modes.py</files>
  <action>
    Create `src/sqlery/fastapi_sqlery/subprocess_executor.py` defining `def spawn_subprocess_worker(database_url: str, queues: list[str], one_shot: bool = True) -> int` that runs `python -m sqlery.core.worker_runner --standalone --database-url ... --queues ... --one-shot` and returns the exit code. Reuse fork-safety considerations (close engine before spawn — RESEARCH §8 confirms fork-safety needed for subprocess but NOT async).

    Wire into the harness: edit `tests/integration/test_modes.py` to remove the skip for (subprocess, standalone, *). Implement the harness branch in `conftest.py` (extend Plan 02-07's harness).

    Acceptance: (subprocess, standalone, sqlite) matrix cell passes.
  </action>
  <verify>
    <automated>pytest "tests/integration/test_modes.py::test_mode_e2e[sqlite-standalone-subprocess]" -x</automated>
  </verify>
  <done>Cell green; subprocess actually executes the job and writes 'finished' to the standalone DB.</done>
</task>

<task type="auto">
  <name>Task 2: SMOD-03 — Pure-core trigger handler + adapters</name>
  <files>src/sqlery/core/signature.py, src/sqlery/django_sqlery/signature.py, src/sqlery/core/triggers.py, src/sqlery/django_sqlery/views.py, src/sqlery/fastapi_sqlery/triggers.py, src/sqlery/fastapi_sqlery/app.py, tests/integration/test_modes.py</files>
  <action>
    1. Move HMAC helpers: copy current contents of `src/sqlery/django_sqlery/signature.py` to new `src/sqlery/core/signature.py` (strip Django imports — RESEARCH §6 confirms it's already pure HMAC). Replace `src/sqlery/django_sqlery/signature.py` with a dated stub:
       ```
       # #CLEANUP: 2026-05-13 — moved to sqlery.core.signature. Remove after 2026-11-13.
       from sqlery.core.signature import *  # noqa
       ```

    2. Create `src/sqlery/core/triggers.py` exposing `TriggerEnvelope`, `TriggerResult`, and `handle(envelope) -> TriggerResult`. Logic: verify signature via `core.signature.verify_signed_request`; check idempotency cache (use an in-memory LRU keyed by `(timestamp, signature)` for now — flag as future replacement); dispatch to subprocess/thread/django-tasks strategy based on `envelope.payload['action']` (delegate to the existing top-level `sqlery.triggers` strategy module per CONTEXT open-question 3 — DO NOT rewrite the 147-line file).

    3. Update `src/sqlery/django_sqlery/views.py`: refactor the existing trigger view to build a `TriggerEnvelope` from the Django `request` and call `core.triggers.handle()`. Translate `TriggerResult` to `JsonResponse`. Preserve URL routing and existing tests.

    4. Create `src/sqlery/fastapi_sqlery/triggers.py` with a `router = APIRouter()` exposing `POST /trigger` that builds an envelope from the FastAPI `Request`, calls `handle()`, returns the FastAPI response. Mount the router in `src/sqlery/fastapi_sqlery/app.py` via `app.include_router(triggers.router)`.

    5. Wire harness: remove skip for (http-trigger, standalone, *) in `test_modes.py` and conftest. The harness branch uses `httpx.AsyncClient(app=app)` to POST a signed envelope.

    Acceptance: existing 18 trigger tests still pass; (http-trigger, standalone, sqlite) cell passes.
  </action>
  <verify>
    <automated>pytest tests/test_triggers.py "tests/integration/test_modes.py::test_mode_e2e[sqlite-standalone-http-trigger]" -x</automated>
  </verify>
  <done>Old trigger tests green; new cell green; Django view + FastAPI router both delegate to `core.triggers.handle`.</done>
</task>

<task type="auto">
  <name>Task 3: DMOD-04 + SMOD-04 — Lambda smoke tests + standalone handler</name>
  <files>src/sqlery/core/lambda_core.py, src/sqlery/fastapi_sqlery/lambda_handler.py, tests/integration/test_lambda_django.py, tests/integration/test_lambda_standalone.py</files>
  <action>
    1. Extract a mode-agnostic helper `src/sqlery/core/lambda_core.py::process_event(event: dict, backend) -> dict`. Logic: switch on `event['action']` (`process_queue`, `process_scheduled`), claim N jobs from `event.get('queue_name', 'default')`, execute synchronously, return `{'processed': [...], 'failed': [...]}`. Reuse the existing executor from `core/worker.py`.

    2. Refactor `src/sqlery/lambda_handler.py` minimally to call `process_event(event, get_backend())` for the Django branch (keeps existing setup_django() entrypoint untouched — RESEARCH §5 says don't try to make this file mode-agnostic).

    3. Create `src/sqlery/fastapi_sqlery/lambda_handler.py` with `def handler(event, context)` per `<interfaces>` shape. ZERO Django imports. Verify with `grep -E "import django|from django|setup_django" src/sqlery/fastapi_sqlery/lambda_handler.py` returns empty.

    4. Write `tests/integration/test_lambda_django.py`:
       ```
       def test_lambda_django_smoke(django_db_setup):
           from sqlery.lambda_handler import handler
           enqueue(simple_job, args=(1, 2))
           result = handler({"action": "process_queue", "queue_name": "default"}, None)
           assert result["processed"]
       ```

    5. Write `tests/integration/test_lambda_standalone.py` (mirror, importing from `sqlery.fastapi_sqlery.lambda_handler`). Run in a subprocess-isolated test per Plan 01-03 pattern, scrubbing `DJANGO_SETTINGS_MODULE` from env, to prove the no-Django property.
  </action>
  <verify>
    <automated>pytest tests/integration/test_lambda_django.py tests/integration/test_lambda_standalone.py -x</automated>
  </verify>
  <done>Both smoke tests pass; standalone handler has zero Django references.</done>
</task>

<task type="auto">
  <name>Task 4: DMOD-06 + SMOD-06 — AsyncWorker E2E matrix rows</name>
  <files>tests/integration/test_modes.py, tests/integration/conftest.py</files>
  <action>
    Extend conftest.py's harness with a `mode == 'async'` branch:
    - Build an `AsyncDatabaseBackend` (DjangoAsyncBackend or SQLAlchemyAsyncBackend depending on integration).
    - Construct an `AsyncWorker(backend, queues=['default'], shutdown_deadline_seconds=5)`.
    - In a `pytest-asyncio` test, schedule `worker.run()` as a background task, `await harness.enqueue(...)`, poll `await backend.aget_status(job_id)` until 'finished' or 30s timeout, then trigger `worker._initiate_shutdown()` and `await worker_task`.

    Remove the skip for (async, *, *) rows in `test_modes.py`. Use `@pytest.mark.asyncio` on the parametrized test or split into a sibling `test_modes_async.py` if the sync+async parametrization mixes awkwardly.

    Acceptance: (async, django, sqlite) and (async, standalone, sqlite) cells pass; Postgres variants marked slow.
  </action>
  <verify>
    <automated>pytest -x -k "async and sqlite and not slow" tests/integration/</automated>
  </verify>
  <done>Both async cells green; AsyncWorker runs a full lifecycle inside the harness; clean shutdown via deadline.</done>
</task>

</tasks>

<verification>
- All four task verifications green.
- `pytest tests/integration/ -k "not slow"` shows zero skipped cells (besides Postgres without env var).
- `grep -E "import django|from django" src/sqlery/fastapi_sqlery/lambda_handler.py` returns empty.
- `grep -c "core\\.triggers\\.handle\\|triggers\\.handle" src/sqlery/django_sqlery/views.py src/sqlery/fastapi_sqlery/triggers.py | grep -v ':0$' | wc -l` is 2.
</verification>

<success_criteria>
- All 17 Phase-2 requirements (DMOD-01..06, SMOD-01..06, ASYN-01..05) verified observably.
- The parametrized matrix has zero skipped non-Postgres cells.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-08-SUMMARY.md`.
</output>
