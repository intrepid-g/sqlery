---
phase: 02-execution-modes
plan: 04
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - src/sqlery/django_sqlery/async_backend.py
  - src/sqlery/django_sqlery/__init__.py
  - tests/test_django_async_backend.py
autonomous: true
requirements: [ASYN-02]
tags: [async, django, ormpath]

must_haves:
  truths:
    - "`DjangoAsyncBackend` is a concrete subclass of `AsyncDatabaseBackend`"
    - "`await backend.aclaim_job(['default'], 'wkr-1')` claims a queued row atomically via `connection.acursor()` (Postgres) or `aupdate()` CAS (SQLite)"
    - "`await backend.amark_running(job_id, worker_id)` flips status to 'running' and sets worker_id via native async ORM"
    - "`await backend.amark_shutting_down(job_id)` writes 'shutting_down' status (depends on Plan 02 migration)"
    - "All declared async methods use native async ORM (no `sync_to_async` wrappers)"
  artifacts:
    - path: "src/sqlery/django_sqlery/async_backend.py"
      provides: "DjangoAsyncBackend(AsyncDatabaseBackend) implementation"
      contains: "class DjangoAsyncBackend"
    - path: "tests/test_django_async_backend.py"
      provides: "Unit tests for each implemented async method, SQLite-backed"
  key_links:
    - from: "src/sqlery/django_sqlery/async_backend.py"
      to: "src/sqlery/compat/__init__.py::AsyncDatabaseBackend"
      via: "class DjangoAsyncBackend(AsyncDatabaseBackend)"
      pattern: "AsyncDatabaseBackend"
    - from: "src/sqlery/django_sqlery/async_backend.py::aclaim_job"
      to: "django.db.connection.acursor()"
      via: "raw SELECT FOR UPDATE SKIP LOCKED on Postgres"
      pattern: "acursor"
---

<objective>
Implement `DjangoAsyncBackend` against the ASYN-01 contract using **native** Django 5.2 async ORM. Per RESEARCH.md §1 and §A: every method maps cleanly to `.aget()`/`.acreate()`/`.aupdate()`/`async for` EXCEPT the atomic claim path, which drops to raw SQL via `await connection.acursor()` on Postgres (Django's async queryset does not support `select_for_update(skip_locked=True)` as of 5.2). SQLite uses the existing `version`-CAS pattern via `await filter().aupdate()` returning rowcount.

Purpose: Provide the Django side of the async backend so ASYN-04 (AsyncWorker) can run on Django.
Output: New `async_backend.py`, unit tests, exported symbol.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@src/sqlery/compat/__init__.py
@src/sqlery/django_sqlery/backend.py
@src/sqlery/django_sqlery/models.py

<interfaces>
<!-- Pattern for the Postgres claim path (from RESEARCH §1) -->
async with connection.acursor() as cur:
    await cur.execute(
        "SELECT id FROM sqlery_queued_job "
        "WHERE queue_name = ANY(%s) AND status = 'queued' "
        "  AND (scheduled_at IS NULL OR scheduled_at <= NOW()) "
        "ORDER BY priority DESC, created_at "
        "FOR UPDATE SKIP LOCKED LIMIT 1",
        [queues],
    )
    row = await cur.fetchone()
if row is None: return None
job = await QueuedJob.objects.aget(pk=row[0])
await QueuedJob.objects.filter(pk=row[0]).aupdate(status='running', worker_id=worker_id, started_at=now)

<!-- SQLite CAS path -->
job = await QueuedJob.objects.filter(status='queued', queue_name__in=queues).afirst()
if job is None: return None
rowcount = await QueuedJob.objects.filter(pk=job.pk, version=job.version).aupdate(
    status='running', worker_id=worker_id, version=job.version + 1, started_at=now,
)
return job if rowcount == 1 else None  # retry one tier up

<!-- Pitfall (RESEARCH §9): capture timezone.now() BEFORE await -->
now = timezone.now()
await QueuedJob.objects.filter(scheduled_at__lte=now).afirst()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DjangoAsyncBackend hot-path methods</name>
  <files>src/sqlery/django_sqlery/async_backend.py, src/sqlery/django_sqlery/__init__.py</files>
  <behavior>
    - `aclaim_job` returns None when no queued rows; returns the row and marks 'running' atomically when one is available.
    - Concurrent `aclaim_job` calls on the same queue never both claim the same row (SQLite: CAS retry; Postgres: SKIP LOCKED).
    - `amark_running` / `amark_success` / `amark_failed` / `amark_shutting_down` each update exactly the documented columns.
    - `aupdate_heartbeat` updates `last_heartbeat` to a captured-before-await timestamp.
    - `aregister_worker` / `aunregister_worker` create/delete a WorkerProcess row via `.acreate()` / `.adelete()`.
    - Lease ops use `.aupdate_or_create()` + TTL check on a captured-before-await `now`.
    - `aget_due_scheduled_tasks` yields via `async for` over the filtered queryset.
    - `aregistry_add` / `aregistry_remove` operate via async ORM on the registry table.
    - Importing the module without Postgres available does NOT execute the raw SQL path (it's only inside `aclaim_job` and gated on `connection.vendor == 'postgresql'`).
  </behavior>
  <action>
    Create `src/sqlery/django_sqlery/async_backend.py`. Define `class DjangoAsyncBackend(AsyncDatabaseBackend):` and implement each abstract method using the patterns in `<interfaces>`.

    Branch on `connection.vendor` inside `aclaim_job`:
    - `postgresql` → raw `acursor()` path with `FOR UPDATE SKIP LOCKED`.
    - `sqlite` → optimistic `version`-CAS via `aupdate(...)` rowcount.

    For every method, capture `now = timezone.now()` BEFORE any `await` that uses it (RESEARCH §9 pitfall).

    Do NOT call `sync_to_async` anywhere — decision A is LOCKED.

    Export `DjangoAsyncBackend` from `src/sqlery/django_sqlery/__init__.py` alongside the sync `DjangoBackend`.

    Write `tests/test_django_async_backend.py` covering every method against SQLite (Django pytest fixtures, `pytest.mark.asyncio`). Include a concurrency test using `asyncio.gather` on two `aclaim_job` calls against a single queued row — assert exactly one wins.
  </action>
  <verify>
    <automated>pytest tests/test_django_async_backend.py -x</automated>
  </verify>
  <done>All async methods implemented; tests green on SQLite; no `sync_to_async` in the new file (`! grep sync_to_async src/sqlery/django_sqlery/async_backend.py`).</done>
</task>

</tasks>

<verification>
- `pytest tests/test_django_async_backend.py` passes.
- `grep -c sync_to_async src/sqlery/django_sqlery/async_backend.py` == 0.
- `grep -c acursor src/sqlery/django_sqlery/async_backend.py` >= 1.
</verification>

<success_criteria>
- ASYN-02 met: every AsyncDatabaseBackend method has a Django-native async implementation.
- AsyncWorker plan (Wave 3) can target a real Django backend.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-04-SUMMARY.md`.
</output>
