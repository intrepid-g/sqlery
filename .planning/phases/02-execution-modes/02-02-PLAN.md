---
phase: 02-execution-modes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/models.py
  - src/sqlery/core/models.py
  - src/sqlery/django_sqlery/migrations/0026_add_shutting_down_status.py
  - alembic/versions/20260514_0014_add_shutting_down_status.py
autonomous: false
requirements: [ASYN-05]
tags: [migrations, schema, status-enum]

must_haves:
  truths:
    - "`QueuedJob.status` (Django) accepts the string `'shutting_down'` without truncation or choices-validation error"
    - "`QueuedJob.status` (SQLModel/core) accepts `'shutting_down'` without truncation"
    - "Running `python manage.py migrate sqlery 0026` upgrades a fresh DB cleanly"
    - "Running `alembic upgrade head` upgrades a fresh DB cleanly"
    - "`status` column `max_length`/`String(...)` is >= 15 in both schemas"
  artifacts:
    - path: "src/sqlery/django_sqlery/migrations/0026_add_shutting_down_status.py"
      provides: "Django migration: AlterField max_length 10->20 + new choice"
      contains: "AlterField"
    - path: "alembic/versions/20260514_0014_add_shutting_down_status.py"
      provides: "Alembic revision: alter_column type_=String(20)"
      contains: "op.alter_column"
    - path: "src/sqlery/django_sqlery/models.py"
      provides: "STATUS_CHOICES (QueuedJob only, line ~350) includes ('shutting_down', 'Shutting Down')"
    - path: "src/sqlery/core/models.py"
      provides: "SQLModel status field max_length=20"
  key_links:
    - from: "src/sqlery/django_sqlery/migrations/0026_add_shutting_down_status.py"
      to: "src/sqlery/django_sqlery/migrations/0025_daemoncommand.py"
      via: "dependencies = [('sqlery', '0025_daemoncommand')]"
      pattern: "0025_daemoncommand"
    - from: "alembic/versions/20260514_0014_add_shutting_down_status.py"
      to: "alembic/versions/20250101_0013_worker_unique_constraint.py"
      via: "down_revision = '20250101_0013'"
      pattern: "down_revision"
---

<objective>
Add `shutting_down` to the `QueuedJob.status` enum on both schemas (Django + SQLModel/Alembic). Per RESEARCH.md §C: the string is 13 chars and the current `max_length=10` truncates it, so the column width is bumped to 20 (slack for future states). This is a pre-requisite for ASYN-05 drain-with-deadline semantics — the AsyncWorker writes this transient state during shutdown.

Purpose: Schema groundwork for ASYN-05; touches both migration trees in lockstep (existing two-source-of-truth pattern).
Output: One Django migration, one Alembic revision, two model files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@src/sqlery/django_sqlery/models.py
@src/sqlery/core/models.py
@src/sqlery/django_sqlery/migrations/0025_daemoncommand.py
@alembic/versions/20250101_0013_worker_unique_constraint.py

<interfaces>
<!-- From src/sqlery/django_sqlery/models.py (RESEARCH §C verified) -->
- STATUS_CHOICES tuple at ~line 350 — only the QueuedJob copy gets the new entry; lines 1024 and 1130 are different models, do NOT modify.
- `status = models.CharField(max_length=10, choices=STATUS_CHOICES, default='queued', ...)` at ~line 384-386.

<!-- From src/sqlery/core/models.py -->
- Line ~75: `status: str = Field(default="queued", max_length=10, ...)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Django model + create migration 0026</name>
  <files>src/sqlery/django_sqlery/models.py, src/sqlery/django_sqlery/migrations/0026_add_shutting_down_status.py</files>
  <action>
    Edit `src/sqlery/django_sqlery/models.py`:
    - In the QueuedJob STATUS_CHOICES tuple (around line 350 per RESEARCH §C — confirm by reading 5-line context), append `("shutting_down", "Shutting Down")`. Do NOT modify the other two STATUS_CHOICES tuples at ~1024 and ~1130.
    - Change `max_length=10` to `max_length=20` on the QueuedJob `status` CharField only (~line 385).

    Generate the migration by running `python manage.py makemigrations sqlery --name add_shutting_down_status` against a Django settings module if available; otherwise hand-write `src/sqlery/django_sqlery/migrations/0026_add_shutting_down_status.py` with `dependencies = [('sqlery', '0025_daemoncommand')]` and a single `migrations.AlterField` on `QueuedJob.status` that updates both `max_length=20` and the choices list.
  </action>
  <verify>
    <automated>python -c "from sqlery.django_sqlery import models; assert any(c[0]=='shutting_down' for c in models.QueuedJob._meta.get_field('status').choices); assert models.QueuedJob._meta.get_field('status').max_length >= 15"</automated>
  </verify>
  <done>Migration file exists with correct `dependencies`; model field updated; `manage.py migrate --plan` shows the new op cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update SQLModel + create Alembic revision</name>
  <files>src/sqlery/core/models.py, alembic/versions/20260514_0014_add_shutting_down_status.py</files>
  <action>
    Edit `src/sqlery/core/models.py` line ~75: change `max_length=10` to `max_length=20` on the `status` field. If the SQLModel has a `Literal[...]` or enum constraint on status, add `"shutting_down"` to it; otherwise the string-typed field needs no choice update.

    Create `alembic/versions/20260514_0014_add_shutting_down_status.py` with:
    - `revision = '20260514_0014'`
    - `down_revision = '20250101_0013'` (verify exact id from existing file header)
    - `def upgrade(): op.alter_column('queued_job', 'status', type_=sa.String(20), existing_type=sa.String(10), existing_nullable=False)`
    - `def downgrade(): op.alter_column('queued_job', 'status', type_=sa.String(10), existing_type=sa.String(20), existing_nullable=False)`

    Verify `script_location` in `alembic.ini` (RESEARCH §7 flagged this — it lives at repo root pointing at `alembic/`).
  </action>
  <verify>
    <automated>alembic check 2>&1 | grep -qiE "no new upgrade|up to date|No changes" || alembic history | head -3 | grep -q 20260514_0014</automated>
  </verify>
  <done>Alembic head is the new revision; `alembic upgrade head && alembic downgrade -1 && alembic upgrade head` round-trips cleanly on an ephemeral SQLite DB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two migrations adding `shutting_down` state and widening the column to `max_length=20`.</what-built>
  <how-to-verify>
    1. Spin up a throwaway dev DB.
    2. `python manage.py migrate` — verify 0026 applies clean.
    3. `alembic upgrade head` against the standalone DB URL — verify 0014 applies clean.
    4. Verify no schema drift warnings.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Both migrations forward + reverse cleanly.
- Inserting a row with `status='shutting_down'` succeeds in both schemas.
</verification>

<success_criteria>
- `shutting_down` is a valid status across all backends.
- Schema width supports future state-name growth.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-02-SUMMARY.md`.
</output>
