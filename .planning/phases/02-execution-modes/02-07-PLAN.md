---
phase: 02-execution-modes
plan: 07
type: execute
wave: 4
depends_on: [02-02]
files_modified:
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_modes.py
autonomous: true
requirements: [DMOD-01, DMOD-02, DMOD-03, DMOD-05, SMOD-01, SMOD-05]
tags: [tests, e2e, parametrized-matrix, integration]

must_haves:
  truths:
    - "`tests/integration/test_modes.py` runs a parametrized matrix indexed by (mode, integration, db)"
    - "Fixtures provision a fresh DB per combo; `(mode='lambda'|'subprocess-standalone'|'http-trigger-standalone'|'async')` rows are SKIPPED here (handled in Plan 02-08)"
    - "DMOD-01 (Django daemon E2E): enqueue → daemon claims → executes → marks finished — passes"
    - "DMOD-02 (Django subprocess E2E): passes"
    - "DMOD-03 (Django HTTP trigger E2E): passes via existing middleware + trigger view"
    - "DMOD-05 (Django sync/thread E2E): passes"
    - "SMOD-01 (standalone daemon E2E): passes against fastapi_sqlery + SQLAlchemyBackend on SQLite"
    - "SMOD-05 (standalone sync/thread E2E): passes"
    - "Slow combos (Postgres, daemon) marked `@pytest.mark.slow`"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "Fixtures for each (integration, db) combo + helper to enqueue+drain a job"
    - path: "tests/integration/test_modes.py"
      provides: "Single parametrized test class covering 6 (mode, integration, db) cells"
  key_links:
    - from: "tests/integration/test_modes.py"
      to: "tests/integration/conftest.py"
      via: "pytest parametrize indirect fixtures"
      pattern: "@pytest.fixture"
---

<objective>
Build the parametrized E2E test harness per CONTEXT decision B [ASSUMED] and land the 6 existing-mode E2E tests that don't need any net-new implementation. RESEARCH §5 confirms: zero E2E coverage exists for Django daemon, both standalone daemon and standalone sync, and several others — they are all net-new tests but against existing (working) implementations. The four net-new implementation modes (subprocess-standalone, http-trigger-standalone, lambda × 2, async-worker × 2) ship in Plan 02-08.

Purpose: Establish the harness shape + close 6 of 12 acceptance tests in one plan.
Output: A populated `tests/integration/` package with conftest fixtures + one parametrized test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@tests/test_subprocess.py
@tests/test_http_trigger.py
@tests/test_core_standalone.py
@src/sqlery/core/daemon.py
@src/sqlery/fastapi_sqlery/backend.py

<interfaces>
<!-- Parametrization shape (RESEARCH §5) -->
@pytest.mark.parametrize("mode", ["daemon", "subprocess", "http-trigger", "sync"])
@pytest.mark.parametrize("integration", ["django", "standalone"])
@pytest.mark.parametrize("db", ["sqlite", pytest.param("postgres", marks=pytest.mark.slow)])
def test_mode_e2e(mode, integration, db, harness):
    job_id = harness.enqueue(simple_job, args=(1, 2))
    harness.run_mode_until_finished(job_id, timeout=30)
    assert harness.status(job_id) == "finished"
    assert harness.result(job_id) == 3

<!-- Skip rows handled elsewhere -->
- (mode="lambda", *) — Plan 02-08
- (mode="async", *) — Plan 02-08
- (mode="subprocess", integration="standalone") — Plan 02-08 (SMOD-02 is net-new impl)
- (mode="http-trigger", integration="standalone") — Plan 02-08 (SMOD-03 is net-new impl)
Mark these with `pytest.skip("covered by plan 02-08")` for now.

<!-- Subprocess isolation pattern from Phase 1 plan 01-03 — reuse for standalone-no-django combos -->
- Use `subprocess.run([sys.executable, "-c", "..."], env={"PYTHONPATH": ...}, capture_output=True)` and scrub Django from env where the harness must prove no-Django operation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness fixtures in conftest</name>
  <files>tests/integration/__init__.py, tests/integration/conftest.py</files>
  <action>
    Replace the empty `tests/integration/__init__.py` if needed; create `tests/integration/conftest.py` exposing:

    - `db_url(request)` fixture: yields a fresh SQLite URL per test (tempfile) for `db == 'sqlite'`; yields the CI Postgres URL from `SQLERY_TEST_PG_URL` for `db == 'postgres'`. Skip Postgres if env var unset.
    - `integration_setup(request, db_url)` fixture: for `integration == 'django'` runs `django.setup()` with an in-test `settings_module`; for `integration == 'standalone'` calls `sqlery.compat.initialize(database_url=db_url, ...)`. Tears down via `_reset_backend()` (add this helper to `compat/__init__.py` if not present).
    - `harness(integration_setup, mode)` fixture returning an object with `.enqueue(fn, args, kwargs, **opts)`, `.run_mode_until_finished(job_id, timeout)`, `.status(job_id)`, `.result(job_id)`. Mode dispatch:
      - `daemon` → starts a daemon subprocess (Django: `python manage.py daemon --once`; standalone: `python -m sqlery.core.cli daemon --once`).
      - `subprocess` → invokes the existing subprocess executor in-process.
      - `http-trigger` → for Django, posts a signed request to the existing trigger view via Django test client; for standalone, defers to Plan 02-08 by raising `pytest.skip`.
      - `sync` → directly invokes the synchronous/thread path.
    - A simple registered job `simple_job(a, b) -> a + b` for use in tests.

    Mark `(db='postgres')` parametrizations with `pytest.mark.slow` so inner-loop runs skip them.
  </action>
  <verify>
    <automated>pytest tests/integration/conftest.py --collect-only 2>&1 | grep -q "no tests collected"</automated>
  </verify>
  <done>Conftest imports cleanly; fixtures parameterize correctly; harness construction works for sqlite+django+daemon.</done>
</task>

<task type="auto">
  <name>Task 2: Parametrized E2E test module</name>
  <files>tests/integration/test_modes.py</files>
  <action>
    Write `tests/integration/test_modes.py` with a single `test_mode_e2e(mode, integration, db, harness)` test parametrized over `(mode, integration, db)` per `<interfaces>`. The function:

    1. Enqueues `simple_job(1, 2)` via the harness.
    2. Runs the mode until the job reaches a terminal state or the 30s timeout fires.
    3. Asserts `harness.status(job_id) == 'finished'`.
    4. Asserts `harness.result(job_id) == 3`.

    Use `pytest.skip(...)` for the four net-new combos noted in `<interfaces>` (they ship in 02-08). Cells covered by this plan: (daemon, django, sqlite), (daemon, standalone, sqlite), (subprocess, django, sqlite), (http-trigger, django, sqlite), (sync, django, sqlite), (sync, standalone, sqlite). Postgres variants of the same 6 added with `@pytest.mark.slow`.

    Use subprocess-isolation per Plan 01-03 pattern for any (`standalone`, `*`) cell that must prove no-Django operation — scrub `DJANGO_SETTINGS_MODULE` from the subprocess env.
  </action>
  <verify>
    <automated>pytest tests/integration/test_modes.py -x -k "not slow and not skip"</automated>
  </verify>
  <done>The 6 non-slow cells pass green; the 4 deferred-to-02-08 cells are explicitly skipped with a referencing message.</done>
</task>

</tasks>

<verification>
- `pytest tests/integration/test_modes.py -k "not slow"` shows 6 passed / 4 skipped (+ optionally postgres rows skipped if no env var).
- DMOD-01/02/03/05 and SMOD-01/05 verifications all green.
</verification>

<success_criteria>
- Harness exists, is documented in conftest, and DMOD-01/02/03/05 + SMOD-01/05 are observably passing.
- The shape lets Plan 02-08 plug in the net-new modes without restructuring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-07-SUMMARY.md`.
</output>
