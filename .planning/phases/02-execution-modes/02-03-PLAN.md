---
phase: 02-execution-modes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/compat/__init__.py
autonomous: true
requirements: [ASYN-01]
tags: [async, abc, compat]

must_haves:
  truths:
    - "`from sqlery.compat import AsyncDatabaseBackend` succeeds"
    - "`AsyncDatabaseBackend` is an ABC; instantiating it without a subclass raises TypeError"
    - "All 12-15 hot-path methods identified in RESEARCH §ASYN-01 are present as abstract `async def` methods"
    - "Method signatures mirror their sync counterparts in `DatabaseBackend` (same args, awaitable return)"
  artifacts:
    - path: "src/sqlery/compat/__init__.py"
      provides: "AsyncDatabaseBackend ABC with hot-path async methods"
      contains: "class AsyncDatabaseBackend"
  key_links:
    - from: "src/sqlery/compat/__init__.py"
      to: "src/sqlery/compat/__init__.py (DatabaseBackend sync ABC)"
      via: "sibling class; ASYN-01 methods 1:1 with hot-path subset of sync ABC"
      pattern: "class AsyncDatabaseBackend"
---

<objective>
Define `AsyncDatabaseBackend` ABC alongside the existing sync `DatabaseBackend` in `src/sqlery/compat/__init__.py`. Only the hot-path methods the AsyncWorker actually awaits get an async variant (per RESEARCH §ASYN-01: ~12–15 methods, not all 30+). Sync methods remain the only path for daemon/scheduler internals; the async path is for the AsyncWorker (ASYN-04) plus future serverless async use.

Purpose: Establish the async contract before any backend implementation work (ASYN-02/03) so both can be written in parallel against a stable interface.
Output: New abstract class added to compat, exported in module `__all__` if present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@src/sqlery/compat/__init__.py

<interfaces>
<!-- Hot-path async methods to declare on AsyncDatabaseBackend (per RESEARCH §ASYN-01) -->
<!-- All are `async def` and abstract. Argument shape mirrors the sync DatabaseBackend method of the same base name. -->

Required abstract methods:
- aclaim_job(self, queues: list[str], worker_id: str) -> QueuedJob | None
- amark_running(self, job_id, worker_id) -> None
- amark_success(self, job_id, result) -> None
- amark_failed(self, job_id, error: str, traceback: str | None = None) -> None
- amark_shutting_down(self, job_id) -> None  # NEW — drives ASYN-05 transient state
- aupdate_heartbeat(self, worker_id) -> None
- aregister_worker(self, worker_id, metadata: dict) -> None
- aunregister_worker(self, worker_id) -> None
- aclaim_lease(self, queue_name: str, worker_id: str, ttl_seconds: int) -> bool
- arenew_lease(self, queue_name: str, worker_id: str) -> bool
- arelease_lease(self, queue_name: str, worker_id: str) -> None
- aget_due_scheduled_tasks(self, now) -> list  # async iteration helper
- aregistry_add(self, registry_name: str, job_id) -> None
- aregistry_remove(self, registry_name: str, job_id) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AsyncDatabaseBackend ABC</name>
  <files>src/sqlery/compat/__init__.py, tests/test_async_backend_abc.py</files>
  <behavior>
    - Importing `AsyncDatabaseBackend` from `sqlery.compat` succeeds.
    - Attempting `AsyncDatabaseBackend()` raises TypeError (abstract).
    - A trivial subclass that implements all abstract methods can be instantiated.
    - Every method listed in the `<interfaces>` block is present, is `async def`, and is decorated with `@abstractmethod` (verified via `inspect.iscoroutinefunction` on the concrete subclass method when overridden trivially with `async def ... return None`).
  </behavior>
  <action>
    Add `class AsyncDatabaseBackend(ABC):` immediately after the existing `DatabaseBackend` class in `src/sqlery/compat/__init__.py`. Each method is declared with `@abstractmethod` and a one-line docstring describing the sync analog (e.g. `"""Async analog of DatabaseBackend.claim_job."""`). Use `from abc import ABC, abstractmethod` (already imported per existing sync ABC).

    Add `AsyncDatabaseBackend` to the module's `__all__` if such a list exists.

    Write a TDD test at `tests/test_async_backend_abc.py` that verifies the four behaviors above. Use a minimal in-test subclass that overrides every abstract method with `async def m(self, *a, **kw): return None`.

    Do NOT touch `get_backend()` yet — that wiring (returning an async backend instance) is part of ASYN-02/03 plans.
  </action>
  <verify>
    <automated>pytest tests/test_async_backend_abc.py -x</automated>
  </verify>
  <done>Tests green; ABC committed; sync ABC unchanged.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_async_backend_abc.py` passes.
- `grep -c "@abstractmethod" src/sqlery/compat/__init__.py` rises by exactly 14 (the new method count). If you add more/fewer, update this number.
</verification>

<success_criteria>
- ASYN-01 contract pinned down so ASYN-02 and ASYN-03 can run in parallel against it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-03-SUMMARY.md`.
</output>
