---
phase: 02-execution-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - .github/workflows/postgres.yml
  - CHANGELOG.md
autonomous: false
requirements: [ASYN-02]
tags: [django-version, ci, breaking-change]

must_haves:
  truths:
    - "`pyproject.toml` pins `django>=5.2` in every dependency block (main, dev, django extra, test extra)"
    - "CI matrix no longer references Django 4.2 anywhere; runs against Django 5.2 (and optionally 5.1 smoke row)"
    - "CHANGELOG.md announces the Django 5.2 floor as a documented BC break for the next release"
    - "`uv sync` resolves cleanly against the new floor"
  artifacts:
    - path: "pyproject.toml"
      provides: "Django >= 5.2 dependency pin in all 4 locations identified in RESEARCH.md §A.1"
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix without django==4.2 entries"
    - path: "CHANGELOG.md"
      provides: "Documented BC break for Django minimum bump"
  key_links:
    - from: "pyproject.toml"
      to: ".github/workflows/ci.yml"
      via: "matrix django-version strategy must satisfy `>=5.2`"
      pattern: "django-version.*5\\.[12]"
---

<objective>
Raise the Django minimum to 5.2 LTS per CONTEXT.md decision A.1 (LOCKED). This unblocks ASYN-02 — Django's native async ORM features (`.aget`/`.acreate`/`.aupdate`, `connection.acursor()`) used by the async backend require 5.2. The bump is a documented BC break and must land in Wave 1 before any async backend work begins.

Purpose: Establish the dependency floor and CI guardrail for the entire phase.
Output: Updated `pyproject.toml`, refreshed CI matrix, CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-execution-modes/02-CONTEXT.md
@.planning/phases/02-execution-modes/02-RESEARCH.md
@CLAUDE.md
@pyproject.toml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Django pin in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>Update every `django>=4.2` occurrence to `django>=5.2` (RESEARCH.md §A.1 identifies 4 lines: 45, 66, 85, 110 — verify with `grep -n 'django>=' pyproject.toml`). Do NOT raise `requires-python` (3.10 still supported by Django 5.2). Run `uv lock --upgrade-package django` afterwards to refresh `uv.lock`. Commit per A.1.</action>
  <verify>
    <automated>grep -nE 'django\s*>=\s*4\.' pyproject.toml | grep -v '^#' | wc -l | grep -q '^[[:space:]]*0$'</automated>
  </verify>
  <done>No `django>=4.x` remaining; `uv sync` resolves; `uv.lock` updated and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Drop Django 4.2 from CI matrix and add 5.2 row</name>
  <files>.github/workflows/ci.yml, .github/workflows/postgres.yml</files>
  <action>In each workflow file, remove any matrix entry referencing `django-version: '4.2'` or `django==4.2`. Add/ensure entries for `django==5.2` (LTS, required) and OPTIONALLY `django==5.1` as a smoke row (planner recommendation per RESEARCH.md open question #1 — keep 5.1 as smoke only if low cost; otherwise drop). Preserve existing Python version matrix (3.11/3.12/3.13). If a `postgres.yml` matrix references Django versions, mirror the change.</action>
  <verify>
    <automated>! grep -rE "django[-_=]?version.*4\\.2|django==4\\.2" .github/workflows/</automated>
  </verify>
  <done>No 4.2 references in workflows; 5.2 row present; lint via `actionlint .github/workflows/*.yml` (if available) passes.</done>
</task>

<task type="auto">
  <name>Task 3: Document BC break in CHANGELOG</name>
  <files>CHANGELOG.md</files>
  <action>Add an entry under the next-release Unreleased section: "**BREAKING:** Django minimum version raised to 5.2 LTS. Users on Django 4.2 must upgrade before installing this release. Rationale: native async ORM features required for the async worker rebuild (Phase 2, ASYN-02)." If CHANGELOG.md does not exist, create it with a minimal Keep-a-Changelog header and this entry.</action>
  <verify>
    <automated>grep -q "Django.*5\\.2" CHANGELOG.md && grep -qi "breaking" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG entry present and includes "BREAKING" marker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Django pin raised to 5.2; CI matrix dropped 4.2.</what-built>
  <how-to-verify>
    1. Push the branch and open a draft PR.
    2. Confirm the CI run schedules the new 5.2 row(s) and that no job tries to install Django 4.2.
    3. Skim CHANGELOG.md — wording acceptable?
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rE "django[><=]+4" pyproject.toml .github/workflows/` returns nothing.
- CI green on the new matrix (human-verify).
</verification>

<success_criteria>
- Django 5.2 is the floor in code and CI.
- BC break is documented.
- Phase 2 plans depending on async ORM can assume 5.2.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-modes/02-01-SUMMARY.md`.
</output>
