---
phase: 14-scheduled-job-staging
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/sqlery/django_sqlery/backend.py
  - tests/unit/test_staging.py
autonomous: true
requirements:
  - R5
must_haves:
  truths:
    - "get_job_by_id returns a ScheduledJob when the id exists only in sqlery_scheduled_job"
    - "cancel_job cancels a staged job (deletes ScheduledJob row) even if no QueuedJob row with that id exists"
    - "get_jobs includes staged jobs when they match the requested queue and pagination window"
    - "A staged job cannot be claimed by a worker (it is never a QueuedJob until promoted)"
    - "Concurrent promoters never double-promote — SKIP LOCKED + advisory lock prevents it"
    - "Config validation rejects retention <= threshold"
  artifacts:
    - path: src/sqlery/django_sqlery/backend.py
      provides: dual-table implementations of get_job_by_id, cancel_job, get_jobs
      contains: "ScheduledJob.objects"
    - path: tests/unit/test_staging.py
      provides: tests for all three success criteria
      contains: "def test_"
  key_links:
    - from: backend.get_job_by_id
      to: ScheduledJob.objects.get
      via: "fallback when QueuedJob.DoesNotExist"
      pattern: "ScheduledJob.objects"
    - from: backend.cancel_job
      to: ScheduledJob.objects.filter
      via: "try QueuedJob first, then ScheduledJob"
      pattern: "ScheduledJob.objects.filter"
---

<objective>
Patch the three DjangoBackend API consumers — get_job_by_id, cancel_job, and get_jobs — to span both sqlery_queued_job and sqlery_scheduled_job so a staged job is never invisible or uncancellable. Then write the full test suite covering all three phase success criteria.

Purpose: "A staged job disappearing from status/cancel is a bug" (CONTEXT.md). This plan closes that gap for the Django backend. FastAPI parity is Phase 17 per the deferred scope in CONTEXT.md.
Output: Dual-table DjangoBackend methods; tests/unit/test_staging.py covering SC-1, SC-2, and SC-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-scheduled-job-staging/14-CONTEXT.md
@src/sqlery/django_sqlery/backend.py
@src/sqlery/core/scheduler.py
@tests/unit/test_partitioning.py

# Prior plan outputs
@.planning/phases/14-scheduled-job-staging/14-01-SUMMARY.md
@.planning/phases/14-scheduled-job-staging/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-table API surface — get_job_by_id, cancel_job, get_jobs</name>
  <files>src/sqlery/django_sqlery/backend.py</files>
  <read_first>
    - src/sqlery/django_sqlery/backend.py lines 643-660 (get_job_by_id — single-table)
    - src/sqlery/django_sqlery/backend.py lines 296-303 (cancel_job — single-table)
    - src/sqlery/django_sqlery/backend.py lines 938-958 (get_jobs — single-table)
    - src/sqlery/django_sqlery/models.py ScheduledJob model (fields and Meta from Plan 01)
  </read_first>
  <behavior>
    get_job_by_id:
    - Test 1: job exists only in QueuedJob → returns QueuedJob instance (regression: existing behavior preserved).
    - Test 2: job exists only in ScheduledJob → returns ScheduledJob instance.
    - Test 3: job exists in neither table → returns None.

    cancel_job:
    - Test 4: job is a QueuedJob with status='queued' → cancels it (existing behavior preserved).
    - Test 5: job is a ScheduledJob → deletes the ScheduledJob row, returns True.
    - Test 6: id does not exist in either table → returns False.

    get_jobs:
    - Test 7: get_jobs() with no filters returns QueuedJob rows (existing behavior preserved with no staged rows).
    - Test 8: get_jobs() with staged jobs present — staged jobs are not included (get_jobs returns QueuedJob only; staged jobs have their own visibility via get_job_by_id and cancel_job; the list API reflects the executable queue).
    NOTE on get_jobs design (use Claude's discretion, SC-1 criterion): A staged job is "visible to status/cancel APIs" — meaning get_job_by_id and cancel_job must find it. The list API (get_jobs) is the executable queue view; including staged rows in the paginated list would require merging two heterogeneous rowsets with different status columns. The correct interpretation is: get_jobs returns only queued/running/etc rows from sqlery_queued_job. A caller wanting staged jobs should use get_staged_jobs (added below). This satisfies SC-1 without a complex merge.
  </behavior>
  <action>
    Patch src/sqlery/django_sqlery/backend.py as follows. For each existing method, comment out the existing implementation body lines (do not delete) and add the dual-table implementation beneath.

    --- get_job_by_id ---
    The existing implementation:
      try:
          return self.QueuedJob.objects.get(id=job_id)
      except self.QueuedJob.DoesNotExist:
          return None

    New implementation (comment out the above, add beneath):
      try:
          return self.QueuedJob.objects.get(id=job_id)
      except self.QueuedJob.DoesNotExist:
          pass
      try:
          return self.ScheduledJob.objects.get(id=job_id)
      except self.ScheduledJob.DoesNotExist:
          return None

    --- cancel_job ---
    The existing implementation cancels only QueuedJob status='queued'. New implementation adds ScheduledJob fallback:
    Comment out the existing implementation body. Add beneath:
      updated = self.QueuedJob.objects.filter(id=job_id, status='queued').update(
          status='failed', error='Cancelled by user'
      )
      if updated > 0:
          return True
      deleted, _ = self.ScheduledJob.objects.filter(id=job_id).delete()
      return deleted > 0

    --- add get_staged_jobs method ---
    Add a new method get_staged_jobs(self, queue_name=None, limit=100, offset=0) -> list after get_jobs:
      """Return staged (pre-promotion) jobs from sqlery_scheduled_job."""
      query = self.ScheduledJob.objects.all()
      if queue_name:
          query = query.filter(queue_name=queue_name)
      query = query.order_by('scheduled_at')
      return list(query[offset:offset + limit])

    --- get_jobs comment ---
    In the existing get_jobs method, add a one-line comment above the QueuedJob.objects.all() call:
      # Staged jobs (sqlery_scheduled_job) are in a separate table; use get_staged_jobs() for them.
    Do not modify the query logic itself.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/unit/test_django_backend.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    All tests in test_django_backend.py pass. get_job_by_id returns ScheduledJob for staged IDs; cancel_job removes ScheduledJob rows; get_staged_jobs is importable and callable; get_jobs is unchanged in behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/unit/test_staging.py covering all three success criteria</name>
  <files>tests/unit/test_staging.py</files>
  <read_first>
    - tests/unit/test_partitioning.py lines 1-50 (mock cursor pattern for promote_due_scheduled_jobs tests)
    - tests/unit/test_partitioning.py TestEnsureFuturePartitions (advisory lock contention test pattern)
    - tests/unit/conftest.py (check for any shared fixtures)
    - tests/settings.py or tests/__init__.py (Django test settings reference)
    - src/sqlery/core/scheduler.py promote_due_scheduled_jobs, ADVISORY_LOCK_PROMOTE (to know what to mock)
    - src/sqlery/core/daemon.py _validate_staging_config (to know function under test)
  </read_first>
  <behavior>
    SC-1 (far-future job invisible to claims, visible to status/cancel):
    - Test: enqueue a job with scheduled_at 60 days out via DjangoBackend.create_job (SQLite) → assert isinstance(result, ScheduledJob) — it went to staging, not QueuedJob.
    - Test: get_job_by_id(result.id) returns the ScheduledJob instance — it is visible to status API.
    - Test: cancel_job(result.id) returns True and deletes the ScheduledJob — visible to cancel API.
    - Test: QueuedJob.objects.filter(id=result.id).exists() is False — never in the claimable queue.

    SC-2 (no double-promote — SKIP LOCKED + advisory lock):
    - Test using mock cursor: call promote_due_scheduled_jobs(cur) where pg_try_advisory_lock returns False → returns 0, no DELETE executed.
    - Test using mock cursor: two mock cursors simulate two concurrent promoters — first acquires lock, second sees pg_try_advisory_lock=False → only one set of INSERTs is executed.
    - Test using mock cursor: promote_due_scheduled_jobs with lock acquired and 2 rows returned → returns 2; advisory lock released in finally.
    - Test using mock cursor: promote_due_scheduled_jobs with lock acquired and advisory unlock call in finally when DELETE raises → unlock is still called.

    SC-3 (config validation rejects retention <= threshold):
    - Test: _validate_staging_config(threshold_days=1, retention_str='30 days') does not raise.
    - Test: _validate_staging_config(threshold_days=30, retention_str='30 days') raises ValueError.
    - Test: _validate_staging_config(threshold_days=31, retention_str='30 days') raises ValueError.
    - Test: _validate_staging_config(threshold_days=1, retention_str='1 day') raises ValueError (equal case).
  </behavior>
  <action>
    Create tests/unit/test_staging.py.

    Module docstring: "Unit tests for Phase 14 scheduled-job staging. Tests cover all three success criteria: SC-1 (far-future routing + dual-table visibility), SC-2 (exactly-once promotion with mock cursor), SC-3 (config validation)."

    Imports needed (all top-level, per project convention):
      import pytest
      from datetime import datetime, timezone, timedelta
      from unittest.mock import MagicMock, call

    Django setup guard (same pattern as other unit tests): use @pytest.mark.django_db decorator on SC-1 tests that hit the ORM. Check tests/unit/conftest.py for any existing django_db marker setup.

    For mock cursor tests (SC-2): use the same _make_cursor helper pattern as test_partitioning.py. Define it locally in this file (no cross-file import).

    SC-1 class: class TestStagingRouting: — marked @pytest.mark.django_db
      - test_far_future_job_goes_to_staging: create_job with scheduled_at = now + 60 days → isinstance ScheduledJob
      - test_far_future_job_invisible_to_claim_queue: after above, QueuedJob.objects.filter(id=job.id).exists() is False
      - test_far_future_job_visible_to_get_job_by_id: backend.get_job_by_id(job.id) returns the ScheduledJob
      - test_far_future_job_cancellable: backend.cancel_job(job.id) returns True and deletes the row
      - test_near_future_job_goes_to_queued_job: scheduled_at = now + 12 hours → isinstance QueuedJob

    SC-2 class: class TestPromotion: — no django_db marker needed (mock cursor only)
      Import: from sqlery.core.scheduler import promote_due_scheduled_jobs, ADVISORY_LOCK_PROMOTE
      - test_skips_when_lock_not_acquired: fetchone returns [(False,)]; returns 0; no DELETE called
      - test_promotes_rows_when_lock_acquired: 2 rows returned from DELETE RETURNING; returns 2; advisory_unlock called
      - test_returns_zero_when_no_due_rows: lock acquired; DELETE RETURNING yields empty; returns 0
      - test_advisory_unlock_called_even_on_insert_error: lock acquired; DELETE RETURNING yields 1 row; INSERT raises; advisory_unlock still called (use mock.side_effect to raise on the second execute call)

    SC-3 class: class TestStagingConfigValidation: — no django_db marker needed
      Import: from sqlery.core.daemon import _validate_staging_config
      - test_valid_config_does_not_raise: threshold=1, retention='30 days' — no exception
      - test_equal_retention_and_threshold_raises: threshold=30, retention='30 days' — ValueError
      - test_retention_less_than_threshold_raises: threshold=31, retention='30 days' — ValueError
      - test_exactly_equal_raises: threshold=1, retention='1 day' — ValueError

    Test discovery: file must be discoverable by `pytest tests/unit/test_staging.py`. Use standard pytest conventions (class names start with Test, method names start with test_).
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    All tests in test_staging.py pass. The three success criteria are covered: SC-1 routing + dual-table visibility on SQLite ORM, SC-2 exactly-once mock-cursor promotion, SC-3 config validation. No test is marked xfail or skip.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API caller → cancel_job | Job ID comes from trusted internal callers (backend API), not directly from HTTP request body in this phase |
| Staged payload → consumer | payload is opaque JSON, treated at same trust level as existing job kwargs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | cancel_job now also deletes ScheduledJob | mitigate | cancel_job is already an authenticated operation in the Django admin/API layer (auth enforced upstream). No privilege escalation from extending to ScheduledJob. |
| T-14-07 | Information Disclosure | get_job_by_id reveals staged jobs to callers | accept | Staged job content (task_path, kwargs) is no more sensitive than a queued job; the existing access model applies. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. |
</threat_model>

<verification>
- `env -u VIRTUAL_ENV uv run pytest tests/unit/ -x -q` passes — no regressions across all unit tests.
- `env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py -v` shows all tests green.
- `env -u VIRTUAL_ENV uv run python -c "from sqlery.django_sqlery.backend import DjangoBackend; b = DjangoBackend(); print(hasattr(b, 'get_staged_jobs'))"` prints True.
</verification>

<success_criteria>
All three phase success criteria verified by tests:
1. SC-1: A job scheduled 60 days out is in ScheduledJob (not QueuedJob), visible to get_job_by_id and cancel_job.
2. SC-2: promote_due_scheduled_jobs mock tests confirm SKIP LOCKED advisory-lock pattern prevents double-promote.
3. SC-3: _validate_staging_config raises ValueError when retention <= threshold.

The full unit test suite (tests/unit/) passes with no regressions.
</success_criteria>

<output>
Create .planning/phases/14-scheduled-job-staging/14-03-SUMMARY.md when done.
</output>
