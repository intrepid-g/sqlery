---
phase: 16-backend-wiring-pruning
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/db_compat.py
  - src/sqlery/django_sqlery/models.py
autonomous: true
requirements:
  - R6
  - R10

must_haves:
  truths:
    - "atomic_claim_job_sqlite CAS filter includes created_at (checklist item 1)"
    - "atomic_claim_job_postgres CAS filter includes created_at (checklist item 2)"
    - "mark_running filter includes created_at (checklist item 3)"
    - "mark_success filter includes created_at (checklist item 4)"
    - "mark_failed filter includes created_at (checklist item 5)"
    - "save_meta already has created_at — verified and unchanged (checklist item 6)"
    - "All CAS filters retain version field — D7 optimistic-locking semantics unchanged"
  artifacts:
    - path: "src/sqlery/django_sqlery/db_compat.py"
      provides: "CAS claim functions with created_at in filter"
      contains: "created_at=job.created_at"
    - path: "src/sqlery/django_sqlery/models.py"
      provides: "mark_running/mark_success/mark_failed with created_at in filter"
      contains: "created_at=self.created_at"
  key_links:
    - from: "src/sqlery/core/claiming.py"
      to: "atomic_claim_job_sqlite / atomic_claim_job_postgres"
      via: "atomic_claim_job() dispatcher"
      pattern: "atomic_claim_job_sqlite|atomic_claim_job_postgres"
    - from: "src/sqlery/core/worker.py"
      to: "models.py mark_running/mark_success/mark_failed"
      via: "job lifecycle state transitions"
      pattern: "mark_running|mark_success|mark_failed"
---

<objective>
Write-path pruning — checklist items 1 through 6: add created_at to the CAS filter in
both atomic_claim_job_sqlite and atomic_claim_job_postgres (db_compat.py), and add
created_at to the filter in mark_running, mark_success, and mark_failed (models.py).
Verify save_meta already has the composite filter from Phase 15 (item 6 — no change
needed). All six changes: comment out the old filter line, add the corrected line below
(CLAUDE.md convention). version is never removed from any CAS (D7).

Purpose: Ensures PG can prune each UPDATE to a single partition using the partition
key (created_at). The composite PK (created_at, id) was established in Phase 15; this
plan makes every hot lifecycle UPDATE use both columns.

Output: Six checklist items addressed. Old filter lines commented out, new lines with
created_at below them. No behavioral change on SQLite (CAS semantics identical; the
extra filter column is always in hand from get_claimable_jobs returning full rows).
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-backend-wiring-pruning/16-CONTEXT.md
@src/sqlery/django_sqlery/db_compat.py
@src/sqlery/django_sqlery/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add created_at to CAS filters in atomic_claim_job_sqlite and atomic_claim_job_postgres (checklist items 1-2)</name>
  <files>src/sqlery/django_sqlery/db_compat.py</files>
  <behavior>
    - atomic_claim_job_sqlite: QueuedJob.objects.filter(id=job.id, status='queued', version=expected_version) → filter gains created_at=job.created_at; version retained; status='queued' retained
    - atomic_claim_job_postgres: QueuedJob.objects.filter(id=job.id, version=expected_version) → gains created_at=job.created_at; version retained
    - Both functions: job.created_at is already in hand from get_claimable_jobs returning full model rows (no .only()/.values() strips it) — no extra query needed
    - Old filter lines commented out, corrected lines added below (CLAUDE.md convention)
    - EXPLAIN on PG after this change will show "Partitions: 1 of N" instead of "Partitions: N of N"
  </behavior>
  <read_first>
    src/sqlery/django_sqlery/db_compat.py — full file (already read); focus on lines 72-154: atomic_claim_job_sqlite (lines 100-109) and atomic_claim_job_postgres (lines 139-147)
    src/sqlery/django_sqlery/backend.py get_claimable_jobs (lines 958-993) — confirm no .only() or .values() that would drop created_at from returned rows
  </read_first>
  <action>
    In atomic_claim_job_sqlite (around line 100): locate the block:
      rows_updated = QueuedJob.objects.filter(
          id=job.id,
          status='queued',
          version=expected_version
      ).update(...)

    Comment out the filter call (all 4 lines of the filter arguments), add below:
      rows_updated = QueuedJob.objects.filter(
          id=job.id,
          created_at=job.created_at,
          status='queued',
          version=expected_version
      ).update(...)

    In atomic_claim_job_postgres (around line 139): locate:
      rows_updated = QueuedJob.objects.filter(
          id=job.id,
          version=expected_version
      ).update(...)

    Comment out and add below:
      rows_updated = QueuedJob.objects.filter(
          id=job.id,
          created_at=job.created_at,
          version=expected_version
      ).update(...)

    No other changes to db_compat.py. All imports remain at top level.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/unit/test_claiming.py tests/test_atomic_claiming.py tests/test_version_locking.py -x -q 2>/dev/null</automated>
  </verify>
  <done>
    Both CAS functions contain created_at=job.created_at in filter. Old lines commented out above corrected lines. All existing CAS tests pass. version field is present in both filters.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add created_at to mark_running, mark_success, mark_failed filters; verify save_meta (checklist items 3-6)</name>
  <files>src/sqlery/django_sqlery/models.py</files>
  <behavior>
    - mark_running (line ~647): filter(id=self.id, version=expected_version) → add created_at=self.created_at; version retained
    - mark_success (line ~680): filter(id=self.id, version=expected_version) → add created_at=self.created_at; version retained
    - mark_failed (line ~731): filter(id=self.id, version=expected_version) → add created_at=self.created_at; version retained
    - save_meta (line ~852): already filter(id=self.id, created_at=self.created_at) per Phase 15 audit — READ and confirm, no change needed; note "verified, no change" in a comment
    - ConcurrentModificationError still raised when rows_updated == 0 — semantics unchanged
    - Old filter lines commented out, corrected lines added below (CLAUDE.md convention)
  </behavior>
  <read_first>
    src/sqlery/django_sqlery/models.py mark_running (line 633-659), mark_success (line 661-698), mark_failed (line 706-760), save_meta (line 849-852) — locate exact filter() call in each function
  </read_first>
  <action>
    In mark_running: locate `QueuedJob.objects.filter(id=self.id, version=expected_version).update(`. Comment out that line. Add below:
      `QueuedJob.objects.filter(id=self.id, created_at=self.created_at, version=expected_version).update(`

    In mark_success: same pattern — locate filter(id=self.id, version=expected_version). Comment out, add below with created_at=self.created_at added between id and version.

    In mark_failed: same pattern — locate filter(id=self.id, version=expected_version). Comment out, add below with created_at=self.created_at.

    In save_meta: READ the existing filter call. If it already includes created_at (expected from Phase 15 15-01-PLAN.md): add an inline comment `# created_at already present per Phase 15 — checklist item 6 verified` and make no code change.

    Per CLAUDE.md: comment out wrong lines (old filter args), add corrected lines below. Do not delete any code.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/test_models.py tests/test_version_locking.py tests/test_concurrency_and_timeout.py -x -q 2>/dev/null</automated>
  </verify>
  <done>
    mark_running, mark_success, mark_failed each have created_at=self.created_at in their QueuedJob.objects.filter() CAS calls. version field is retained in all three. save_meta already had created_at (Phase 15) — confirmed and commented. Old filter lines are commented out above corrected lines. Existing model tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claim CAS path (db_compat.py) | Worker-provided job object; created_at must come from the DB-returned row, not from user input |
| Model lifecycle transitions | mark_running/mark_success/mark_failed called by job executor with job instance loaded from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | atomic_claim_job_sqlite CAS | accept | created_at sourced from DB-fetched job row (get_claimable_jobs returns full rows); not user-supplied |
| T-16-05 | Tampering | atomic_claim_job_postgres CAS | accept | Same — row locked by SELECT FOR UPDATE before this update; created_at from locked row |
| T-16-06 | Denial of Service | CAS filter with extra column | accept | Adding created_at to filter is index-friendly; partition pruning REDUCES total rows scanned |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
- `env -u VIRTUAL_ENV uv run pytest tests/unit/test_claiming.py tests/test_atomic_claiming.py tests/test_version_locking.py tests/test_models.py -x -q`
- grep confirms: `grep -n "created_at=job.created_at" src/sqlery/django_sqlery/db_compat.py` returns 2 hits (one per CAS function)
- grep confirms: `grep -n "created_at=self.created_at" src/sqlery/django_sqlery/models.py` returns 3+ hits (mark_running, mark_success, mark_failed, save_meta)
- grep confirms version not removed: `grep -n "version=expected_version" src/sqlery/django_sqlery/db_compat.py src/sqlery/django_sqlery/models.py` returns hits in all 5 CAS functions
</verification>

<success_criteria>
- Items 1-5: created_at=job.created_at (or self.created_at) added to every CAS filter in db_compat.py and models.py
- Item 6: save_meta already had created_at — confirmed, comment added, no code change
- version field retained in all CAS filters (D7)
- Old filter lines commented out, corrected lines below (CLAUDE.md)
- All existing claim/model tests pass
</success_criteria>

<output>
Create .planning/phases/16-backend-wiring-pruning/16-02-SUMMARY.md when done.
</output>
