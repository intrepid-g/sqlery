---
phase: 04-security-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # CLEAN-04: webhooks move + stub
  - src/sqlery/webhooks.py
  - src/sqlery/django_sqlery/webhooks.py
  - tests/unit/test_clean04_webhook_import.py
  # CLEAN-01: 20 BC stubs date-stamp (existing files, single-line edit each)
  - src/sqlery/admin.py
  - src/sqlery/apps.py
  - src/sqlery/cleanup.py
  - src/sqlery/daemon_manager.py
  - src/sqlery/daemon_middleware.py
  - src/sqlery/daemon_worker.py
  - src/sqlery/dashboard_views.py
  - src/sqlery/db_compat.py
  - src/sqlery/http_trigger_middleware.py
  - src/sqlery/middleware.py
  - src/sqlery/models.py
  - src/sqlery/registries.py
  - src/sqlery/settings.py
  - src/sqlery/subprocess_executor.py
  - src/sqlery/subprocess_middleware.py
  - src/sqlery/urls.py
  - src/sqlery/views.py
  - src/sqlery/worker_claiming.py
  - src/sqlery/worker_process.py
  - src/sqlery/worker_registry.py
  # CLEAN-03: commented-block hotspots (top 4 per RESEARCH)
  - src/sqlery/core/daemon.py
  - src/sqlery/rate_limit_utils.py
  - src/sqlery/django_sqlery/utils.py
  - src/sqlery/core/worker.py
  # worker_process.py:71 arity fix (canonical Django copy)
  - src/sqlery/django_sqlery/worker_process.py
  - tests/unit/test_worker_process_arity.py
autonomous: false
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04]
tags: [cleanup, dead-code, refactor, bugfix]

must_haves:
  truths:
    - "`from sqlery.django_sqlery.webhooks import send_webhook_with_retry` succeeds"
    - "`from sqlery.webhooks import send_webhook_with_retry` still succeeds (BC stub)"
    - "All 20 marker-only BC stubs carry an explicit `Remove after 2027-05-14` line"
    - "`src/sqlery/async_worker.py` already has remove-after marker (CLEAN-02 = verification no-op)"
    - "Top 4 commented-block files have `#CLEANUP 2026-05-14 — remove after 2027-05-14` markers on dead blocks"
    - "`claim_next_job_with_queue_priority` is called with the full `(worker, backend, queues)` signature at django_sqlery/worker_process.py:71"
  artifacts:
    - path: "src/sqlery/django_sqlery/webhooks.py"
      provides: "Canonical webhooks module — Django-coupled, exports send_webhook_with_retry"
      contains: "def send_webhook_with_retry"
    - path: "src/sqlery/webhooks.py"
      provides: "Dated BC stub re-exporting from django_sqlery.webhooks"
      contains: "from sqlery.django_sqlery.webhooks import"
    - path: "tests/unit/test_clean04_webhook_import.py"
      provides: "Both-import-paths regression test"
      min_lines: 15
    - path: "tests/unit/test_worker_process_arity.py"
      provides: "Regression test for worker_process.py:71 calling claim_next_job_with_queue_priority with backend+queues"
      min_lines: 20
  key_links:
    - from: "src/sqlery/django_sqlery/models.py:678,734"
      to: "sqlery.django_sqlery.webhooks.send_webhook_with_retry"
      via: "`from .webhooks import send_webhook_with_retry` (intra-package)"
      pattern: "from \\.webhooks import send_webhook_with_retry"
    - from: "src/sqlery/django_sqlery/worker_process.py:71"
      to: "src/sqlery/core/claiming.py:178 claim_next_job_with_queue_priority"
      via: "explicit (worker, backend, queues) call"
      pattern: "claim_next_job_with_queue_priority\\(worker,\\s*backend,\\s*queues"
---

<objective>
Omnibus cleanup plan covering CLEAN-01/02/03/04 + the deferred `worker_process.py:71` arity bug from Phase 03. Each task is atomically committable, narrowly scoped, and obeys the user's `feedback_dead_code` policy (mark + date, never delete).

The headline structural fix is CLEAN-04: the `from .webhooks import …` calls inside `django_sqlery/models.py` resolve to a non-existent `sqlery/django_sqlery/webhooks.py`. We move the canonical file into `django_sqlery/` (it's already Django-coupled — uses `django.db.models.F`, `.objects.filter`, `.save(update_fields=…)`) and leave a dated BC stub at the old path. Verified at plan time: `from sqlery.django_sqlery.webhooks import send_webhook_with_retry` raises `ModuleNotFoundError` — bug is live.

Purpose: clear technical debt and the arity bug before Wave-2 SEC plans patch the same files.
Output: 22 file edits + 2 new test files + 1 file move (with stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-security-cleanup/04-CONTEXT.md
@.planning/phases/04-security-cleanup/04-RESEARCH.md
@CLAUDE.md
@/Users/user/.claude/projects/-Users-user-Documents-GitHub-sqlery/memory/feedback_dead_code.md

<interfaces>
From RESEARCH verification:
- `src/sqlery/webhooks.py` is Django-coupled (imports `F`, uses ORM `.save(update_fields=…)` at lines 145, 184, 205, 213) — safe to relocate under `django_sqlery/`.
- `tests/unit/test_webhooks.py:47` imports `from sqlery import webhooks as webhooks_mod` — BC stub must keep this working.
- 20 marker-only BC stubs enumerated in RESEARCH "Backward-Compat Stub Inventory" table.
- `src/sqlery/async_worker.py:1-2` already carries `#CLEANUP: 2026-05-14 ... Remove after 2026-11-14` — CLEAN-02 is verification only.
- `src/sqlery/django_sqlery/worker_process.py:71` canonical signature mismatch: `claim_next_job_with_queue_priority(worker)` → must be `(worker, backend, queues)` per `core/claiming.py:178`. Same fix pattern as Phase 03 commit `f22049d`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLEAN-04 — verify bug, then move webhooks.py into django_sqlery/ with dated BC stub at old path</name>
  <files>
    src/sqlery/webhooks.py,
    src/sqlery/django_sqlery/webhooks.py,
    tests/unit/test_clean04_webhook_import.py
  </files>
  <read_first>
    - src/sqlery/webhooks.py (entire file)
    - src/sqlery/django_sqlery/models.py lines 670-740 (callsite of `from .webhooks import send_webhook_with_retry`)
    - tests/unit/test_webhooks.py lines 1-60 (existing import patterns)
  </read_first>
  <action>
    Step A — verify bug status: run `uv run python -c "from sqlery.django_sqlery.webhooks import send_webhook_with_retry"`. If this SUCCEEDS, skip to Step C (write the regression test only). If it raises ModuleNotFoundError (expected — confirmed at plan time), proceed with Step B.

    Step B — move the file:
    1. Copy current contents of `src/sqlery/webhooks.py` to `src/sqlery/django_sqlery/webhooks.py`. The `.models` / `.settings` intra-package imports inside the file MUST be rewritten so they resolve in the new location:
       - `from .models import QueuedJob` (resolves to `sqlery.models` shim at old path) → at the new location it resolves to `sqlery.django_sqlery.models` — that's the canonical location, so the line stays IDENTICAL and works correctly post-move.
       - Same for `from .settings import get_setting`.
       Verify with `grep -n '^from \.' src/sqlery/webhooks.py` before the move and confirm each dotted import resolves correctly in `django_sqlery/`.
    2. Replace `src/sqlery/webhooks.py` with a BC stub:
       ```
       # #CLEANUP 2026-05-14: This module has been moved to sqlery.django_sqlery.webhooks
       # (CLEAN-04, Phase 04). This stub re-exports for backward compatibility.
       # Remove after 2027-05-14.
       from sqlery.django_sqlery.webhooks import *  # noqa: F401,F403
       from sqlery.django_sqlery.webhooks import send_webhook_with_retry  # noqa: F401
       ```
       NEVER delete the file — per user `feedback_dead_code` memory.
    3. Leave `src/sqlery/django_sqlery/models.py:678,734` UNCHANGED — `from .webhooks import send_webhook_with_retry` now resolves correctly.

    Step C — write regression test `tests/unit/test_clean04_webhook_import.py` with two asserts:
    - `from sqlery.webhooks import send_webhook_with_retry` succeeds (BC path)
    - `from sqlery.django_sqlery.webhooks import send_webhook_with_retry` succeeds (canonical path)
    - Both resolve to the same callable (`is` check)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_clean04_webhook_import.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sqlery/django_sqlery/webhooks.py` exists and contains `def send_webhook_with_retry`.
    - `src/sqlery/webhooks.py` is a dated stub (≤ 10 lines, no business logic), with `#CLEANUP 2026-05-14` and `Remove after 2027-05-14` markers.
    - `tests/unit/test_clean04_webhook_import.py` passes.
    - `uv run pytest tests/unit/test_webhooks.py -x` still passes (BC preserved).
  </acceptance_criteria>
  <done>CLEAN-04 fixed structurally; both import paths work; regression locked in.</done>
</task>

<task type="auto">
  <name>Task 2: CLEAN-01 — date-stamp 20 marker-only BC stubs with `Remove after 2027-05-14`</name>
  <files>
    src/sqlery/admin.py, src/sqlery/apps.py, src/sqlery/cleanup.py,
    src/sqlery/daemon_manager.py, src/sqlery/daemon_middleware.py,
    src/sqlery/daemon_worker.py, src/sqlery/dashboard_views.py,
    src/sqlery/db_compat.py, src/sqlery/http_trigger_middleware.py,
    src/sqlery/middleware.py, src/sqlery/models.py, src/sqlery/registries.py,
    src/sqlery/settings.py, src/sqlery/subprocess_executor.py,
    src/sqlery/subprocess_middleware.py, src/sqlery/urls.py, src/sqlery/views.py,
    src/sqlery/worker_claiming.py, src/sqlery/worker_process.py,
    src/sqlery/worker_registry.py
  </files>
  <read_first>
    - .planning/phases/04-security-cleanup/04-RESEARCH.md (Backward-Compat Stub Inventory table)
    - src/sqlery/admin.py (one representative shim — confirm existing marker shape)
    - src/sqlery/models.py (another representative shim)
  </read_first>
  <action>
    For each of the 20 listed files, insert a single new comment line IMMEDIATELY AFTER the existing `# #CLEANUP: This file has been moved...` line, reading:
    ```
    # Remove after 2027-05-14 (12 months from CLEAN-01 stamp date 2026-05-14, per Phase 04 CONTEXT).
    ```
    Do NOT modify the `from .X import Y` re-export line(s). Do NOT modify any other content. The goal is a 1-line additive change per file.

    After edits, verify with: `grep -l "Remove after 2027-05-14" src/sqlery/*.py | wc -l` → must be ≥ 20.

    Note: `src/sqlery/worker_process.py` is BOTH a CLEAN-01 stub AND the shim that re-exports the Django worker_process. The arity fix in Task 5 is in the CANONICAL file (`src/sqlery/django_sqlery/worker_process.py`), not this shim — keep changes here limited to the date-stamp.
  </action>
  <verify>
    <automated>test $(grep -l "Remove after 2027-05-14" src/sqlery/admin.py src/sqlery/apps.py src/sqlery/cleanup.py src/sqlery/daemon_manager.py src/sqlery/daemon_middleware.py src/sqlery/daemon_worker.py src/sqlery/dashboard_views.py src/sqlery/db_compat.py src/sqlery/http_trigger_middleware.py src/sqlery/middleware.py src/sqlery/models.py src/sqlery/registries.py src/sqlery/settings.py src/sqlery/subprocess_executor.py src/sqlery/subprocess_middleware.py src/sqlery/urls.py src/sqlery/views.py src/sqlery/worker_claiming.py src/sqlery/worker_process.py src/sqlery/worker_registry.py 2>/dev/null | wc -l) -eq 20</automated>
  </verify>
  <acceptance_criteria>
    - All 20 files contain the `Remove after 2027-05-14` marker line.
    - No re-export `from .X import Y` lines were modified.
    - `uv run python -c "import sqlery; import sqlery.models; import sqlery.settings"` still works (BC preserved).
  </acceptance_criteria>
  <done>All 20 BC stubs carry explicit remove-after dates.</done>
</task>

<task type="auto">
  <name>Task 3: CLEAN-02 verification (no-op) + CLEAN-03 commented-block date-stamps in top 4 hotspots</name>
  <files>
    src/sqlery/core/daemon.py,
    src/sqlery/rate_limit_utils.py,
    src/sqlery/django_sqlery/utils.py,
    src/sqlery/core/worker.py
  </files>
  <read_first>
    - src/sqlery/async_worker.py lines 1-5 (CLEAN-02 verification — confirm marker already present)
    - src/sqlery/core/daemon.py (locate the 5 commented blocks per RESEARCH)
    - src/sqlery/rate_limit_utils.py (3 blocks)
    - src/sqlery/django_sqlery/utils.py (3 blocks)
    - src/sqlery/core/worker.py (3 blocks)
  </read_first>
  <action>
    CLEAN-02 verification: run `grep -n "Remove after" src/sqlery/async_worker.py`. Confirm output contains `Remove after 2026-11-14`. If yes, NO EDIT — document the verification result in the task's SUMMARY note. If no (unexpected), add the marker.

    CLEAN-03: For each of the 4 hotspot files, identify contiguous commented-out code blocks (3+ lines starting with `#` that contain Python tokens like `def`, `=`, `import`, or `return`). For each such block, INSERT a single marker line IMMEDIATELY BEFORE the block:
    ```
    # #CLEANUP 2026-05-14: dead code below — remove after 2027-05-14.
    ```
    Do NOT delete the commented block itself. Do NOT touch live code. If a block is shorter than 3 lines or is obviously a docstring/example, skip it.

    Cap: edit no more than 20 distinct insertion points across the 4 files combined (to keep this task atomic). If more candidates exist, leave them for a follow-up CLEAN-03b plan and note in the task SUMMARY.
  </action>
  <verify>
    <automated>test $(grep -c "Remove after 2027-05-14" src/sqlery/core/daemon.py src/sqlery/rate_limit_utils.py src/sqlery/django_sqlery/utils.py src/sqlery/core/worker.py 2>/dev/null | grep -v ':0' | wc -l) -ge 1 && grep -q "Remove after 2026-11-14" src/sqlery/async_worker.py</automated>
  </verify>
  <acceptance_criteria>
    - `async_worker.py` marker confirmed (CLEAN-02 verification recorded in SUMMARY).
    - At least one CLEAN-03 marker added per hotspot file (or noted as "no qualifying blocks" in SUMMARY).
    - No commented blocks were deleted.
    - No live code was modified.
  </acceptance_criteria>
  <done>CLEAN-02 verified; CLEAN-03 hotspots date-stamped.</done>
</task>

<task type="auto">
  <name>Task 4: Fix worker_process.py:71 arity bug + regression test</name>
  <files>
    src/sqlery/django_sqlery/worker_process.py,
    tests/unit/test_worker_process_arity.py
  </files>
  <read_first>
    - src/sqlery/django_sqlery/worker_process.py lines 60-90 (the broken call)
    - src/sqlery/core/claiming.py lines 170-200 (canonical signature)
    - .planning/phases/03-testing-ci/03-GAPS-SUMMARY.md (Fix 1 pattern reference)
  </read_first>
  <action>
    Patch the bad call at `src/sqlery/django_sqlery/worker_process.py:71`. Before the call, resolve the backend and queues from runtime config:
    ```
    from sqlery.compat import get_backend
    from sqlery.django_sqlery.settings import get_setting
    backend = get_backend()
    queues = get_setting("WORKER_QUEUES", ["default"])
    job = claim_next_job_with_queue_priority(worker, backend, queues)
    ```
    If `get_backend` / `get_setting` are already imported at module top, reuse the existing imports — do not duplicate. Match the import style of the surrounding file.

    Write `tests/unit/test_worker_process_arity.py` that imports the function, inspects its signature via `inspect.signature`, and asserts: (a) the canonical `claim_next_job_with_queue_priority` requires `worker`, `backend`, `queues` positional params; (b) a static/AST scan of `django_sqlery/worker_process.py` confirms the call at the patched location passes 3 args (use `ast.parse` + walk for `Call(func=Name(id="claim_next_job_with_queue_priority"))` and assert `len(node.args) >= 3`).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_worker_process_arity.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - The call at `django_sqlery/worker_process.py` passes `(worker, backend, queues)`.
    - Regression test passes and would fail if the args list were truncated.
    - No new TypeError at import time.
  </acceptance_criteria>
  <done>Arity bug closed; regression locked in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human review — 24 file edits + 1 file move</name>
  <what-built>
    CLEAN-04 webhooks file moved + BC stub at old path + regression test.
    CLEAN-01: 20 BC stubs date-stamped.
    CLEAN-02: async_worker.py marker verified (no-op).
    CLEAN-03: top 4 commented-block hotspots date-stamped.
    worker_process.py:71 arity bug fixed + regression test.
  </what-built>
  <how-to-verify>
    1. `git diff --stat` — expect roughly 24 modified files + 3 new files (webhooks.py at new location, two new test files).
    2. `uv run pytest tests/unit/test_clean04_webhook_import.py tests/unit/test_worker_process_arity.py tests/unit/test_webhooks.py -v` — all green.
    3. `uv run python -c "from sqlery.webhooks import send_webhook_with_retry as a; from sqlery.django_sqlery.webhooks import send_webhook_with_retry as b; assert a is b"` — no error.
    4. Spot-check 3 BC stubs (e.g. `src/sqlery/admin.py`, `src/sqlery/models.py`, `src/sqlery/views.py`) — each contains `Remove after 2027-05-14`.
    5. `grep -n "Remove after 2026-11-14" src/sqlery/async_worker.py` — non-empty.
    6. `git diff src/sqlery/django_sqlery/worker_process.py` — shows 3-arg call.
    7. No file was deleted (`git status` shows no `deleted:` entries).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what needs adjustment.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BC stub re-exports | A botched re-export breaks downstream imports across the whole tree |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `src/sqlery/webhooks.py` (post-move BC stub) | mitigate | Stub re-exports `*` AND explicit name; regression test asserts both paths resolve to same callable |
| T-04-01-02 | DoS | `worker_process.py:71` arity bug | mitigate | Patch + AST-based regression test prevents recurrence |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_clean04_webhook_import.py tests/unit/test_worker_process_arity.py -v` green.
- `uv run pytest tests/unit/test_webhooks.py -v` green (BC unchanged).
- 20 BC stubs grep-confirmed.
- async_worker.py marker grep-confirmed.
- No `deleted:` entries in `git status`.
</verification>

<success_criteria>
1. Both `sqlery.webhooks` and `sqlery.django_sqlery.webhooks` import paths succeed and resolve to the same callable.
2. All 20 marker-only BC stubs carry `Remove after 2027-05-14`.
3. CLEAN-02 verification recorded (no edit to async_worker.py).
4. CLEAN-03 hotspots have at least one dated marker each (or "no qualifying blocks" noted).
5. `worker_process.py:71` calls `claim_next_job_with_queue_priority` with 3 positional args.
6. No file was deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-cleanup/04-01-SUMMARY.md`.
</output>
