---
status: paused
paused_at: 2026-06-15
milestone: v0.24.0 (partition-bloat-elimination) — COMPLETE, tagged v0.24.0
branch: v0.22.3-branch
---

# Continue Here

## State
Milestone **v0.24.0 is shipped** (audit passed 11/11, tagged `v0.24.0`, phases archived to `.planning/milestones/v0.24.0-phases/`). Work since then is post-milestone fixes + sample-project tooling, all committed on `v0.22.3-branch`.

## Post-milestone commits NOT in any PR yet
- `b665381` — fix: non-locking `_has_more_queued_jobs()` probe (select_for_update-outside-txn crash on PG worker). Test: `tests/test_more_jobs_probe_regression.py`; REGRESSIONS.md entry added.
- `d63d8df` — fix: resolve `fields.E100` on `QueuedJob.id` (BigAutoField→BigIntegerField + uuid7 `_generate_job_id` default; state-only migration `0032`). Verified PG + SQLite (632 passed).
- `f637790` — chore: Makefile `make sample-project-postgres` (from-scratch Django+PG dashboard at http://127.0.0.1:8000/, admin/admin), `sample-worker-postgres`, `sample-project-postgres-stop`.
- (also: `hourly_counter_task` + `register_hourly_counter()` in `sample_project/tasks_app/tasks.py` — demo job: increments a file counter and self-schedules +2s; hourly cron `0 * * * *`.)

## Open PR stack (GitHub, repo intrepid-g/sqlery)
- #8 v0.23 catch-up (base `main`) → #1 p12 → #2 p13 → #3 p14 → #4 p15 → #5 p16 → #6 p17 → #7 p18. Branches `v0.24-base`, `v0.24-p12..18`. Review bottom-up. The 3 post-milestone commits above are NOT in this stack.

## Next steps (pick up here)
1. Open a PR for the 3 post-milestone fix commits (or fold into the stack).
2. Tech debt (in `.planning/milestones/v0.24.0-MILESTONE-AUDIT.md`): PG-aware cleanup-test fixtures (~8–11 Django cleanup tests assert `deleted==N`, fail on live partitioned PG; SQLite baseline clean); standalone `staging_depth` metric returns None.
3. Verify CI: now Python-3.13-only; needs a Postgres service + `aiosqlite`/`greenlet` (added to dev extra) for PG-gated tests.

## Env notes
- Test PG container for the milestone is torn down. Sample PG: `make sample-project-postgres` (port env `POSTGRES_PORT`, default 55433).
- Run tests with `env -u VIRTUAL_ENV uv run pytest ...` (stale VIRTUAL_ENV in shell). See memory `project_sqlery_test_env_gotchas`.
</content>
</parameter>
