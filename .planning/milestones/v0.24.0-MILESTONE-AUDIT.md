---
milestone: v0.24.0
milestone_name: partition-bloat-elimination
audited: 2026-06-12
status: passed
scores:
  requirements: 11/11
  phases: 7/7
  integration: 7/7 cross-phase chains wired
  flows: 6/6 E2E flows complete
gaps: {}
tech_debt:
  - phase: 17-fastapi-parity / 16-backend-wiring-pruning
    items:
      - "PG-aware cleanup test fixtures (integration WARNING-3): ~8-11 Django cleanup tests (test_ttl_retention TestCleanupOldJobs, test_django_backend TestCleanup, test_batched_cleanup) assert result['deleted']==N and fail when run against a LIVE partitioned PG because the D5 reclaim path returns {deleted:0, reclaimed_via_partition_drop:True}. The SQLite baseline (1055 tests) is unaffected and production code is correct. Needs a fixture that mocks _partitioned_pg()=False for the batched-DELETE assertions, or new PG-specific tests asserting on reclaimed_via_partition_drop."
      - "R9 staging_depth metric is Django-ORM-based (ScheduledJob.objects.count()); in standalone mode ScheduledJob is guarded to None so the metric logs as staging_depth=None. Minor operator-parity gap; the other 4 metrics work in both modes."
  - phase: 17-fastapi-parity
    items:
      - "Standalone shared-sequence: standalone QueuedJob/ScheduledJob ids are assigned by the before_flush UUID-v7 listener (both backends), NOT the sqlery_job_id_seq sequence (which exists on PG as a shadowed column DEFAULT). No-collision guarantee holds via UUID-v7 uniqueness. Docstrings corrected in the audit. True sequence-drawn parity with Django would require the listener to skip on PG — deferred (current behavior is correct, just UUID-based)."
resolved_during_audit:
  - "WARNING-1: inaccurate ScheduledJob/QueuedJob docstrings (claimed a nonexistent Alembic migration wires the shared sequence) — corrected to describe the real UUID-v7 listener behavior."
  - "WARNING-2: get_raw_cursor() absent from DatabaseBackend ABC — added a concrete default returning None so the daemon's duck-typed call is contract-backed."
  - "Phase 17 VERIFICATION human_needed items (greenlet/async-on-PG, cold-start cache) — resolved (greenlet+aiosqlite added to dev deps; async-on-PG tests pass; Bug-SA-01 fixed and verified from a live PG catalog query)."
---

# Milestone v0.24.0 — partition-bloat-elimination — Audit

## Verdict: PASSED

All 11 requirements satisfied across 7 phases. Cross-phase integration verified (PASS, 0 blockers). End-to-end flows complete on both Django and standalone/SQLAlchemy modes, on both SQLite and PostgreSQL.

## Phase Verification Summary

| Phase | Name | Status |
|-------|------|--------|
| 12 | quick-wins | passed |
| 13 | partition-core | passed |
| 14 | scheduled-job-staging | passed |
| 15 | schema-cutover | passed (gating: 1M-row round-trip on real PG) |
| 16 | backend-wiring-pruning | passed |
| 17 | fastapi-parity | passed (human_needed items resolved in-audit) |
| 18 | listen-notify | passed (optional phase, delivered) |

## Requirements Coverage (3-source cross-reference)

| REQ | Description | Phases | Status |
|-----|-------------|--------|--------|
| R1 | partial-pending-index | 12 (+17 standalone) | satisfied |
| R2 | batched-delete-cleanup | 12 (+17) | satisfied |
| R3 | partition-drop-reclaim | 13, 16 (+17) | satisfied |
| R4 | backpressure-invariant | 13 (+17) | satisfied |
| R5 | scheduled-staging | 14 (+17) | satisfied |
| R6 | single-partition-writes | 15, 16 (+17) | satisfied |
| R7 | migration-rollback | 15 | satisfied |
| R8 | advisory-lock-coordination | 13 | satisfied |
| R9 | operator-metrics | 13, 16 | satisfied (standalone staging_depth parity note) |
| R10 | sqlite-unchanged | 12, 16 | satisfied |
| R11 | python-313-floor | 12 | satisfied |

## Cross-Phase Integration (7/7 wired)

1. daemon → get_raw_cursor → promote_due_scheduled_jobs / reclaim_drained_partitions — WIRED
2. cleanup routing seam → _partitioned_pg() → reclaim_drained_partitions — WIRED
3. Django migration chain 0028 → 0029 → 0030 → 0031 (shared sqlery_job_id_seq survives cutover) — WIRED
4. Composite PK created_at threaded through all 11 write-path methods (both backends) — WIRED
5. Standalone (database.py + SQLAlchemyBackend) mirrors partitioning/staging/cleanup/vacuum-skip — WIRED
6. SQLite path unchanged across all phases (D6/R10) — WIRED
7. LISTEN/NOTIFY opt-in, fork-safe, byte-identical when off (D8) — WIRED

## Test Evidence

- Full SQLite baseline (excl. pre-existing env-gap files): 1055 passed, 0 non-noise failures.
- Real Postgres 15: Django + standalone lifecycle + divergence + EXPLAIN-pruning all pass; Phase 15 ≥1M-row migration round-trip passes (0 rows in DEFAULT, identity continues, idempotent rerun, rollback preserves rows).
- Migration chain applies cleanly on a fresh PG DB; makemigrations --check clean.

## Approved Deviations (user-decided, recorded in STATE.md)

- Migration renumber: staging=0029, cutover=0030 (execution order).
- Shared standalone sequence sqlery_job_id_seq replacing Django GENERATED-AS-IDENTITY (identity can't be shared/survive cutover).
- Full-fidelity payload for staged jobs (Phase 14).

## Remaining Tech Debt (non-blocking)

See `tech_debt` in frontmatter. None block milestone completion. Recommend tracking the PG-aware cleanup test fixtures as a follow-up.
