---
phase: 18-listen-notify
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/settings.py
  - src/sqlery/fastapi_sqlery/config.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "SQLERY_PG_NOTIFY=False is the default in the Django DEFAULTS dict"
    - "SQLERY_PG_NOTIFY is loadable via get_config('SQLERY_PG_NOTIFY', False) in both adapters"
    - "StandaloneConfig has SQLERY_PG_NOTIFY=False and loads it from env var SQLERY_PG_NOTIFY"
    - "With the flag absent, zero behavior change — all existing tests still pass"
  artifacts:
    - path: src/sqlery/django_sqlery/settings.py
      provides: "SQLERY_PG_NOTIFY key in DEFAULTS dict"
      contains: "SQLERY_PG_NOTIFY"
    - path: src/sqlery/fastapi_sqlery/config.py
      provides: "SQLERY_PG_NOTIFY key in StandaloneConfig._config"
      contains: "SQLERY_PG_NOTIFY"
  key_links:
    - from: "src/sqlery/django_sqlery/settings.py"
      to: "src/sqlery/django_sqlery/backend.py"
      via: "get_setting('SQLERY_PG_NOTIFY', False)"
      pattern: "SQLERY_PG_NOTIFY"
    - from: "src/sqlery/fastapi_sqlery/config.py"
      to: "src/sqlery/fastapi_sqlery/backend.py"
      via: "get_config('SQLERY_PG_NOTIFY', False)"
      pattern: "SQLERY_PG_NOTIFY"
---

<objective>
Add the SQLERY_PG_NOTIFY=False opt-in config flag to both adapter config systems.

Purpose: This is the sole feature gate for Phase 18 (D1, D8). Both Django settings and
StandaloneConfig must expose it with False as the default so all existing code paths are
byte-identical when the flag is absent.

Output: Two edited files. No new files. The flag is readable via the existing
get_config/get_setting access path in each adapter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-listen-notify/18-CONTEXT.md
@src/sqlery/django_sqlery/settings.py
@src/sqlery/fastapi_sqlery/config.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add SQLERY_PG_NOTIFY to Django DEFAULTS (per D1)</name>
  <files>src/sqlery/django_sqlery/settings.py</files>
  <action>
In DEFAULTS dict in src/sqlery/django_sqlery/settings.py, add the flag
SQLERY_PG_NOTIFY=False immediately after the "DB resilience settings" block (after
PG_LOCK_TIMEOUT_MS). Comment the line with "# Opt-in PG LISTEN/NOTIFY dispatch
(Phase 18). Default False = byte-identical polling behaviour." Do NOT delete or
replace any existing line — comment out the old line if modifying; in this case the
flag is new so simply add it. The DEFAULTS dict is read by get_setting(name, default)
which already provides the correct fallback chain; no changes to get_setting are needed.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && python -c "from sqlery.django_sqlery.settings import DEFAULTS; assert DEFAULTS.get('SQLERY_PG_NOTIFY') == False, 'flag missing or not False'"</automated>
  </verify>
  <done>DEFAULTS['SQLERY_PG_NOTIFY'] is False; get_setting('SQLERY_PG_NOTIFY', False) returns False when DJANGO_SQL_JOBS is unset</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SQLERY_PG_NOTIFY to StandaloneConfig (per D1)</name>
  <files>src/sqlery/fastapi_sqlery/config.py</files>
  <action>
In src/sqlery/fastapi_sqlery/config.py, make two edits:

1. In StandaloneConfig.__init__ self._config dict, add 'SQLERY_PG_NOTIFY': False
   immediately after the INTERNAL_ALLOWED_IPS entry and before the partition
   configuration block. Comment: "# Opt-in PG LISTEN/NOTIFY dispatch (Phase 18).
   # Default False = byte-identical behaviour when off."

2. In _load_from_env, add env-var loading for SQLERY_PG_NOTIFY after the
   SQLERY_INTERNAL_ALLOWED_IPS block. Read os.getenv("SQLERY_PG_NOTIFY"); if not
   None, parse as bool (lower() in ('true', '1', 'yes')); store as
   self._config["SQLERY_PG_NOTIFY"]. Do not touch env_mappings dict (it only covers
   numeric/string vars); add a standalone block for this boolean flag.

Do not modify _PARTITION_KEYS, _validate_partition_config, or any other method.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && python -c "
import os; os.environ.pop('DJANGO_SETTINGS_MODULE', None)
from sqlery.fastapi_sqlery.config import StandaloneConfig
cfg = StandaloneConfig()
assert cfg.get('SQLERY_PG_NOTIFY') == False, 'flag missing'
os.environ['SQLERY_PG_NOTIFY'] = 'true'
cfg2 = StandaloneConfig()
assert cfg2.get('SQLERY_PG_NOTIFY') == True, 'env override failed'
print('OK')
"</automated>
  </verify>
  <done>StandaloneConfig._config['SQLERY_PG_NOTIFY'] defaults to False; SQLERY_PG_NOTIFY=true env var flips it to True</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | SQLERY_PG_NOTIFY env var is parsed as boolean — only True/False outcome, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | SQLERY_PG_NOTIFY env parse | accept | Value coerced to bool via string comparison; arbitrary env content cannot inject SQL or alter logic beyond the flag value |
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run full test suite (SQLite path) to confirm no regression from the flag addition:
`cd /Users/gabriel/Documents/GitHub/sqlery-public && python -m pytest tests/ -x -q --ignore=tests/integration 2>&1 | tail -20`

Both new keys must be present with False defaults. Existing tests must stay green.
</verification>

<success_criteria>
- DEFAULTS['SQLERY_PG_NOTIFY'] == False in django_sqlery/settings.py
- StandaloneConfig().get('SQLERY_PG_NOTIFY') == False
- SQLERY_PG_NOTIFY=true env var → StandaloneConfig().get('SQLERY_PG_NOTIFY') == True
- Full test suite passes with no new failures (flag is additive, no behavior change)
</success_criteria>

<output>
Create `.planning/phases/18-listen-notify/18-01-SUMMARY.md` when done
</output>
