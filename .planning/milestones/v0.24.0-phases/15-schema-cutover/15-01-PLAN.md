---
phase: 15-schema-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/models.py
autonomous: true
requirements:
  - R6
  - R7

must_haves:
  truths:
    - "Blast-radius audit artifact exists at .planning/phases/15-schema-cutover/BLAST-RADIUS-AUDIT.md listing every .pk / pk= / pk__in / refresh_from_db / in_bulk / FK-traversal hit and its disposition"
    - "QueuedJob.pk is models.CompositePrimaryKey('created_at', 'id') and QueuedJob.id is models.BigAutoField(primary_key=False)"
    - "JobRegistry.job is a plain BigIntegerField (db_index=True), not a ForeignKey"
    - "Worker.current_job is a plain BigIntegerField (db_index=True, null=True, blank=True), not a ForeignKey"
    - "save_meta filters on filter(id=self.id, created_at=self.created_at) — the filter(pk=self.pk) line is commented out with the replacement beneath it"
    - "Zero unaddressed .pk hits remain (every hit has a disposition: fixed / deferred-Phase-16 with explanation / N/A)"
  artifacts:
    - path: ".planning/phases/15-schema-cutover/BLAST-RADIUS-AUDIT.md"
      provides: "Enumerated audit hits and dispositions"
    - path: "src/sqlery/django_sqlery/models.py"
      provides: "CompositePrimaryKey, demoted FK fields, save_meta rewrite"
      contains: "CompositePrimaryKey"
  key_links:
    - from: "models.py QueuedJob"
      to: "CompositePrimaryKey('created_at', 'id')"
      via: "pk field declaration"
      pattern: "CompositePrimaryKey"
    - from: "models.py save_meta"
      to: "filter(id=self.id, created_at=self.created_at)"
      via: "commented-out pk=self.pk line replaced by id+created_at filter"
      pattern: "filter\\(id=self\\.id"
---

<objective>
Run the mandatory blast-radius audit for the QueuedJob composite-PK change, then apply
the Step-7 model changes: CompositePrimaryKey("created_at", "id"), BigAutoField with
primary_key=False, demotion of JobRegistry.job and Worker.current_job FKs to plain
indexed BigIntegerField, and the save_meta filter rewrite. (Per D4 — referential integrity
to jobs is intentionally dropped; orphans on partition drop are accepted.)

Purpose: Every downstream task — migration 0030, write-path pruning in Phase 16 — assumes
this model is already in place. The audit artifact gates all of Phase 15.

Output:
- BLAST-RADIUS-AUDIT.md listing every .pk / pk= / pk__in / refresh_from_db / in_bulk /
  FK-traversal hit with dispositions
- models.py with CompositePK, demoted FKs, and save_meta rewrite
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-schema-cutover/15-CONTEXT.md

# Key source files
@src/sqlery/django_sqlery/models.py
@src/sqlery/django_sqlery/apps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blast-radius audit — enumerate every .pk / FK hit and produce BLAST-RADIUS-AUDIT.md</name>
  <files>.planning/phases/15-schema-cutover/BLAST-RADIUS-AUDIT.md</files>
  <read_first>
    - src/sqlery/django_sqlery/models.py — full file; QueuedJob (~line 344), save_meta (~line 826),
      JobRegistry.job FK (~line 981), Worker.current_job FK (~line 1052)
    - src/sqlery/django_sqlery/backend.py — full file; scan for .pk, pk=, pk__in, refresh_from_db,
      in_bulk, QueuedJob FK traversals
    - src/sqlery/django_sqlery/db_compat.py — scan for .pk, pk=, atomic_claim_job_sqlite (~line 100),
      atomic_claim_job_postgres (~line 139)
    - src/sqlery/fastapi_sqlery/backend.py — full file; same scan
    - tests/ — grep for .pk, pk=, pk__in, refresh_from_db, in_bulk patterns on QueuedJob
    - src/sqlery/compat/rq.py — FK/pk usage
    - src/sqlery/compat/scheduler.py — FK/pk usage
  </read_first>
  <action>
    Run the following grep commands to find every hit. Record ALL results, not just the known ones.

    Commands to run (adapt paths as needed):
      grep -rn "\.pk\b" src/sqlery/ --include="*.py"
      grep -rn "pk=" src/sqlery/ --include="*.py"
      grep -rn "pk__in" src/sqlery/ --include="*.py"
      grep -rn "refresh_from_db" src/sqlery/ --include="*.py"
      grep -rn "in_bulk" src/sqlery/ --include="*.py"
      grep -rn "current_job\|JobRegistry.*job\b" src/sqlery/ --include="*.py"
      grep -rn "\.pk\b\|pk=" tests/ --include="*.py"
      grep -rn "refresh_from_db\|in_bulk" tests/ --include="*.py"

    For each hit, record: file path, line number, the code snippet, and one of these dispositions:
      FIXED-HERE — rewritten in Task 2 of this plan
      DEFERRED-PHASE-16 — part of the 11-item write-path pruning checklist; must add created_at filter
      N/A — not a QueuedJob pk/FK access (e.g. Worker.id, ScheduledJob, etc.)
      ACCEPTABLE — .pk usage on a model that does NOT have a composite PK (JobRegistry, Worker, etc.)

    Known mandatory dispositions (from PLAN.md Step 7 / senior review finding #7):
      - models.py save_meta filter(pk=self.pk) → FIXED-HERE (rewritten in Task 2)
      - JobRegistry.job ForeignKey → FIXED-HERE (demoted in Task 2)
      - Worker.current_job ForeignKey → FIXED-HERE (demoted in Task 2)
      - refresh_from_db in refresh_meta (models.py) → ACCEPTABLE (fields=["meta"] still works
        after composite PK; Django refresh_from_db uses pk internally but this call specifies
        fields explicitly — annotate as ACCEPTABLE with this note)

    Write the audit as BLAST-RADIUS-AUDIT.md in Markdown table format:

      # Phase 15 Blast-Radius Audit — QueuedJob Composite PK

      ## Audit scope
      Patterns: `.pk`, `pk=`, `pk__in`, `refresh_from_db`, `in_bulk`, FK traversals of QueuedJob
      Audited: src/sqlery/, tests/

      ## Hits

      | # | File | Line | Snippet | Disposition | Notes |
      |---|------|------|---------|-------------|-------|
      ...

      ## Summary
      - Total hits: N
      - FIXED-HERE: N (completed in 15-01 Task 2)
      - DEFERRED-PHASE-16: N (write-path pruning — adding created_at to id-only filters)
      - N/A: N
      - ACCEPTABLE: N
      - UNADDRESSED: 0  ← must be zero to pass acceptance

    The UNADDRESSED count MUST be zero. If any hit cannot be categorised as one of the above
    dispositions, mark it UNADDRESSED and note why — this will block Phase 15 completion.

    Zero UNADDRESSED hits is a hard acceptance criterion (per success criterion SC5 and R7).
  </action>
  <verify>
    <automated>
      grep -rn "UNADDRESSED" .planning/phases/15-schema-cutover/BLAST-RADIUS-AUDIT.md | grep -v "^#" | grep -v "UNADDRESSED: 0" && echo "FAIL: unaddressed hits remain" || echo "PASS: zero unaddressed hits"
    </automated>
  </verify>
  <done>
    BLAST-RADIUS-AUDIT.md exists; every grep hit has a disposition; UNADDRESSED count is 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Step-7 model changes — CompositePK, FK demotion, save_meta rewrite</name>
  <files>src/sqlery/django_sqlery/models.py</files>
  <read_first>
    - src/sqlery/django_sqlery/models.py — QueuedJob class (~lines 344–500), save_meta (~line 826),
      JobRegistry.job field (~line 981), Worker.current_job field (~line 1052)
    - src/sqlery/django_sqlery/apps.py — confirms default_auto_field = BigAutoField (line 34),
      which means Django emits GENERATED BY DEFAULT AS IDENTITY for id
    - BLAST-RADIUS-AUDIT.md — confirm all FIXED-HERE items are covered by this task
    - .planning/intel/ingest-src/PLAN.md — Step 7 model changes (lines ~191–218)
  </read_first>
  <behavior>
    - After change: QueuedJob.pk is CompositePrimaryKey("created_at", "id")
    - After change: QueuedJob.id is BigAutoField(primary_key=False) — Django 5.2 required
    - After change: JobRegistry.job is BigIntegerField(db_index=True, help_text="...")
    - After change: Worker.current_job is BigIntegerField(null=True, blank=True, db_index=True, help_text="...")
    - After change: save_meta calls filter(id=self.id, created_at=self.created_at).update(meta=self.meta)
    - version-based optimistic locking in mark_running/mark_success/mark_failed filters on id+version,
      never pk — these are DEFERRED-PHASE-16; verify they still work unchanged for SQLite path
  </behavior>
  <action>
    Apply the following changes to src/sqlery/django_sqlery/models.py. In every case: comment
    out the wrong line and add the corrected line beneath it (per CLAUDE.md convention — never
    delete or replace outright).

    CHANGE 1 — CompositePrimaryKey declaration.
    In QueuedJob's class body, BEFORE the task_path field, add:
      pk = models.CompositePrimaryKey("created_at", "id")
    No existing line to comment out — this is a new field declaration.
    Note: Django 5.2 CompositePrimaryKey requires the partition key (created_at) first.

    CHANGE 2 — id field: demote from implicit auto-PK to explicit BigAutoField(primary_key=False).
    Locate the id field if it exists explicitly, or add it explicitly. The apps.py sets
    default_auto_field = BigAutoField which means QueuedJob currently has an implicit id PK.
    Add explicitly to QueuedJob before the task_path field (after the pk declaration):
      id = models.BigAutoField(primary_key=False)
    If an explicit id field already exists in QueuedJob, comment it out and replace.

    CHANGE 3 — save_meta rewrite (D3/R7 per audit FIXED-HERE).
    Locate save_meta (~line 826):
      # Old: QueuedJob.objects.filter(pk=self.pk).update(meta=self.meta)
      QueuedJob.objects.filter(id=self.id, created_at=self.created_at).update(meta=self.meta)

    CHANGE 4 — JobRegistry.job FK demotion (per D4 — referential integrity intentionally dropped).
    Locate JobRegistry.job ForeignKey (~line 981):
      # Old: job = models.ForeignKey(QueuedJob, on_delete=models.CASCADE, related_name="registry_entries", help_text="Job being tracked")
      job_id = models.BigIntegerField(
          db_index=True,
          help_text="ID of the QueuedJob being tracked (FK demoted — D4: referential integrity to partitioned table intentionally dropped)",
      )
    Important: rename the field to job_id (plain integer) to avoid attribute collision. Update
    any __str__ or other methods in JobRegistry that reference self.job to use self.job_id.
    Add a note in a comment: "# #CLEANUP: Remove after soak — rename job_id back to job if FK ever restored"

    CHANGE 5 — Worker.current_job FK demotion (per D4).
    Locate Worker.current_job ForeignKey (~line 1052):
      # Old: current_job = models.ForeignKey(QueuedJob, null=True, blank=True, on_delete=models.SET_NULL, related_name="current_worker", help_text="Job currently being processed")
      current_job_id = models.BigIntegerField(
          null=True,
          blank=True,
          db_index=True,
          help_text="ID of the QueuedJob currently being processed (FK demoted — D4: referential integrity to partitioned table intentionally dropped)",
      )
    Important: rename to current_job_id to avoid attribute collision. Update any Worker methods
    that reference self.current_job to use self.current_job_id.
    Add: "# #CLEANUP: Remove after soak — rename current_job_id back to current_job if FK ever restored"

    After all changes: confirm that version-based optimistic locking methods (mark_running,
    mark_success, mark_failed) filter on id + version — these are unchanged here and remain
    as DEFERRED-PHASE-16 items in the audit. Do NOT add created_at to those filters yet;
    that is the Phase 16 write-path pruning work.

    The refresh_meta method calls self.refresh_from_db(fields=["meta"]) — this is ACCEPTABLE
    per the audit (fields-specific refresh_from_db still works with composite PK). Leave it unchanged.

    Write a brief comment block near the new pk/id declarations explaining the composite PK
    trade-off (D4) so it is findable by future maintainers:
      # Django 5.2 composite PK — partition key (created_at) must be first.
      # id stays globally unique (shared IDENTITY sequence with sqlery_scheduled_job).
      # FKs from JobRegistry and Worker demoted to BigIntegerField per D4 (Phase 15):
      # orphans on partition drop are an accepted, documented trade-off.
  </action>
  <verify>
    <automated>
      cd /Users/gabriel/Documents/GitHub/sqlery-public && env -u VIRTUAL_ENV uv run python -c "import django; django.setup()" 2>&1 | head -5
    </automated>
    <automated>
      cd /Users/gabriel/Documents/GitHub/sqlery-public && env -u VIRTUAL_ENV uv run python -c "
import django
django.setup()
from sqlery.django_sqlery.models import QueuedJob, JobRegistry, Worker
from django.db import models
assert hasattr(QueuedJob, 'pk'), 'pk missing'
# CompositePrimaryKey should be present
pk_field = QueuedJob._meta.get_field('pk') if hasattr(QueuedJob._meta, 'get_field') else None
# Verify job_id on JobRegistry is BigIntegerField
jf = JobRegistry._meta.get_field('job_id')
assert isinstance(jf, models.BigIntegerField), f'Expected BigIntegerField, got {type(jf)}'
# Verify current_job_id on Worker is BigIntegerField
wf = Worker._meta.get_field('current_job_id')
assert isinstance(wf, models.BigIntegerField), f'Expected BigIntegerField, got {type(wf)}'
print('PASS: model field types correct')
" 2>&1
    </automated>
    <automated>
      cd /Users/gabriel/Documents/GitHub/sqlery-public && grep -n "filter(id=self.id, created_at=self.created_at)" src/sqlery/django_sqlery/models.py && echo "PASS: save_meta rewrite present" || echo "FAIL: save_meta not rewritten"
    </automated>
  </verify>
  <done>
    CompositePrimaryKey("created_at", "id") declared on QueuedJob; id = BigAutoField(primary_key=False);
    JobRegistry.job_id and Worker.current_job_id are plain BigIntegerField with db_index=True;
    save_meta uses filter(id=self.id, created_at=self.created_at); all old FK lines commented out
    with corrected lines beneath; django.setup() imports cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM model → DB schema | CompositePrimaryKey declaration must stay synchronized with migration 0030 state_operations |
| audit artifact → plan gating | BLAST-RADIUS-AUDIT.md UNADDRESSED=0 gates the entire phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | save_meta — pk=self.pk becomes a tuple under composite PK, silently updating wrong rows | mitigate | Rewrite to filter(id=self.id, created_at=self.created_at) in Task 2; test in 15-03 round-trip |
| T-15-02 | Tampering | JobRegistry / Worker orphan rows after partition drop | accept | D4: documented trade-off; orphaning is intentional; operators must run registry-cleanup if needed |
| T-15-03 | Denial of service | FK CASCADE/SET_NULL behavior removed from JobRegistry.job and Worker.current_job | accept | Demoting to BigIntegerField eliminates cascades; orphans are bounded by retention window |
| T-15-04 | Information disclosure | .pk returning a tuple unexpectedly reaching API serializers or admin | mitigate | Audit (Task 1) enumerates all .pk accesses; admin and API surface verified in blast-radius scan |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this plan (pure model-layer change, no installs) |
</threat_model>

<verification>
  cd /Users/gabriel/Documents/GitHub/sqlery-public && env -u VIRTUAL_ENV uv run pytest tests/ -x -q --timeout=60 -k "not test_pg" 2>&1 | tail -20
</verification>

<success_criteria>
- BLAST-RADIUS-AUDIT.md exists with UNADDRESSED=0
- QueuedJob has pk=CompositePrimaryKey("created_at","id") and id=BigAutoField(primary_key=False)
- JobRegistry.job_id is BigIntegerField; Worker.current_job_id is BigIntegerField
- save_meta has filter(pk=self.pk) commented out and filter(id=self.id, created_at=self.created_at) active
- All existing SQLite-path tests pass (django.setup() clean; pytest -k "not test_pg" green)
</success_criteria>

<output>
Create .planning/phases/15-schema-cutover/15-01-SUMMARY.md when done.
</output>
