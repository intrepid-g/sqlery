---
phase: 17-fastapi-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/sqlery/fastapi_sqlery/database.py
  - alembic/versions/20260612_0016_partition_queued_job.py
  - src/sqlery/tables.py
autonomous: true
requirements:
  - R1
  - R2
  - R7

must_haves:
  truths:
    - "Fresh standalone PG install via init_database() creates sqlery_queued_job PARTITION BY RANGE(created_at) with a DEFAULT partition and today's partition (per D8)"
    - "Fresh standalone SQLite install via init_database() creates a plain unpartitioned table (D6 unchanged)"
    - "Alembic revision 20260612_0016 chains after 20260608_0015, is PG-only, and is idempotent"
    - "Alembic revision creates sqlery_job_id_seq if absent, sets id column DEFAULT, adds composite PK, creates DEFAULT partition and pending partial index"
    - "tables.py gains SCHEDULED_JOB constant for sqlery_scheduled_job"
  artifacts:
    - path: "src/sqlery/fastapi_sqlery/database.py"
      provides: "Partitioned DDL path for PG fresh installs; plain create_all for SQLite"
      contains: "PARTITION BY RANGE"
    - path: "alembic/versions/20260612_0016_partition_queued_job.py"
      provides: "Standalone cutover migration chaining after 0015; PG-only; idempotent"
      contains: "20260608_0015"
    - path: "src/sqlery/tables.py"
      provides: "SCHEDULED_JOB table name constant"
      contains: "SCHEDULED_JOB"
  key_links:
    - from: "src/sqlery/fastapi_sqlery/database.py init_database()"
      to: "src/sqlery/core/models.py QueuedJob"
      via: "SQLModel.metadata.create_all (SQLite) or raw DDL emission (PG)"
      pattern: "PARTITION BY RANGE"
    - from: "alembic/versions/20260612_0016"
      to: "alembic/versions/20260608_0015"
      via: "down_revision = '20260608_0015'"
      pattern: "down_revision"
---

<objective>
Implement the partitioned-DDL fresh-install path in database.py (PG: partitioned table +
DEFAULT partition + pending partial index; SQLite: plain create_all unchanged) and add the
Alembic cutover revision for existing standalone PG installs upgrading from an unpartitioned
table.

Purpose: SC-2 (fresh install creates partitioned table by default) requires database.py to
emit partitioned DDL on PG. Existing installs upgrading via "alembic upgrade head" need the
cutover migration. The two paths are complementary: database.py handles new installs;
Alembic handles existing ones.

Output: Updated database.py with PG-specific DDL path; new Alembic revision
20260612_0016_partition_queued_job.py chaining after 0015.
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-fastapi-parity/17-CONTEXT.md
@.planning/phases/17-fastapi-parity/17-01-SUMMARY.md
@src/sqlery/fastapi_sqlery/database.py
@src/sqlery/tables.py
@alembic/versions/20260608_0015_add_daemon_lease.py
@src/sqlery/django_sqlery/migrations/0030_partition_queued_job.py
@src/sqlery/django_sqlery/migrations/0031_secondary_indexes.py
@src/sqlery/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Partitioned DDL in database.py (PG fresh install) + tables.py constant</name>
  <files>src/sqlery/fastapi_sqlery/database.py, src/sqlery/tables.py</files>
  <action>
    In tables.py, add the SCHEDULED_JOB constant:
    SCHEDULED_JOB = "sqlery_scheduled_job"

    In database.py, modify init_database() to fork the PG path before calling SQLModel.metadata.create_all:

    After the _engine is created for non-sqlite paths, detect the vendor:
    if not database_url.startswith('sqlite'):
        Check if the engine dialect is postgresql via _engine.dialect.name == "postgresql".
        If PG: call a new private function _init_partitioned_pg(_engine) instead of SQLModel.metadata.create_all.
        If non-PG (other dialects): fall through to SQLModel.metadata.create_all unchanged.
    SQLite path: SQLModel.metadata.create_all(_engine) unchanged (D6).

    Implement _init_partitioned_pg(engine) as a module-level function:
    1. Run SQLModel.metadata.create_all(engine) first — this creates all non-QueuedJob tables (Worker, ScheduledTask, JobRegistry, DaemonLease, ScheduledJobStaging, etc.). QueuedJob's composite PK SQLModel definition will create a plain table if partition DDL is absent, so we must DROP and recreate it.
       Actually: the safer strategy (mirrors Django 0030) is: run create_all to create all *other* tables, then issue the partitioned DDL for sqlery_queued_job explicitly.
       Implementation: first run SQLModel.metadata.create_all(engine, tables=[t for t in SQLModel.metadata.sorted_tables if t.name != 'sqlery_queued_job']) to create non-jobs tables. Then issue the jobs DDL manually.

    2. Execute the partitioned jobs DDL via a raw connection (engine.connect() + connection.execute(text(...))):
       a. Create sqlery_job_id_seq if absent: "CREATE SEQUENCE IF NOT EXISTS sqlery_job_id_seq"
       b. Create sqlery_scheduled_job table if absent: creates it with BIGSERIAL-equivalent (or simply let create_all handle it since it is a separate table). Actually ScheduledJobStaging is already handled by create_all above if we excluded only sqlery_queued_job. Include sqlery_scheduled_job in the non-excluded tables.
       c. Create sqlery_queued_job as PARTITION BY RANGE(created_at) with composite PK (created_at, id) — idempotent via CREATE TABLE IF NOT EXISTS. The full DDL (mirrors Django 0030 S2/S3 for fresh install — no legacy table rename needed here):
         "CREATE TABLE IF NOT EXISTS sqlery_queued_job (id BIGINT NOT NULL DEFAULT nextval('sqlery_job_id_seq'), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), status VARCHAR(20) NOT NULL DEFAULT 'queued', [all other columns from the SQLModel QueuedJob], PRIMARY KEY (created_at, id)) PARTITION BY RANGE (created_at);"
         Note: rather than hand-enumerating all columns (fragile), use a programmatic approach: inspect SQLModel.metadata.tables['sqlery_queued_job'] to get column definitions, then build the DDL using a with-DDL helper. The simplest idempotent approach: attempt CREATE TABLE IF NOT EXISTS sqlery_queued_job (...) PARTITION BY RANGE (created_at); where the column list is taken from the SQLModel metadata via a helper _build_jobs_partition_ddl(metadata). Alternatively, use the LIKE approach: if an unpartitioned sqlery_queued_job already exists (fresh install won't have it; this handles edge cases) we skip. For pure fresh installs the cleanest approach is to call create_all for QueuedJob first (it creates a plain table), then check relkind: if it is 'p' (already partitioned) stop; if 'r' (plain), perform the inline cutover (rename → partition → copy). However that is complex. For a FRESH INSTALL the simplest path: skip create_all for sqlery_queued_job entirely, and emit the DDL from a Python list of Column objects obtained from the metadata.
         Most pragmatic clean implementation: after running create_all for all other tables, execute:
         (i) "CREATE SEQUENCE IF NOT EXISTS sqlery_job_id_seq" — idempotent
         (ii) Check pg_class: if sqlery_queued_job does not exist at all (truly fresh), create it partitioned directly. If it exists and is not partitioned (relkind = 'r'), perform the inline cutover (legacy rename path from 0030). If already partitioned (relkind = 'p'), skip.
         (iii) CREATE TABLE sqlery_queued_job (...) PARTITION BY RANGE (created_at) — column list assembled from Python constants or SQLModel metadata
         (iv) CREATE TABLE IF NOT EXISTS sqlery_queued_job_default PARTITION OF sqlery_queued_job DEFAULT
         (v) Create today's partition: sqlery_queued_job_YYYYMMDD FOR VALUES FROM ('YYYY-MM-DD') TO ('YYYY-MM-DD+1day') — use date_trunc logic matching core/partitioning.py
         (vi) CREATE INDEX IF NOT EXISTS sqlery_job_pending_idx ON sqlery_queued_job (queue_name, priority DESC, created_at) WHERE status = 'queued' — byte-identical to Django 0028 (D7)
       Use text() from sqlalchemy for all raw SQL. Wrap in a connection.begin() transaction; commit at the end. Use autobegin=False or explicit begin().

    The function signature: def _init_partitioned_pg(engine) -> None.
    Import datetime, date from datetime at the top of database.py (already imported indirectly; add explicit import if missing).

    validate_partition_config() from StandaloneConfig should be called here if a config is available. However database.py does not import config. Leave validation to the caller (init_database caller is expected to call get_config().validate_partition_config() separately, or wire it in compat/initialize). Do not add a config import to database.py (keep the module dependency boundary clean).

    Convention: comment out the old SQLModel.metadata.create_all(_engine) line for the PG branch and add the new branching logic beneath.
  </action>
  <verify>
    <automated>SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test python -c "
import os
os.environ['SQLERY_DATABASE_URL'] = os.environ['SQLERY_TEST_PG_URL']
from sqlery.fastapi_sqlery.database import init_database, get_engine
from sqlalchemy import text
url = os.environ['SQLERY_TEST_PG_URL']
# Use a throw-away test database prefix
test_url = url.replace('/sqlery_test', '/sqlery_phase17_ddl_test')
import sqlalchemy as sa
admin = sa.create_engine(url)
with admin.connect() as c:
    c.execute(text('DROP DATABASE IF EXISTS sqlery_phase17_ddl_test'))
    c.execute(text('CREATE DATABASE sqlery_phase17_ddl_test'))
    c.commit()
admin.dispose()
init_database(test_url)
eng = get_engine()
with eng.connect() as c:
    row = c.execute(text(\"SELECT relkind FROM pg_class WHERE relname = 'sqlery_queued_job'\")).fetchone()
    assert row and row[0] == 'p', f'Expected p (partitioned), got {row}'
    print('Fresh PG install: partitioned table OK')
eng.dispose()
"</automated>
  </verify>
  <done>
    init_database() on a fresh PG URL creates sqlery_queued_job with relkind='p' (partitioned). init_database() on a SQLite URL creates a plain table (relkind check not applicable; SQLite create_all runs without error). Tables.py has SCHEDULED_JOB constant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic cutover revision for existing standalone PG installs</name>
  <files>alembic/versions/20260612_0016_partition_queued_job.py</files>
  <action>
    Create alembic/versions/20260612_0016_partition_queued_job.py with:
    revision = '20260612_0016'
    down_revision = '20260608_0015'
    branch_labels = None
    depends_on = None

    upgrade() function: vendor-guarded (only runs on PostgreSQL). Detection: op.get_bind().dialect.name == 'postgresql'. If not PG, return immediately (no-op on SQLite — D6).

    On PG, upgrade() executes the same logical steps as Django migration 0030 (S0–S9), adapted for standalone:

    S0: Drop FK constraints referencing sqlery_queued_job (catalog query + dynamic DROP CONSTRAINT IF EXISTS) — same DO block as 0030 S0.
    S1: Idempotent rename: sqlery_queued_job → sqlery_queued_job_legacy (DO block checking to_regclass — same as 0030 S1).
    S2: CREATE TABLE IF NOT EXISTS sqlery_queued_job (LIKE sqlery_queued_job_legacy INCLUDING DEFAULTS INCLUDING STORAGE) PARTITION BY RANGE (created_at) — same as 0030 S2.
    S3: Add composite PRIMARY KEY (created_at, id) — catalog-guarded for idempotency (same pg_constraint check as 0030 S3).
    S3b: DROP CONSTRAINT IF EXISTS sqlery_queued_job_job_name_key — same as 0030 S3b.
    S4: Removed (same as Django 0030 — id DEFAULT already copied by LIKE INCLUDING DEFAULTS from S2, which used migration 0029's shared sequence; standalone equivalent: CREATE SEQUENCE IF NOT EXISTS sqlery_job_id_seq before S1, then it is present on the legacy table already from the create_all path, so LIKE copies the default).
    Note: For standalone, there may not be a Django migration 0029 that created sqlery_job_id_seq. Add a step before S1: "CREATE SEQUENCE IF NOT EXISTS sqlery_job_id_seq; ALTER TABLE sqlery_queued_job ALTER COLUMN id SET DEFAULT nextval('sqlery_job_id_seq');" so the legacy table carries the default before LIKE copies it.
    S5: CREATE TABLE IF NOT EXISTS sqlery_queued_job_default PARTITION OF sqlery_queued_job DEFAULT — same as Django.
    S6: Recreate sqlery_job_pending_idx (byte-identical to 0028 / Django 0030 S6 — D7): CREATE INDEX IF NOT EXISTS sqlery_job_pending_idx ON sqlery_queued_job (queue_name, priority DESC, created_at) WHERE status = 'queued'.
    S7: Historical daily partitions before the bulk copy — CREATE TABLE IF NOT EXISTS sqlery_queued_job_YYYYMMDD PARTITION OF sqlery_queued_job FOR VALUES FROM (...) TO (...) for a 30-day lookback window (matches Django 0030 S7 range).
    S8: INSERT INTO sqlery_queued_job SELECT * FROM sqlery_queued_job_legacy ON CONFLICT DO NOTHING.
    S9: Seed sqlery_job_id_seq past max(id): SELECT setval('sqlery_job_id_seq', GREATEST(max(id), 1)) FROM sqlery_queued_job.

    The upgrade() uses op.get_bind().execute(text(...)) for each step. op.get_context().dialect.name for vendor check. Keep atomic=False at migration level (same as Django 0030 — stop-the-world, manual transaction control). Add a top-level module docstring with the same operational protocol note as Django 0030 (stop workers, run migration, restart).

    downgrade() function: vendor-guarded. On PG, performs the rollback (creates unpartitioned copy, copies rows back, renames) — same logic as Django 0030 backward. Keeps legacy table (rename-based rollback).

    Do NOT entangle with the pre-existing 20250101_0002 chain collision (that is a separate carry-forward item). This revision's down_revision = '20260608_0015' is independent.

    Import: from alembic import op; import sqlalchemy as sa; from sqlalchemy import text; from datetime import datetime, timedelta, timezone.
  </action>
  <verify>
    <automated>python -c "
import importlib.util, pathlib
spec = importlib.util.spec_from_file_location('rev', 'alembic/versions/20260612_0016_partition_queued_job.py')
mod = importlib.util.load_from_spec(spec)
spec.loader.exec_module(mod)
assert mod.revision == '20260612_0016'
assert mod.down_revision == '20260608_0015'
print('Alembic revision metadata OK')
"</automated>
  </verify>
  <done>
    Revision file exists with correct revision/down_revision. upgrade() is vendor-guarded PG-only and idempotent (DO blocks + IF NOT EXISTS). downgrade() provides rename-based rollback. Docstring documents stop-the-world operational protocol.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database_url input → DDL | URL passed to init_database determines DDL path (PG vs SQLite) |
| Alembic migration → DB | Migration executes DDL with elevated privileges; vendor-guarded |
| S8 INSERT → data | Bulk copy from legacy; ON CONFLICT DO NOTHING prevents duplicates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | init_database DDL | mitigate | PG DDL uses static SQL with no user-controlled interpolation; table names are hard-coded constants from tables.py |
| T-17-05 | Denial of Service | S8 bulk INSERT | accept | Stop-the-world migration; operator protocol documented in docstring; < 1 min / 1M rows on modern hardware |
| T-17-06 | Elevation of Privilege | Alembic migration | mitigate | Vendor guard returns immediately on non-PG; no-op on SQLite (D6) prevents accidental DDL on wrong DB |
| T-17-07 | Information Disclosure | Legacy table retention | accept | sqlery_queued_job_legacy retained for rollback; contains only job data already accessible to the DB user; DROP deferred (same policy as Django D3) |
| T-17-SC | Tampering | pip installs | accept | No new packages; alembic already in lockfile |

</threat_model>

<verification>
python -c "import alembic.versions.20260612_0016_partition_queued_job as r; assert r.revision == '20260612_0016'; assert r.down_revision == '20260608_0015'; print('OK')"
SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test python -m pytest tests/test_standalone_fresh_install.py -x -v
</verification>

<success_criteria>
- init_database() on PG creates relkind='p' partitioned table (verified by pg_class query)
- init_database() on SQLite creates plain table without error (D6 unchanged)
- Alembic revision 20260612_0016 has correct revision chain (down_revision = '20260608_0015')
- Alembic revision is vendor-guarded: on SQLite upgrade() is a no-op
- Fresh install smoke test: after init_database(PG_URL), SELECT FROM sqlery_queued_job succeeds and the table has a DEFAULT partition
</success_criteria>

<output>
Create `.planning/phases/17-fastapi-parity/17-02-SUMMARY.md` when done
</output>
