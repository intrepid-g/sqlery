---
phase: 17-fastapi-parity
plan: "04"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
  - "17-03"
files_modified:
  - tests/test_standalone_lifecycle_partitioned.py
  - tests/test_standalone_divergence_matrix.py
autonomous: true
requirements:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6

must_haves:
  truths:
    - "SC-1: Full claim → run → complete → reclaim lifecycle test passes against SQLAlchemyBackend on partitioned PG"
    - "SC-1 async: The same lifecycle test passes against SQLAlchemyAsyncBackend on PG via psycopg async URL"
    - "SC-2: Fresh install via init_database(PG_URL) verified in an isolated test DB — relkind='p' in pg_class"
    - "R1: EXPLAIN on claim_job shows partial-index (sqlery_job_pending_idx) used on PG"
    - "R2: SQLite cleanup_jobs batched DELETE still terminates without unbounded lock"
    - "R3: cleanup_jobs on partitioned PG routes to reclaim and returns reclaimed_via_partition_drop: True"
    - "R4: back-pressure invariant verified by the standalone backend — cleanup skips today's partition"
    - "R5: staging round-trip: create_job far-future → ScheduledJobStaging; get_job_by_id finds it; cancel_job deletes it"
    - "R6: single-partition pruning: EXPLAIN on UPDATE in mark_job_archived and cascade_ancestor_status shows no Append node"
    - "Standalone SQLite × PG divergence matrix green (cleanup_jobs, create_job far-future routing, _partitioned_pg)"
  artifacts:
    - path: "tests/test_standalone_lifecycle_partitioned.py"
      provides: "SC-1 (sync + async) lifecycle test + SC-2 fresh install test against SQLAlchemyBackend"
      contains: "reclaimed_via_partition_drop"
    - path: "tests/test_standalone_divergence_matrix.py"
      provides: "Standalone SQLite × PG divergence matrix covering all R1–R6 items for both backends"
      contains: "TestStandaloneDivergenceMatrixSQLite"
  key_links:
    - from: "tests/test_standalone_lifecycle_partitioned.py"
      to: "SQLAlchemyBackend.cleanup_jobs → reclaim_drained_partitions"
      via: "partition DROP after all jobs complete"
      pattern: "reclaimed_via_partition_drop"
    - from: "tests/test_standalone_divergence_matrix.py"
      to: "SQLAlchemyBackend + SQLAlchemyAsyncBackend"
      via: "PG URL fixture + SQLite fixture"
      pattern: "TestStandaloneDivergenceMatrixPG"
---

<objective>
Phase 17 verification suite: standalone lifecycle test (SC-1, sync + async), fresh-install
test (SC-2), and the standalone SQLite × PG divergence matrix re-checking R1–R6 under
both SQLAlchemyBackend and SQLAlchemyAsyncBackend.

Purpose: These tests are the acceptance bar for Phase 17's two success criteria plus the
R1–R6 re-verification requirement. They are the standalone counterpart to Phase 16's
test_lifecycle_partitioned.py and test_divergence_matrix.py.

Output: Two test files. All PG tests skip cleanly when SQLERY_TEST_PG_URL is unset.
SQLite tests always run. Async tests use psycopg async URL (NOT aiosqlite — no new deps).
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-fastapi-parity/17-CONTEXT.md
@.planning/phases/17-fastapi-parity/17-01-SUMMARY.md
@.planning/phases/17-fastapi-parity/17-02-SUMMARY.md
@.planning/phases/17-fastapi-parity/17-03-SUMMARY.md
@tests/test_lifecycle_partitioned.py
@tests/test_divergence_matrix.py
@src/sqlery/fastapi_sqlery/backend.py
@src/sqlery/fastapi_sqlery/async_backend.py
@src/sqlery/fastapi_sqlery/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone lifecycle test + fresh-install test (SC-1 + SC-2)</name>
  <files>tests/test_standalone_lifecycle_partitioned.py</files>
  <action>
    Create tests/test_standalone_lifecycle_partitioned.py. Mirror the structure of
    tests/test_lifecycle_partitioned.py (Phase 16) but targeting SQLAlchemyBackend and
    SQLAlchemyAsyncBackend instead of DjangoBackend. All tests skip when SQLERY_TEST_PG_URL
    is unset.

    Module-level skip guard: pytestmark = pytest.mark.skipif(not os.environ.get('SQLERY_TEST_PG_URL'), reason="SQLERY_TEST_PG_URL not set — PG required").

    Helper _make_pg_backend() → SQLAlchemyBackend:
    - Reset global _engine via a private reset helper (add a reset_engine() function to database.py that sets _engine = None; or call database._engine = None directly).
    - Call init_database(os.environ['SQLERY_TEST_PG_URL']).
    - Return SQLAlchemyBackend().

    Helper _make_pg_async_backend() → SQLAlchemyAsyncBackend:
    - Call reset_async_engine() from database.py (already exists).
    - Call get_async_session_factory(os.environ['SQLERY_TEST_PG_URL']) to prime the async engine.
    - Return SQLAlchemyAsyncBackend().

    Helper _create_basic_job(backend) — same shape as Phase 16 helper.

    class TestStandaloneLifecycle:
      def test_table_is_partitioned_on_pg(self):
        - backend = _make_pg_backend()
        - assert backend._partitioned_pg() is True

      def test_fresh_install_creates_partitioned_table(self):
        - SC-2 acceptance criterion.
        - Get engine via get_engine().
        - Execute: SELECT relkind FROM pg_class WHERE relname = 'sqlery_queued_job'
        - assert row[0] == 'p' (partitioned)

      def test_create_job_lands_in_today_partition(self):
        - Create job via backend.create_job(...).
        - Query: SELECT tableoid::regclass FROM sqlery_queued_job WHERE id = :id
        - Assert result does NOT contain 'default' (not in DEFAULT partition)
        - Assert result matches pattern sqlery_queued_job_YYYYMMDD

      def test_claim_run_complete_reclaim(self):
        - SC-1 core test (sync).
        - create_job → claim_job → (assert status running) → mark_job_success → cleanup_jobs
        - Assert cleanup_jobs result["reclaimed_via_partition_drop"] is True or dropped_partitions >= 0
        - Note: reclaim may not drop today's partition (back-pressure: active partition skipped); assert the routing happened (reclaimed_via_partition_drop key present)

      def test_cleanup_returns_reclaimed_via_partition_drop_true(self):
        - Standalone assertion on routing key.
        - cleanup_jobs(status="success") result must have "reclaimed_via_partition_drop" key.

      def test_mark_job_success_uses_created_at_filter(self):
        - EXPLAIN-verify mark_job_success UPDATE uses created_at in predicate.
        - Create job, get its created_at. EXPLAIN (ANALYZE FALSE, FORMAT TEXT) the UPDATE for mark_success.
        - Assert no "Append" in plan_text (single-partition pruning — same assertion strategy as Phase 16's EXPLAIN tests).

    class TestStandaloneLifecycleAsync:
      """Async lifecycle tests using psycopg async URL (NOT aiosqlite — no new deps).

      All tests use SQLERY_TEST_PG_URL translated to postgresql+psycopg:// async form.
      SQLite async is NOT tested here (aiosqlite not installed — pre-existing env gap per
      planning context; skip with a note rather than adding a dependency).
      """

      def test_aclaim_job_on_partitioned_pg(self):
        - backend = _make_pg_async_backend()
        - Run asyncio.run(self._run_claim_test(backend))
        - Inner coroutine: aclaim_job; assert returned job status == "running"

      def test_amark_success_on_partitioned_pg(self):
        - Create job via sync backend, then async-mark it success, verify via sync get_job_by_id.

      def test_async_cleanup_routes_to_reclaim(self):
        - If SQLAlchemyAsyncBackend has acleanup_jobs: call it; assert routing key.
        - If not implemented (async cleanup not in scope): skip with pytest.skip("acleanup_jobs not implemented").

    Tests use import pytest, asyncio. asyncio.run() for async test bodies (no pytest-asyncio needed).
    All PG URLs use SQLERY_TEST_PG_URL directly. Use try/finally or pytest fixtures for cleanup.

    EXPLAIN pruning assertion helper _has_single_partition(plan_text) — same logic as Phase 16:
    "Append" not in plan_text AND count of distinct sqlery_queued_job_YYYYMMDD|default children == 1.
  </action>
  <verify>
    <automated>SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test python -m pytest tests/test_standalone_lifecycle_partitioned.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_table_is_partitioned_on_pg passes. test_fresh_install_creates_partitioned_table (SC-2) passes. test_claim_run_complete_reclaim (SC-1 sync) passes with reclaimed_via_partition_drop key in result. test_aclaim_job_on_partitioned_pg (SC-1 async) passes. All tests skip cleanly without SQLERY_TEST_PG_URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Standalone SQLite × PG divergence matrix (R1–R6 re-verification)</name>
  <files>tests/test_standalone_divergence_matrix.py</files>
  <action>
    Create tests/test_standalone_divergence_matrix.py. Mirror tests/test_divergence_matrix.py
    (Phase 16) but targeting SQLAlchemyBackend and SQLAlchemyAsyncBackend.

    Module docstring documenting the documented divergences (same D6 items as Phase 16):
    - D6: cleanup_jobs: SQLite → {"deleted": N}; PG partitioned → {"reclaimed_via_partition_drop": True}
    - D6: create_job far-future: SQLite → QueuedJob; PG partitioned → ScheduledJobStaging
    - D8: _partitioned_pg(): SQLite → False; PG fresh install → True

    Helper _make_sqlite_backend() → SQLAlchemyBackend:
    - Reset global _engine (set database._engine = None).
    - init_database("sqlite:///./test_standalone_matrix.db") — use a temp file in /tmp or the working dir.
    - Return SQLAlchemyBackend().

    class TestStandaloneDivergenceMatrixSQLite:
      """Always runs — no PG URL required."""

      def test_cleanup_jobs_returns_deleted_dict_on_sqlite(self):
        - cleanup_jobs(status="success") result has "deleted" key (not "reclaimed_via_partition_drop").
        - R2: batched DELETE path runs on SQLite.

      def test_partitioned_pg_returns_false_on_sqlite(self):
        - backend._partitioned_pg() is False (D6).

      def test_create_job_far_future_stays_in_queued_job_on_sqlite(self):
        - Create job with scheduled_at = now + 60 days.
        - Assert returned object is QueuedJob (not ScheduledJobStaging).
        - R5: far-future routing gated on _partitioned_pg().

      def test_get_staged_jobs_returns_empty_on_sqlite(self):
        - backend.get_staged_jobs() returns [].

      def test_get_raw_cursor_returns_none_on_sqlite(self):
        - backend.get_raw_cursor() is None.

      def test_claim_job_uses_optimistic_cas_on_sqlite(self):
        - Create and claim a job. Assert returned job.status == "running".
        - R1: claim path works on SQLite.

      def test_vacuum_runs_on_sqlite(self):
        - vacuum_database() returns dict without error (SQLite VACUUM works).

    class TestStandaloneDivergenceMatrixPG:
      """Skips when SQLERY_TEST_PG_URL is unset."""

      pytestmark = pytest.mark.skipif(not os.environ.get('SQLERY_TEST_PG_URL'), reason="PG required")

      def test_cleanup_jobs_routes_to_reclaim_on_partitioned_pg(self):
        - backend = _make_pg_backend()
        - result = backend.cleanup_jobs()
        - assert "reclaimed_via_partition_drop" in result (R3).

      def test_partitioned_pg_returns_true_on_fresh_pg_install(self):
        - backend._partitioned_pg() is True (D8).

      def test_create_job_far_future_routes_to_staging_on_pg(self):
        - Create job scheduled 60 days out.
        - from sqlery.core.models import ScheduledJobStaging
        - assert isinstance(returned_job, ScheduledJobStaging) (R5).

      def test_get_staged_jobs_returns_list_on_pg(self):
        - backend.get_staged_jobs() returns a list (may be empty if no staged rows).
        - R5: staging surface present.

      def test_get_raw_cursor_returns_cursor_on_partitioned_pg(self):
        - cur = backend.get_raw_cursor(); assert cur is not None (R3 daemon integration).
        - Close cursor and connection: cur.close(); cur.connection.close().

      def test_claim_uses_skip_locked_on_pg(self):
        - Create and claim a job. Assert returned job.status == "running" (R1).

      def test_explain_cleanup_routes_explain_on_pg(self):
        - R3: verify cleanup routing is active (returns reclaim key, not deleted key).

      def test_back_pressure_invariant_not_dropped_today(self):
        - R4: create a job for today; run cleanup_jobs; verify today's partition still exists.
        - Check pg_class: SELECT relname FROM pg_class WHERE relname LIKE 'sqlery_queued_job_%' AND relname != 'sqlery_queued_job_default'.
        - Today's partition must still be present after cleanup.

      def test_r6_write_path_pruning_mark_archived(self):
        - Create a failed job. Run EXPLAIN on mark_job_archived UPDATE.
        - R6: assert no Append node (single-partition pruning, same helper as lifecycle test).

    For SQLite cleanup in tests: use a pytest fixture with tmp_path or explicit teardown.
    For PG tests: wrap each test in a transaction or truncate tables in a fixture.

    Run command: SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test python -m pytest tests/test_standalone_divergence_matrix.py -x -v
  </action>
  <verify>
    <automated>python -m pytest tests/test_standalone_divergence_matrix.py::TestStandaloneDivergenceMatrixSQLite -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestStandaloneDivergenceMatrixSQLite (7 tests) always passes — no PG URL needed. TestStandaloneDivergenceMatrixPG (8 tests) passes when SQLERY_TEST_PG_URL is set; skips cleanly otherwise. R1–R6 each have at least one test assertion in either the lifecycle test file or this divergence matrix.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test SQLERY_TEST_PG_URL → PG test DB | Tests connect to the shared CI test database; writes must be isolated or cleaned up |
| EXPLAIN output → assertion | EXPLAIN text is read-only; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Tampering | test DB pollution | mitigate | Tests use explicit teardown or truncate fixtures; PG tests create jobs in isolated queue names to reduce cross-test contamination |
| T-17-14 | Denial of Service | lifecycle test on partitioned PG | accept | Tests run once per CI cycle; partitioned table with fresh data is fast; no production data at risk |
| T-17-SC | Tampering | pip installs | accept | No new packages; pytest and psycopg already in lockfile |

</threat_model>

<verification>
python -m pytest tests/test_standalone_divergence_matrix.py::TestStandaloneDivergenceMatrixSQLite -v
SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test python -m pytest tests/test_standalone_lifecycle_partitioned.py tests/test_standalone_divergence_matrix.py -v --tb=short 2>&1 | tail -30
</verification>

<success_criteria>
- SC-1 (sync): test_claim_run_complete_reclaim passes on SQLERY_TEST_PG_URL — reclaimed_via_partition_drop key present
- SC-1 (async): test_aclaim_job_on_partitioned_pg passes using psycopg async URL
- SC-2: test_fresh_install_creates_partitioned_table passes — relkind='p' confirmed
- R1–R6 each have at least one passing test assertion between the two test files
- SQLite tests (TestStandaloneDivergenceMatrixSQLite, 7 tests) always pass with no PG URL
- All PG tests skip cleanly when SQLERY_TEST_PG_URL is unset
</success_criteria>

<output>
Create `.planning/phases/17-fastapi-parity/17-04-SUMMARY.md` when done
</output>
