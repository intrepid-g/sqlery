---
phase: 17-fastapi-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/core/models.py
  - src/sqlery/fastapi_sqlery/config.py
autonomous: true
requirements:
  - R1
  - R2
  - R5
  - R6

must_haves:
  truths:
    - "QueuedJob has composite PK (created_at, id); id draws from sqlery_job_id_seq (not per-table identity)"
    - "ScheduledJobStaging SQLModel exists in core/models.py, matching Django ScheduledJob schema"
    - "StandaloneConfig has all six partition keys with the same fixed defaults as Django mode"
    - "StandaloneConfig raises ValueError when SQLERY_PARTITION_RETENTION <= SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS"
    - "StandaloneConfig raises ValueError when PARTITION_MAINTENANCE_INTERVAL_MINUTES > SQLERY_PARTITION_INTERVAL hours"
  artifacts:
    - path: "src/sqlery/core/models.py"
      provides: "QueuedJob composite-PK + ScheduledJobStaging SQLModel"
      contains: "primary_key=True"
    - path: "src/sqlery/fastapi_sqlery/config.py"
      provides: "Six partition config keys + validation method"
      contains: "SQLERY_PARTITION_RETENTION"
  key_links:
    - from: "src/sqlery/core/models.py QueuedJob"
      to: "src/sqlery/fastapi_sqlery/database.py"
      via: "SQLModel.metadata used by create_all and partitioned DDL"
      pattern: "primary_key=True"
    - from: "src/sqlery/fastapi_sqlery/config.py"
      to: "src/sqlery/fastapi_sqlery/backend.py"
      via: "get_config().get('SQLERY_PARTITION_RETENTION')"
      pattern: "SQLERY_PARTITION_RETENTION"
---

<objective>
Bring core/models.py QueuedJob to composite-PK parity with the Django model and add the
ScheduledJobStaging SQLModel; then wire all partition config keys into StandaloneConfig.

Purpose: Every subsequent plan in Phase 17 depends on the model schema and config contract
established here. The composite PK is required for the Alembic cutover revision (Plan 02) and
for database.py to emit correct partitioned DDL (Plan 02). The config keys are required by
backend.py cleanup routing and the staging threshold gate (Plan 03).

Output: Updated core/models.py (composite PK, ScheduledJobStaging), updated config.py
(six new keys + two validation invariants).
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-fastapi-parity/17-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/sqlery/core/models.py
@src/sqlery/fastapi_sqlery/config.py
@src/sqlery/django_sqlery/models.py
@.planning/phases/16-backend-wiring-pruning/16-01-SUMMARY.md
@.planning/phases/15-schema-cutover/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QueuedJob composite PK + ScheduledJobStaging SQLModel in core/models.py</name>
  <files>src/sqlery/core/models.py</files>
  <behavior>
    - Test 1: QueuedJob has both created_at and id declared primary_key=True (composite PK)
    - Test 2: id Field uses sa_column with BigInteger, no autoincrement identity; server_default="nextval('sqlery_job_id_seq')" for PG-aware installs (on SQLite the sequence default is unused)
    - Test 3: ScheduledJobStaging table=True, __tablename__="sqlery_scheduled_job", fields: id BigInteger PK, queue_name, task_path, payload JSON, scheduled_at, priority, max_retries, created_at
    - Test 4: Existing JobRegistry.job_id foreign_key="sqlery_queued_job.id" stays unchanged (FK is on the id column only; the FK demoted-to-BigInt pattern from Phase 15 applies)
    - Test 5: Worker.current_job_id foreign_key="sqlery_queued_job.id" stays unchanged
    - Test 6: QueuedJob.__table_args__ or composite_pk approach: SQLModel composite PK is declared by setting primary_key=True on both fields; also add __table_args__ with UniqueConstraint on (created_at, id) if required by SQLModel
  </behavior>
  <action>
    In core/models.py, modify QueuedJob's PK section (per D8/D15-SHARED-ID-SEQUENCE):

    Comment out the existing single-PK id field and add two fields beneath it:
    - id: int | None annotated with Field(sa_column=Column(BigInteger, primary_key=True, server_default=text("nextval('sqlery_job_id_seq')"), nullable=False)). Import BigInteger from sqlalchemy. Import text from sqlalchemy. The server_default is inert on SQLite (sequences don't exist); on PG the Alembic cutover migration creates the sequence first (Plan 02).
    - created_at: already declared lower in the class with a default_factory. Move or re-declare it also as primary_key=True. The cleanest SQLModel pattern: add primary_key=True to the existing created_at Field. Because SQLModel processes field declarations in order, both fields declared primary_key=True form the composite PK — emit them together at the top of the field list (id first, then created_at) to mirror the Django model column order.

    Important: SQLModel's composite-PK support requires adding __table_args__ = (PrimaryKeyConstraint("id", "created_at"),) and removing primary_key=True from the individual Field declarations to avoid a duplicate-PK conflict, OR relying solely on primary_key=True on both fields (test which approach SQLModel 0.0.14+ supports without raising). Use the __table_args__ PrimaryKeyConstraint approach if SQLModel raises on dual primary_key=True fields at import time — that is the safer path. Import PrimaryKeyConstraint from sqlalchemy.

    Also comment out (not delete per convention) the unique=True on job_name if present, and add a note: job_name uniqueness enforced at application level (same as Django backend — partitioned tables cannot have global unique constraints).

    After modifying QueuedJob, add ScheduledJobStaging as a new SQLModel class at the bottom of models.py (before the closing):
    - __tablename__ = "sqlery_scheduled_job"
    - id: int = Field(sa_column=Column(BigInteger, primary_key=True, server_default=text("nextval('sqlery_job_id_seq')"), nullable=False))
    - queue_name: str = Field(default="default", max_length=50)
    - task_path: str = Field(max_length=500)
    - payload: dict = Field(default_factory=dict, sa_column=Column(JSON))
    - scheduled_at: datetime (no default — required)
    - priority: int = Field(default=0)
    - max_retries: int = Field(default=0)
    - created_at: datetime = Field(default_factory=lambda: datetime.now(UTC))
    This mirrors Django's ScheduledJob model exactly. No Relationship fields needed (standalone mode does not traverse FKs from staging).

    Do NOT change FK field declarations on Worker.current_job_id or JobRegistry.job_id — they reference the id column by name; the FK is on the scalar id column (BigInteger), not the composite PK pair. Demoted FKs from Phase 15 still reference sqlery_queued_job.id by column name.

    Convention (CLAUDE.md): comment out wrong lines, place corrections immediately beneath. All imports remain at top of file.
  </action>
  <verify>
    <automated>python -c "from sqlery.core.models import QueuedJob, ScheduledJobStaging; from sqlalchemy import inspect as sa_inspect; pk_cols = [c.name for c in sa_inspect(QueuedJob).primary_key]; assert set(pk_cols) == {'id','created_at'}, pk_cols; print('composite PK OK')"</automated>
  </verify>
  <done>
    QueuedJob has composite PK (created_at, id) importable without error. ScheduledJobStaging imports cleanly. SQLite-only import (no PG connection) succeeds. Existing FKs (worker_id, JobRegistry.job_id) compile without error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Partition config keys + validation in StandaloneConfig</name>
  <files>src/sqlery/fastapi_sqlery/config.py</files>
  <behavior>
    - Test 1: StandaloneConfig().get('SQLERY_PARTITION_INTERVAL') == '1 day' (default)
    - Test 2: StandaloneConfig().get('SQLERY_PARTITION_PREMAKE') == 7 (default)
    - Test 3: StandaloneConfig().get('SQLERY_PARTITION_RETENTION') == '30 days' (default)
    - Test 4: StandaloneConfig().get('SQLERY_PARTITION_ARCHIVE_HOOK') is None (default)
    - Test 5: StandaloneConfig().get('PARTITION_MAINTENANCE_INTERVAL_MINUTES') == 60 (default)
    - Test 6: StandaloneConfig().get('SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS') == 1 (default)
    - Test 7: validate_partition_config() raises ValueError when retention_days <= threshold_days
    - Test 8: validate_partition_config() raises ValueError when maintenance_interval_minutes > interval_hours * 60
    - Test 9: SQLERY_PARTITION_RETENTION env var sets the config value; SQLERY_PARTITION_PREMAKE env var parses as int
  </behavior>
  <action>
    In config.py StandaloneConfig.__init__ _config dict, add six new keys beneath the existing retention section (per D1 — same defaults as Django mode, from django_sqlery/settings.py DEFAULTS):

    'SQLERY_PARTITION_INTERVAL': '1 day',
    'SQLERY_PARTITION_PREMAKE': 7,
    'SQLERY_PARTITION_RETENTION': '30 days',
    'SQLERY_PARTITION_ARCHIVE_HOOK': None,
    'PARTITION_MAINTENANCE_INTERVAL_MINUTES': 60,
    'SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS': 1,

    In _load_from_env, add env var mappings for:
    SQLERY_PARTITION_INTERVAL -> SQLERY_PARTITION_INTERVAL (str, no conversion)
    SQLERY_PARTITION_PREMAKE -> SQLERY_PARTITION_PREMAKE (int)
    SQLERY_PARTITION_RETENTION -> SQLERY_PARTITION_RETENTION (str, no conversion)
    SQLERY_PARTITION_MAINTENANCE_INTERVAL_MINUTES -> PARTITION_MAINTENANCE_INTERVAL_MINUTES (int)
    SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS -> SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS (int)
    SQLERY_PARTITION_ARCHIVE_HOOK -> SQLERY_PARTITION_ARCHIVE_HOOK (str, load as dotted path string; None if absent)

    Add a new method validate_partition_config(self) -> None that enforces the two invariants:
    1. Parse retention_days from SQLERY_PARTITION_RETENTION: split on space, first token is int. Parse threshold_days from SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS. If retention_days <= threshold_days: raise ValueError("SQLERY_PARTITION_RETENTION must be > SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS").
    2. Parse interval_hours: split SQLERY_PARTITION_INTERVAL on space, convert "N day(s)" to hours (N * 24). maintenance_mins = PARTITION_MAINTENANCE_INTERVAL_MINUTES. If maintenance_mins > interval_hours * 60: raise ValueError("PARTITION_MAINTENANCE_INTERVAL_MINUTES must be <= partition interval").
    The method mirrors Django's validate_settings() in settings.py. Keep it pure (same input → same output, easy to test).

    Convention: comment out any old lines before adding replacements. Add a comment referencing D1 at the config key block.
  </action>
  <verify>
    <automated>python -c "
from sqlery.fastapi_sqlery.config import StandaloneConfig
c = StandaloneConfig()
assert c.get('SQLERY_PARTITION_INTERVAL') == '1 day', c.get('SQLERY_PARTITION_INTERVAL')
assert c.get('SQLERY_PARTITION_PREMAKE') == 7
assert c.get('SQLERY_PARTITION_RETENTION') == '30 days'
assert c.get('SQLERY_SCHEDULED_JOB_THRESHOLD_DAYS') == 1
c.validate_partition_config()  # should not raise with defaults
print('config keys OK')
"</automated>
  </verify>
  <done>
    All six partition config keys present with correct defaults. validate_partition_config() raises ValueError on retention <= threshold and maintenance > interval. Env var loading works for SQLERY_PARTITION_PREMAKE (int) and SQLERY_PARTITION_RETENTION (str).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → config | SQLERY_PARTITION_RETENTION / INTERVAL are operator-supplied strings parsed inline |
| SQLModel metadata → DDL | composite PK declaration drives CREATE TABLE shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | models.py composite PK | mitigate | Both id and created_at declared primary_key=True; PrimaryKeyConstraint enforced at DB level; no application-layer bypass |
| T-17-02 | Information Disclosure | config env vars | accept | Partition config values are operational settings (retention duration, interval), not secrets; no PII exposure |
| T-17-03 | Denial of Service | validate_partition_config | mitigate | Validate at startup (called by init_database in Plan 02); invalid config raises ValueError before any DDL runs |
| T-17-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan; existing sqlmodel/sqlalchemy deps already in lockfile |

</threat_model>

<verification>
python -c "from sqlery.core.models import QueuedJob, ScheduledJobStaging; print('models import OK')"
python -c "from sqlery.fastapi_sqlery.config import StandaloneConfig; c = StandaloneConfig(); c.validate_partition_config(); print('config OK')"
</verification>

<success_criteria>
- QueuedJob composite PK (created_at, id) verified by sqlalchemy inspect
- ScheduledJobStaging importable with correct __tablename__ = "sqlery_scheduled_job"
- StandaloneConfig has all 6 partition keys with documented defaults (D1)
- validate_partition_config() raises on retention <= threshold; raises on maintenance > interval
</success_criteria>

<output>
Create `.planning/phases/17-fastapi-parity/17-01-SUMMARY.md` when done
</output>
