---
phase: 16-backend-wiring-pruning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sqlery/django_sqlery/migrations/0031_secondary_indexes.py
  - src/sqlery/django_sqlery/backend.py
  - src/sqlery/django_sqlery/async_backend.py
autonomous: true
requirements:
  - R3
  - R6
  - R9
  - R10

must_haves:
  truths:
    - "_partitioned_pg() returns True on PG when table is partitioned, False on SQLite"
    - "get_raw_cursor() returns a raw psycopg cursor on PG, None on SQLite (no AttributeError)"
    - "create_job routes far-future jobs to ScheduledJob only when _partitioned_pg() is True"
    - "Migration 0031 exists and creates the 5 secondary indexes on the partitioned table under PG, is a no-op on SQLite"
  artifacts:
    - path: "src/sqlery/django_sqlery/migrations/0031_secondary_indexes.py"
      provides: "PG-only secondary indexes on partitioned sqlery_queued_job (IF NOT EXISTS, vendor-guarded)"
    - path: "src/sqlery/django_sqlery/backend.py"
      provides: "_partitioned_pg() helper method and get_raw_cursor() method"
      contains: "def _partitioned_pg"
    - path: "src/sqlery/django_sqlery/async_backend.py"
      provides: "async get_raw_cursor mirror (returns None — async backends don't run maintenance ticks)"
      contains: "def get_raw_cursor"
  key_links:
    - from: "src/sqlery/core/daemon.py"
      to: "backend.get_raw_cursor()"
      via: "daemon partition maintenance tick"
      pattern: "get_raw_cursor"
    - from: "src/sqlery/django_sqlery/backend.py create_job"
      to: "_partitioned_pg()"
      via: "staging threshold guard"
      pattern: "_partitioned_pg"
---

<objective>
Lay the foundation for Phase 16 wiring: add migration 0031 (secondary indexes on the
partitioned sqlery_queued_job), add the _partitioned_pg() helper and get_raw_cursor()
method to DjangoBackend, mirror get_raw_cursor() on DjangoAsyncBackend, and gate the
create_job staging threshold routing on _partitioned_pg() so SQLite keeps far-future
jobs in sqlery_queued_job (D6 — SQLite byte-for-byte unchanged per R10).

Purpose: Unblocks wave-2 cleanup/vacuum routing and daemon promotion tick wiring (Plan
16-03), provides migration 0031 that the EXPLAIN tests in Plan 16-04 depend on, and
resolves the PHASE 16 CARRY-FORWARD from STATE.md regarding get_raw_cursor() and the
SQLite staging resolution.

Output: migration 0031, _partitioned_pg(), get_raw_cursor() on both backends, SQLite
staging gate in create_job.
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-backend-wiring-pruning/16-CONTEXT.md
@src/sqlery/django_sqlery/backend.py
@src/sqlery/django_sqlery/async_backend.py
@src/sqlery/django_sqlery/models.py
@src/sqlery/django_sqlery/db_compat.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0031 — secondary indexes on partitioned sqlery_queued_job</name>
  <files>src/sqlery/django_sqlery/migrations/0031_secondary_indexes.py</files>
  <behavior>
    - Migration is a no-op on SQLite (vendor-guarded with `if schema_editor.connection.vendor == "postgresql"`)
    - Each of the 5 index DDL statements uses IF NOT EXISTS so re-runs are safe
    - Indexes created: sqlery_job_task_status_idx (task_path, status), sqlery_job_created_desc (created_at DESC), sqlery_job_finished_desc (finished_at DESC), sqlery_job_started_desc (started_at DESC), sqlery_job_job_name_idx (job_name) — matching model Meta.indexes exactly
    - Migration depends on "0030_partition_queued_job"
    - state_operations uses AlterField / AddIndex ORM operations for each index so Django migration state stays in sync
    - PARTIAL pending index sqlery_job_pending_idx was already created in 0030; this migration adds only the non-partial secondary indexes
  </behavior>
  <read_first>
    src/sqlery/django_sqlery/models.py QueuedJob.Meta.indexes (lines 610-621) — extract exact field lists and names for each index
    src/sqlery/django_sqlery/migrations/0030_partition_queued_job.py — confirm sqlery_job_pending_idx was already re-created there; confirm LIKE INCLUDING DEFAULTS did NOT copy secondary indexes
  </read_first>
  <action>
    Create src/sqlery/django_sqlery/migrations/0031_secondary_indexes.py as a Django migration class.

    dependencies = [("sqlery", "0030_partition_queued_job")]

    The migration class name should be Migration. Set atomic = False (DDL on partitioned table).

    In database_forwards: guard the entire body with `if schema_editor.connection.vendor != "postgresql": return`.

    Use schema_editor.execute() for each index DDL. Each statement must use CREATE INDEX IF NOT EXISTS.
    The index definitions must match QueuedJob.Meta.indexes exactly (function names, field orders).

    Index 1 — task/status: `CREATE INDEX IF NOT EXISTS "sqlery_queued_job_task_path_status_idx" ON "sqlery_queued_job" ("task_path", "status");`
    Index 2 — created_at DESC: `CREATE INDEX IF NOT EXISTS "sqlery_job_created_desc" ON "sqlery_queued_job" ("created_at" DESC);`
    Index 3 — finished_at DESC: `CREATE INDEX IF NOT EXISTS "sqlery_job_finished_desc" ON "sqlery_queued_job" ("finished_at" DESC);`
    Index 4 — started_at DESC: `CREATE INDEX IF NOT EXISTS "sqlery_job_started_desc" ON "sqlery_queued_job" ("started_at" DESC);`
    Index 5 — job_name: `CREATE INDEX IF NOT EXISTS "sqlery_queued_job_job_name_idx" ON "sqlery_queued_job" ("job_name");`

    Note: the model Meta uses auto-named indexes for task_path+status; check the migration 0030 state_operations to confirm the exact auto-generated names and use matching names here. If 0030 already carries these in state, use AddIndex state_operations to keep ORM state aligned.

    In database_backwards: use DROP INDEX IF EXISTS for each.

    Convention (CLAUDE.md): do NOT delete old lines — comment them out before adding corrected versions. Here, this is a new file so the convention does not apply, but note it for completeness.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/unit/test_pruning_explain.py -x -q -k "test_migration_0031" 2>/dev/null || env -u VIRTUAL_ENV uv run pytest tests/ -x -q -k "0031" 2>/dev/null || echo "Migration tests will be added in Plan 16-04; verify manually for now"</automated>
    <human-check>
      Run `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run python -m django migrate --settings=tests.settings 2>&1 | tail -5` and confirm 0031 applied cleanly.
      Run `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run python -c "import django; django.setup(); from django.db import connection; cursor = connection.cursor(); cursor.execute(\"SELECT indexname FROM pg_indexes WHERE tablename='sqlery_queued_job' AND indexname NOT LIKE '%pkey'\"); print(cursor.fetchall())"` and confirm the 5 new index names appear in the output.
    </human-check>
  </verify>
  <done>
    Migration 0031 applies without error on PG. The five secondary index names appear in pg_indexes for sqlery_queued_job. SQLite: migration runs (vendor guard triggers no-op, no error).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _partitioned_pg() and get_raw_cursor() to DjangoBackend; mirror get_raw_cursor() on DjangoAsyncBackend; gate create_job staging on _partitioned_pg()</name>
  <files>
    src/sqlery/django_sqlery/backend.py
    src/sqlery/django_sqlery/async_backend.py
  </files>
  <behavior>
    - DjangoBackend._partitioned_pg() returns True if and only if connection.vendor == "postgresql" AND the jobs table is physically partitioned. Default: assume partitioned for any PG install (post Phase 15 cutover); implementation: check connection.vendor == "postgresql" (always true for PG after cutover). Do NOT query pg_inherits on every call — this is a hot path; vendor check alone is the correct heuristic (partitioned PG is the only supported PG configuration after Phase 15).
    - DjangoBackend.get_raw_cursor() returns a raw psycopg connection cursor on PG (via django.db.connection.connection.cursor() to get the underlying psycopg3 cursor); returns None on SQLite. No AttributeError; daemon's try/except AttributeError guard becomes unreachable (that is acceptable).
    - DjangoAsyncBackend.get_raw_cursor() returns None (async backend does not run the synchronous maintenance tick).
    - DjangoBackend.create_job: the staging threshold block (already present) gains an additional guard: only route to ScheduledJob when _partitioned_pg() is True. On SQLite (_partitioned_pg() is False), far-future jobs go into sqlery_queued_job exactly as they would have before Phase 14 (D6 — SQLite unchanged).
    - Comment out the existing staging condition line and add the updated condition below it (CLAUDE.md convention: never delete, always comment-out + add below).
  </behavior>
  <read_first>
    src/sqlery/django_sqlery/backend.py lines 86-121 (create_job staging threshold block) — understand current condition to add _partitioned_pg() guard
    src/sqlery/django_sqlery/db_compat.py is_postgresql() — reuse the same vendor check pattern
    src/sqlery/core/daemon.py lines 615-660 — confirm the get_raw_cursor() call sites and the AttributeError guard that will become unreachable
  </read_first>
  <action>
    In DjangoBackend (src/sqlery/django_sqlery/backend.py):

    Add _partitioned_pg() method immediately after __init__. It should check `connection.vendor == "postgresql"`. No pg_inherits query — vendor check alone (the table is always partitioned on PG post-cutover per Phase 15). Docstring: "Return True when the Django connection is PostgreSQL (table is always partitioned on PG after migration 0030). Used to gate cleanup routing and create_job staging."

    Add get_raw_cursor() method after _partitioned_pg(). On PG: return `connection.connection.cursor()` to obtain the underlying psycopg3 raw cursor (connection.connection is the psycopg Connection object; .cursor() returns a psycopg3 cursor compatible with core/partitioning.py). On SQLite: return None. Include docstring noting callers must use parameterized SQL only (security requirement — raw cursor must never be used with string-interpolated SQL).

    In create_job: locate the line `if scheduled_at is not None and scheduled_at > now_utc + staging_threshold:` (D3, Phase 14). Comment it out and add below:
    `if scheduled_at is not None and scheduled_at > now_utc + staging_threshold and self._partitioned_pg():`
    This gates staging routing on PG only, preserving SQLite behavior (D6, R10).

    In DjangoAsyncBackend (src/sqlery/django_sqlery/async_backend.py):

    Add get_raw_cursor() method that returns None, with docstring: "Async backend does not run synchronous partition maintenance ticks; returns None."

    Per CLAUDE.md: comment out any line being replaced; add the corrected version below. All imports must remain at module top level.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/ -x -q -k "_partitioned_pg or get_raw_cursor" 2>/dev/null; env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py -x -q 2>/dev/null</automated>
  </verify>
  <done>
    DjangoBackend._partitioned_pg() returns True on PG, False on SQLite. DjangoBackend.get_raw_cursor() returns a cursor on PG, None on SQLite. DjangoAsyncBackend.get_raw_cursor() returns None. create_job staging routing is gated on _partitioned_pg() — SQLite far-future jobs land in sqlery_queued_job, PG far-future jobs land in sqlery_scheduled_job. Existing staging unit tests still pass on SQLite (they assert ScheduledJob routing — those assertions must now be updated in Plan 16-04 to be PG-conditional, but for now the SQLite tests will fail; note in task output that test_staging.py will be updated in Plan 16-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DjangoBackend.get_raw_cursor() → partitioning.py | Raw psycopg cursor passed to framework-agnostic maintenance functions; callers must use parameterized SQL only |
| create_job staging routing | Far-future job insertion path; _partitioned_pg() gates which table receives the row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | get_raw_cursor() callers | mitigate | Document in method docstring: callers must use parameterized SQL only; core/partitioning.py already uses %s-parameterized psycopg calls exclusively |
| T-16-02 | Information Disclosure | connection.connection.cursor() | accept | Cursor exposes raw DB connection; only callable within the trusted backend process, no user-controlled input reaches it |
| T-16-03 | Denial of Service | _partitioned_pg() hot path | accept | Vendor check is O(1) attribute read; no DB round-trip, no lock |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
- `env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py -x -q` — all SC-1 routing tests pass (note: after Plan 16-04 updates, SQLite routing assertions will be PG-conditional)
- `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/ -x -q -k "partitioned_pg or raw_cursor or 0031"` — helper + migration tests pass on PG
- Migration 0031 applies cleanly on PG: `python manage.py migrate --settings=tests.settings`
</verification>

<success_criteria>
- migration/0031_secondary_indexes.py exists and is PG-only with IF NOT EXISTS guards
- DjangoBackend._partitioned_pg() exists and returns bool based on connection.vendor
- DjangoBackend.get_raw_cursor() exists; returns psycopg cursor on PG, None on SQLite; docstring notes parameterized-SQL-only requirement
- DjangoAsyncBackend.get_raw_cursor() exists and returns None
- create_job staging condition updated with _partitioned_pg() guard (old line commented out, new line below)
- SQLite: far-future job goes to QueuedJob, not ScheduledJob (D6 preserved)
- PG: far-future job still goes to ScheduledJob (staging routing unchanged)
</success_criteria>

<output>
Create .planning/phases/16-backend-wiring-pruning/16-01-SUMMARY.md when done.
</output>
