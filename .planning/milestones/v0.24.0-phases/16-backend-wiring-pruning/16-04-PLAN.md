---
phase: 16-backend-wiring-pruning
plan: "04"
type: execute
wave: 3
depends_on:
  - "16-01"
  - "16-02"
  - "16-03"
files_modified:
  - tests/unit/test_pruning_explain.py
  - tests/unit/test_staging.py
  - tests/test_lifecycle_partitioned.py
  - tests/test_divergence_matrix.py
autonomous: true
requirements:
  - R3
  - R6
  - R9
  - R10

must_haves:
  truths:
    - "EXPLAIN on each of the 11 write-path checklist items shows Partitions: 1 of N (single-partition pruning)"
    - "Full claim → run → complete → reclaim lifecycle test passes on a partitioned PG table"
    - "SQLite divergence matrix green — every backend method exercised under both vendors"
    - "Phase-14 staging tests updated: SQLite far-future jobs go to QueuedJob; PG far-future jobs go to ScheduledJob"
  artifacts:
    - path: "tests/unit/test_pruning_explain.py"
      provides: "11 EXPLAIN-verified partition pruning tests (one per checklist item), PG-only"
      contains: "Partitions: 1"
    - path: "tests/unit/test_staging.py"
      provides: "Updated routing assertions: SQLite = QueuedJob, PG = ScheduledJob (conditional per vendor)"
    - path: "tests/test_lifecycle_partitioned.py"
      provides: "Claim → run → success → reclaim full lifecycle test on partitioned PG table"
    - path: "tests/test_divergence_matrix.py"
      provides: "Every DjangoBackend method exercised under SQLite AND PG with behavior assertions"
  key_links:
    - from: "tests/unit/test_pruning_explain.py"
      to: "src/sqlery/django_sqlery/db_compat.py + models.py + backend.py"
      via: "Django EXPLAIN ANALYZE on each UPDATE/CAS"
      pattern: "Partitions: 1"
    - from: "tests/test_lifecycle_partitioned.py"
      to: "DjangoBackend.cleanup_jobs → reclaim_drained_partitions"
      via: "partition DROP after all jobs complete"
      pattern: "reclaim_drained_partitions"
---

<objective>
Verification suite for Phase 16: EXPLAIN pruning tests (one per each of the 11 checklist
items), the claim → run → complete → reclaim lifecycle test, the SQLite divergence matrix,
and the updated Phase-14 staging tests.

Purpose: These tests are the acceptance bar for all three Phase 16 success criteria:
(1) EXPLAIN shows single-partition pruning per write path; (2) full lifecycle on a
partitioned table; (3) SQLite divergence matrix green. The staging test updates are
required to correctly reflect the _partitioned_pg() gate added in Plan 16-01 (SQLite
far-future jobs now stay in QueuedJob).

Output: Four test files. All EXPLAIN tests require SQLERY_TEST_PG_URL. Divergence matrix
runs both with and without PG URL. Staging tests updated to be vendor-conditional.
</objective>

<execution_context>
@/Users/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-backend-wiring-pruning/16-CONTEXT.md
@tests/unit/test_staging.py
@tests/unit/test_partitioning.py
@src/sqlery/django_sqlery/db_compat.py
@src/sqlery/django_sqlery/models.py
@src/sqlery/django_sqlery/backend.py
@.planning/phases/16-backend-wiring-pruning/16-01-SUMMARY.md
@.planning/phases/16-backend-wiring-pruning/16-02-SUMMARY.md
@.planning/phases/16-backend-wiring-pruning/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXPLAIN pruning tests — 11 checklist items, one test each (PG-only)</name>
  <files>tests/unit/test_pruning_explain.py</files>
  <read_first>
    tests/unit/test_partitioning.py — examine conftest/fixture patterns for PG database access
    tests/test_partial_index_12_01.py — examine EXPLAIN test pattern for an existing precedent
    tests/settings.py — confirm DATABASES configuration and how SQLERY_TEST_PG_URL is used
  </read_first>
  <action>
    Create tests/unit/test_pruning_explain.py. File-level skip guard: `pytest.importorskip("psycopg")` and `pytestmark = pytest.mark.skipif(not os.environ.get("SQLERY_TEST_PG_URL"), reason="PG required")`.

    Use `@pytest.mark.django_db` on each test class. Set DATABASES["default"] to the PG URL from SQLERY_TEST_PG_URL inside a conftest-level autouse fixture or use the existing Django test settings.

    Helper function `_explain_query(sql, params)` that runs `EXPLAIN (ANALYZE FALSE, FORMAT TEXT) <sql>` via `connection.cursor()` and returns the text output. Assert "Partitions: 1" appears in the output.

    Test class TestExplainPruning, one method per checklist item:

    Item 1 — test_explain_atomic_claim_sqlite: Create a QueuedJob row. Call the UPDATE that atomic_claim_job_sqlite uses: `UPDATE sqlery_queued_job SET status='running', version=version+1 WHERE id=%s AND created_at=%s AND status='queued' AND version=%s`. EXPLAIN ANALYZE FALSE this UPDATE. Assert "Partitions: 1" in output.

    Item 2 — test_explain_atomic_claim_postgres: Same UPDATE as atomic_claim_job_postgres: `UPDATE sqlery_queued_job SET status='running', version=version+1 WHERE id=%s AND created_at=%s AND version=%s`. Assert "Partitions: 1".

    Item 3 — test_explain_mark_running: UPDATE sqlery_queued_job SET status='running', version=version+1 WHERE id=%s AND created_at=%s AND version=%s. Assert "Partitions: 1".

    Item 4 — test_explain_mark_success: UPDATE sqlery_queued_job SET status='success', finished_at=%s WHERE id=%s AND created_at=%s AND version=%s. Assert "Partitions: 1".

    Item 5 — test_explain_mark_failed: UPDATE sqlery_queued_job SET status='failed', finished_at=%s WHERE id=%s AND created_at=%s AND version=%s. Assert "Partitions: 1".

    Item 6 — test_explain_save_meta: UPDATE sqlery_queued_job SET meta=%s WHERE id=%s AND created_at=%s. Assert "Partitions: 1".

    Item 7 — test_explain_cancel_job: UPDATE sqlery_queued_job SET status='failed' WHERE id=%s AND created_at=%s AND status='queued'. Assert "Partitions: 1".

    Item 8 — test_explain_mark_job_archived: UPDATE sqlery_queued_job SET status='archived' WHERE id=%s AND created_at=%s AND status='failed'. Assert "Partitions: 1".

    Item 9 — test_explain_cascade_ancestor_status: UPDATE sqlery_queued_job SET status=%s WHERE id=%s AND created_at=%s. Assert "Partitions: 1".

    Item 10 — test_explain_get_job_by_id: SELECT * FROM sqlery_queued_job WHERE id=%s — this is a SELECT, not UPDATE. EXPLAIN should still show "Partitions: 1" when created_at is known. For the EXPLAIN test, use SELECT with both id and created_at: SELECT id FROM sqlery_queued_job WHERE id=%s AND created_at=%s. Assert "Partitions: 1". Note in docstring: get_job_by_id fetches by id only; partition pruning on SELECT is secondary; this test verifies that adding created_at would prune to 1 partition.

    Item 11 — test_explain_update_child_pid: UPDATE sqlery_queued_job SET child_pid=%s WHERE id=%s AND created_at=%s. Assert "Partitions: 1".

    Each test: create one QueuedJob row, capture its id and created_at, run EXPLAIN ANALYZE FALSE on the exact SQL with those values substituted, assert "Partitions: 1" in the plan text.

    EXPLAIN output includes "Partitions: 1 of N" for a range partition scan that prunes to one partition. The assertion `assert "Partitions: 1" in plan_text` correctly passes only when pruning works. Use `assert "Partitions: 1 of" in plan_text or "Partitions: 1\n" in plan_text or "Partitions: 1 " in plan_text` to handle formatting variations.

    Run command: `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/unit/test_pruning_explain.py -v`
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/unit/test_pruning_explain.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR|Partitions"</automated>
  </verify>
  <done>
    11 tests in TestExplainPruning all PASS on PG. Each test's EXPLAIN output contains "Partitions: 1". Tests skip gracefully when SQLERY_TEST_PG_URL is not set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lifecycle test (claim→run→reclaim on PG) + divergence matrix + update staging tests</name>
  <files>
    tests/test_lifecycle_partitioned.py
    tests/test_divergence_matrix.py
    tests/unit/test_staging.py
  </files>
  <read_first>
    tests/test_batched_cleanup.py — lifecycle and cleanup test patterns for DjangoBackend
    tests/unit/test_staging.py lines 40-130 — understand test_far_future_job_goes_to_staging and surrounding tests that need vendor-conditional updates
    tests/test_models.py — model test patterns for QueuedJob creation
  </read_first>
  <action>
    tests/test_lifecycle_partitioned.py:
    New file. Requires SQLERY_TEST_PG_URL. pytestmark = pytest.mark.skipif(not os.environ.get("SQLERY_TEST_PG_URL"), ...).
    Class TestPartitionedLifecycle, method test_claim_run_complete_reclaim:
    1. Create 3 QueuedJob rows with different statuses using DjangoBackend.create_job.
    2. backend.claim_job(queues=["default"], worker_id="test-worker") — assert claimed successfully and returned job has status="running".
    3. job.mark_success(output="ok") — assert status="success".
    4. Call backend.cleanup_jobs(max_age_days=0) — this triggers reclaim_drained_partitions on PG.
    5. Assert the cleanup returns a dict with "reclaimed_via_partition_drop": True OR the jobs are gone (not findable via get_job_by_id).
    6. Assert no jobs remain in sqlery_queued_job for the test partition.

    Also add test_daemon_stats_emits_all_five_metrics: mock the daemon's maintenance tick by calling _partitioning functions directly with a raw cursor, then assert _last_partition_stats contains all 5 keys (partition_count, default_rows, oldest_undrained_age_days, staging_depth, last_tick_duration_s).

    Run command: `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/test_lifecycle_partitioned.py -v`

    tests/test_divergence_matrix.py:
    New file. Matrix: each DjangoBackend public method that touches QueuedJob is called once under SQLite (no SQLERY_TEST_PG_URL) and once under PG (with URL). Assertions: same return type and shape; SQLite-specific assertions (e.g., cleanup_jobs returns "deleted" key, not "reclaimed_via_partition_drop"); PG-specific assertions (cleanup_jobs returns "reclaimed_via_partition_drop": True when table is partitioned). Uses @pytest.mark.parametrize or two test classes: TestDivergenceMatrixSQLite (always runs, uses default SQLite) and TestDivergenceMatrixPG (skips without SQLERY_TEST_PG_URL).

    Methods to cover: create_job, claim_job, get_job_by_id, cancel_job, cleanup_jobs, vacuum_database, mark_job_archived, get_jobs. Assert: SQLite cleanup_jobs returns {"deleted": ..., "count": ...}; PG cleanup_jobs returns {"reclaimed_via_partition_drop": True, ...}. Assert: vacuum_database always returns {"success": True}. Assert: create_job on SQLite with far-future scheduled_at returns QueuedJob (not ScheduledJob) — R10 SQLite unchanged.

    Run command: `env -u VIRTUAL_ENV uv run pytest tests/test_divergence_matrix.py::TestDivergenceMatrixSQLite -v`
    And: `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/test_divergence_matrix.py -v`

    tests/unit/test_staging.py — update:
    Tests in TestStagingRouting that assert far-future jobs go to ScheduledJob (e.g., test_far_future_job_goes_to_staging, test_far_future_job_invisible_to_claim_queue, test_far_future_job_visible_to_get_job_by_id, test_far_future_job_cancellable) now need vendor-conditional logic because _partitioned_pg() gates the routing.

    For each affected test: add a check at the test start. If connection.vendor == "sqlite": assert result is a QueuedJob (not ScheduledJob) — far-future stays in queue on SQLite (D6, R10). If connection.vendor == "postgresql": keep the existing ScheduledJob assertion. Add a comment "# SQLite: _partitioned_pg() is False → far-future goes to QueuedJob (D6 — R10)".

    Comment out the unconditional ScheduledJob assertion lines and add the vendor-conditional block below.

    Per CLAUDE.md: comment out old assertion lines, add corrected conditional block below. No deletions.
  </action>
  <verify>
    <automated>env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py tests/test_divergence_matrix.py::TestDivergenceMatrixSQLite -v 2>&1 | grep -E "PASSED|FAILED|ERROR"; env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/test_lifecycle_partitioned.py tests/test_divergence_matrix.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"</automated>
  </verify>
  <done>
    test_lifecycle_partitioned.py: claim→run→complete→reclaim test passes on PG. test_divergence_matrix.py: SQLite matrix tests always pass; PG matrix tests pass when SQLERY_TEST_PG_URL set. test_staging.py: updated tests pass under both SQLite (QueuedJob assertion) and PG (ScheduledJob assertion).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLERY_TEST_PG_URL | Test environment variable; only used in test code; never in production path |
| EXPLAIN output | Read-only metadata from PostgreSQL query planner; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Information Disclosure | EXPLAIN output in test assertions | accept | EXPLAIN is read-only; reveals query plan structure only; no sensitive data |
| T-16-12 | Repudiation | Divergence matrix | accept | Tests are idempotent; each creates/cleans up its own rows; no cross-test state leakage |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
- `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/unit/test_pruning_explain.py -v` — 11 PASSED, each with "Partitions: 1" in EXPLAIN output
- `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/test_lifecycle_partitioned.py -v` — claim→reclaim lifecycle PASSED
- `env -u VIRTUAL_ENV uv run pytest tests/test_divergence_matrix.py::TestDivergenceMatrixSQLite -v` — SQLite divergence matrix PASSED (no PG URL needed)
- `env -u VIRTUAL_ENV uv run pytest tests/unit/test_staging.py -v` — all staging tests PASSED under SQLite
- Full suite smoke: `env -u VIRTUAL_ENV SQLERY_TEST_PG_URL=postgresql://postgres:sqlery@localhost:55432/sqlery_test uv run pytest tests/ -x -q --ignore=tests/chaos --ignore=tests/integration 2>&1 | tail -5`
</verification>

<success_criteria>
- 11 EXPLAIN tests in test_pruning_explain.py all confirm "Partitions: 1" (single partition pruning)
- Lifecycle test: claim → mark_running → mark_success → cleanup_jobs (→ reclaim) passes on PG
- Divergence matrix: SQLite behavior assertions green; PG behavior assertions green
- Staging tests: vendor-conditional, both paths green
- Phase 16 success criteria 1, 2, 3 all met
</success_criteria>

<output>
Create .planning/phases/16-backend-wiring-pruning/16-04-SUMMARY.md when done.
</output>
