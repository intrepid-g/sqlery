---
phase: 10-harden-cron-semantics
plan: 04
type: tdd
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - tests/test_atomic_scheduler.py
  - tests/test_core_standalone.py
autonomous: true
requirements: [CRON-01, CRON-02, CRON-03, CRON-04]
must_haves:
  truths:
    - "A test proves exactly one job is created when two scheduler runs observe the same due cron tick (atomic single-firing, runs on SQLite)"
    - "A test proves next_run_at advances from the scheduled time across several ticks with no drift and no missed-tick replay"
    - "A test proves the scheduler_jitter_seconds knob is respected (>0 delays, 0 does not)"
    - "Tests prove interval and once schedule types are not regressed"
    - "Coverage exists for the standalone SQLAlchemy backend advance path (DB-correctness, not the Phase 11 matrix proof)"
  artifacts:
    - path: "tests/test_atomic_scheduler.py"
      provides: "Django-mode CRON-01..04 behavioral tests (atomic single-fire, drift, jitter, interval/once non-regression)"
      contains: "advance"
    - path: "tests/test_core_standalone.py"
      provides: "Standalone SQLAlchemy advance_scheduled_task_if_due correctness tests"
      contains: "advance_scheduled_task_if_due"
  key_links:
    - from: "tests/test_atomic_scheduler.py"
      to: "Scheduler.run_due_tasks / advance_scheduled_task_if_due"
      via: "behavioral assertions"
      pattern: "run_due_tasks|advance_scheduled_task_if_due"
    - from: "tests/test_core_standalone.py"
      to: "SQLAlchemyBackend.advance_scheduled_task_if_due"
      via: "direct backend assertions"
      pattern: "advance_scheduled_task_if_due"
---

<objective>
Prove the four CRON behaviors are real, on DB-backed tests (SQLite, plus the Postgres lane the existing suite already exercises). Per the quality gate, the tests must show: (1) atomic single-firing under simulated two-leader overlap, (2) drift correction across several ticks, (3) the jitter knob is respected, (4) interval/once not regressed. These are DB-correctness tests; the full cross-matrix parity proof (`{Django, standalone} × {SQLite, Postgres}`) is deferred to Phase 11 — do NOT attempt the parity gate here.

Purpose: The atomic mechanism in Plans 01/03 is only trustworthy once proven. The CAS-on-observed-next_run_at design now works on SQLite too (unlike the old SELECT FOR UPDATE path), so the no-double-fire test should RUN on SQLite rather than being `@skip_on_sqlite`.

Output: New behavioral tests in the existing Django scheduler test file and standalone core test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-harden-cron-semantics/10-CONTEXT.md
@.planning/phases/10-harden-cron-semantics/10-PATTERNS.md
@.planning/codebase/TESTING.md
@tests/test_atomic_scheduler.py
</context>

<artifacts_this_phase_produces>
- **Django-mode behavioral tests** in `tests/test_atomic_scheduler.py` (new test class or methods):
  - `test_cron_fires_exactly_once_under_simulated_overlap` — two sequential/threaded firing attempts on the same due cron task yield exactly one QueuedJob (runs on SQLite via the CAS, NOT skipped).
  - `test_next_run_at_advances_without_drift_across_ticks` — fire N consecutive ticks; assert each new next_run_at equals the cron occurrence computed from the prior scheduled next_run_at (not from wall-clock), and far-behind tasks clamp to a future occurrence.
  - `test_scheduler_jitter_seconds_respected` — with jitter set >0, enqueue path applies a bounded delay (assert the configured value is read/applied; keep timing-tolerant); with 0, no delay.
  - `test_interval_and_once_not_regressed` — interval task re-advances by its interval; once task is disabled (enabled=False, next_run_at=None) after firing.
- **Standalone-mode correctness tests** in `tests/test_core_standalone.py`:
  - direct `SQLAlchemyBackend.advance_scheduled_task_if_due` assertions: winning CAS creates a job + advances; stale observed_due returns None + no job; two calls with the same observed_due produce exactly one job.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Django CRON-01..04 behavioral tests (atomic single-fire on SQLite, drift, jitter, interval/once)</name>
  <files>tests/test_atomic_scheduler.py</files>
  <read_first>
    - tests/test_atomic_scheduler.py lines 1-130 (existing imports: pytest, threading, time, timezone, timedelta, ScheduledTask, QueuedJob, TaskExecutor; the skip_on_sqlite marker :31; the test_task :38; TestAtomicSchedulerClaiming patterns including test_already_queued_task_not_duplicated :216 and test_running_task_not_duplicated :244)
    - tests/test_atomic_scheduler.py lines 172-275 (test_scheduler_updates_next_run_at_atomically :172, disabled/already-queued/running patterns — model the new drift + single-fire tests on these)
    - src/sqlery/core/scheduler.py (the reworked _enqueue_for_scheduled_task / calculate_next_run from Plan 03 — assert against actual behavior)
    - .planning/phases/10-harden-cron-semantics/10-02-SUMMARY.md (the literal jitter config key + how to set it in Django mode for the jitter test)
  </read_first>
  <behavior>
    - test_cron_fires_exactly_once_under_simulated_overlap: create one due cron task; invoke the firing path twice in a way that simulates two leaders observing the same next_run_at (e.g. two run_due_tasks calls / two threads, or two direct advance_scheduled_task_if_due calls with the same observed_due read before either fires); assert exactly one QueuedJob exists for that scheduled_task_id and exactly one of the two attempts returned a job. MUST run on SQLite (not @skip_on_sqlite) — the CAS makes single-firing engine-independent.
    - test_next_run_at_advances_without_drift_across_ticks: set next_run_at to a fixed past scheduled time; fire; assert the new next_run_at equals calculate_next_run(cron, base_time=old_next_run_at) (computed from the scheduled time, future-clamped), NOT from now; repeat for several ticks and assert monotonic, drift-free advance; include a far-behind task and assert it clamps to a future occurrence.
    - test_scheduler_jitter_seconds_respected: with the jitter key set to a small >0 value, assert the firing path reads/applies it (timing-tolerant: assert a delay was applied OR that the config value is consumed by the path — prefer patching time.sleep to capture the argument is in [0, jitter] rather than asserting wall-clock duration); with 0, assert no sleep call.
    - test_interval_and_once_not_regressed: an interval task re-advances next_run_at by its interval after firing; a once task ends with enabled=False and next_run_at=None.
  </behavior>
  <action>
    Add a new test class (e.g. TestCronSemanticsHardening) to tests/test_atomic_scheduler.py using the existing @pytest.mark.django_db(transaction=True) style and the existing ScheduledTask/QueuedJob/TaskExecutor imports (TaskExecutor is the Scheduler alias used in this file). For the single-fire test, do NOT use @skip_on_sqlite — use the CAS path: read observed_due once, then make two advance attempts (two threads or two sequential calls where the second is given the now-stale observed_due) and assert QueuedJob.objects.filter(scheduled_task_id=task.id).count() == 1. For the jitter test, set the Django jitter setting (DJANGO_SQL_JOBS / SCHEDULER_JITTER_SECONDS per 10-02-SUMMARY) and patch sqlery.core.scheduler.time.sleep (or random.uniform) with unittest.mock.patch to capture the delay arg and assert it is within [0, jitter]; assert sleep is NOT called when jitter is 0. For drift, compute the expected next_run_at directly via the Scheduler.calculate_next_run from the prior scheduled time and assert equality. Reuse the module-level test_task as the task_path. Follow the file's plain-assert, one-behavior-per-method, descriptive-name conventions.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && uv run --active pytest tests/test_atomic_scheduler.py -k "CronSemanticsHardening or cron_fires_exactly_once or drift or jitter or interval_and_once" -v</automated>
  </verify>
  <acceptance_criteria>
    - The new single-fire test runs on SQLite (not skipped) and asserts exactly one QueuedJob per due cron tick under simulated overlap.
    - The drift test asserts new next_run_at == calculate_next_run(cron, base_time=prior_scheduled_time) and that far-behind tasks clamp to a future occurrence.
    - The jitter test asserts the bounded delay arg is in [0, jitter] when >0 and that no sleep occurs when 0 (via patched time.sleep/random.uniform).
    - The interval/once test asserts interval re-advance and once-disable behavior.
    - All selected tests pass on the default SQLite lane; the verify command exits 0.
  </acceptance_criteria>
  <done>Django-mode tests prove atomic single-firing (on SQLite), drift correction, jitter respect, and interval/once non-regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Standalone SQLAlchemyBackend.advance_scheduled_task_if_due correctness tests</name>
  <files>tests/test_core_standalone.py</files>
  <read_first>
    - tests/test_core_standalone.py (existing standalone test setup: how SQLAlchemyBackend / an in-memory SQLite engine / get_session is initialized for standalone tests, the ScheduledTask + QueuedJob SQLModel fixtures, and the test style)
    - src/sqlery/fastapi_sqlery/backend.py lines 968-1004 (has_pending_job_for_scheduled_task, update_scheduled_task_next_run, update_scheduled_task — and the new advance_scheduled_task_if_due added in Plan 01)
    - .planning/phases/10-harden-cron-semantics/10-01-SUMMARY.md (the exact advance signature + job_kwargs field mapping used by the standalone impl)
  </read_first>
  <behavior>
    - Winning CAS: calling advance_scheduled_task_if_due(task_id, observed_due, new_next_run, job_kwargs) where observed_due == the row's current next_run_at returns a job instance, the row's next_run_at is advanced to new_next_run, and exactly one QueuedJob exists for that scheduled_task_id.
    - Stale observed_due: calling with an observed_due that no longer matches the row returns None and creates no job.
    - Two-attempt single-fire: read observed_due once, call advance twice with that same observed_due; the first returns a job, the second returns None; exactly one QueuedJob exists afterward and next_run_at == new_next_run.
  </behavior>
  <action>
    Add a test class (e.g. TestStandaloneAdvanceScheduledTask) to tests/test_core_standalone.py using the file's existing standalone backend/session setup (in-memory SQLite). Create a ScheduledTask SQLModel row with a known next_run_at; build job_kwargs matching the field mapping recorded in 10-01-SUMMARY (task_path, kwargs, queue_name, priority, scheduled_at=None, max_retries, retry_backoff, allow_parallel, timeout_seconds, scheduled_task_id). Assert the winning-CAS, stale-observed, and two-attempt-single-fire behaviors directly against the backend (no Scheduler needed). Query QueuedJob via the session to assert exactly one job per won tick. If the file lacks a reusable standalone-backend fixture, add a minimal one consistent with the existing standalone test setup (do not introduce new dependencies; reuse get_session/init from the existing tests). Follow the file's assert/style conventions.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public && uv run --active pytest tests/test_core_standalone.py -k "StandaloneAdvanceScheduledTask or advance_scheduled_task_if_due" -v</automated>
  </verify>
  <acceptance_criteria>
    - Winning-CAS test: returns a job, advances next_run_at, exactly one QueuedJob created.
    - Stale-observed test: returns None, zero jobs created.
    - Two-attempt test: exactly one job created across two calls with the same observed_due; second call returns None.
    - Tests run on the standalone SQLite setup and pass; verify exits 0.
  </acceptance_criteria>
  <done>Standalone backend advance is proven correct (atomic single-firing on observed-due CAS) at the DB-correctness level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> DB | Tests exercise the real DB (SQLite); they are the contract proof for the CAS, not a mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Repudiation (untested race claim) | CRON-04 single-firing claim | mitigate | The single-fire test directly exercises the CAS under simulated two-leader overlap on SQLite and asserts exactly-once, so the exactly-once guarantee is proven, not assumed. |
| T-10-09 | Tampering (flaky timing-based jitter assertion) | jitter test | mitigate | Assert via patched time.sleep/random.uniform argument bounds rather than wall-clock duration, eliminating timing flakiness while still proving the knob is honored. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; reuses already-declared pytest/pytest-django/sqlmodel test deps. No install task, no slopcheck/human checkpoint required. |
</threat_model>

<verification>
- New single-fire test runs on SQLite (not @skip_on_sqlite) and asserts exactly-once.
- Drift test asserts advance from scheduled time + future-clamp.
- Jitter test asserts bounded delay arg / no-op at 0 without wall-clock timing dependence.
- interval/once non-regression asserted.
- Standalone backend advance proven directly.
- Full suite sanity: cd /Users/gabriel/Documents/GitHub/sqlery-public && uv run --active pytest tests/test_atomic_scheduler.py tests/test_core_standalone.py -q passes (no regressions).
</verification>

<success_criteria>
- DB-correctness tests prove all four CRON behaviors (atomic single-fire, drift, jitter, interval/once preservation) in Django mode and the standalone advance path.
- The single-fire test is engine-independent (runs on SQLite), reflecting the CAS design.
- No cross-matrix parity proof attempted here (deferred to Phase 11).
</success_criteria>

<output>
Create `.planning/phases/10-harden-cron-semantics/10-04-SUMMARY.md` when done, recording: the new test class/method names, which tests run on SQLite vs require Postgres, and any fixture added for the standalone backend.
</output>
