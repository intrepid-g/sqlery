---
phase: 11-parity-gated-tests-ci
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements: [PARITY-05]
must_haves:
  truths:
    - "All four matrix cells of every parity test ACTUALLY RUN in CI: Django×SQLite and standalone×SQLite on the default rail, Django×Postgres and standalone×Postgres on the PG rail"
    - "The standalone×Postgres parity cell is no longer silently skipped — an explicit CI step runs the parity tests under SQLERY_FORCE_STANDALONE=1 with -m postgres against the PG service"
    - "A failing parity cell fails the build (first-class gate, not advisory) and an empty PG collection fails the job"
    - "No production logic and no test logic changes — CI YAML only"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Standalone×Postgres parity CI step + confirmation the Django PG rail collects the new parity tests"
      contains: "SQLERY_FORCE_STANDALONE"
  key_links:
    - from: ".github/workflows/test.yml (PG rail)"
      to: "tests/test_parity_scheduler.py + tests/test_atomic_scheduler.py + tests/test_core_standalone.py PG cells"
      via: "uv run pytest -m postgres collecting the parity cells"
      pattern: "pytest -m postgres"
    - from: ".github/workflows/test.yml (new standalone PG step)"
      to: "standalone×Postgres parity cells"
      via: "SQLERY_FORCE_STANDALONE=1 + SQLERY_TEST_PG_URL + -m postgres"
      pattern: "SQLERY_FORCE_STANDALONE"
---

<objective>
Make PARITY-05 real: ensure every one of the four matrix cells of the parity tests added in Plans
01 and 02 ACTUALLY EXECUTES in CI, with a failing cell failing the build. Today's CI runs the
default rail (-m "not postgres") covering both SQLite cells, and a dedicated PG rail (-m postgres)
— but the PG rail runs under pytest-django (Django backend), so the **standalone×Postgres parity
cell is silently skipped** (the standalone-no-django job is import-only and never executes the test
suite). This plan closes that gap.

This is CI configuration only — no production source and no test logic changes. Plans 01 and 02
produced the PG-marked cells; this plan guarantees the grid runs them.

Purpose: Turn the parity matrix into a genuinely-enforced first-class acceptance gate (PARITY-05),
not a grid with a quietly-missing cell.
Output: An edited .github/workflows/test.yml with an explicit standalone×Postgres parity step
(under SQLERY_FORCE_STANDALONE=1, -m postgres, against the postgres:15 service), and verified
collection of the new parity cells on the existing Django PG rail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-parity-gated-tests-ci/11-CONTEXT.md
@.planning/phases/11-parity-gated-tests-ci/11-PATTERNS.md
@.planning/phases/11-parity-gated-tests-ci/11-01-PLAN.md
@.planning/phases/11-parity-gated-tests-ci/11-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan adds the CI wiring that makes PARITY-05 enforceable:

- New CI step in the test job of .github/workflows/test.yml, named "Run standalone-mode parity suite
  with PostgreSQL", that sets SQLERY_FORCE_STANDALONE=1, SQLERY_TEST_PG_URL + DATABASE_URL to the
  postgres:15 service, and runs uv run pytest -m postgres over the parity test files so the
  standalone×Postgres cells execute. The step rejects an empty collection (a missing/skipped
  standalone PG cell fails the job).
- A confirmation comment on the existing "Run @pytest.mark.postgres suite" step documenting that it
  now also collects tests/test_parity_scheduler.py (Django×Postgres parity cells) since it has no
  path filter.
- Per CLAUDE.md edit discipline: any replaced YAML line is commented out with `# Old:` and the new
  line is added beside it; no line is deleted.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the standalone-by-Postgres parity CI step and confirm Django PG collection (PARITY-05)</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml — REQUIRED. Read the whole file. The test job (lines 16-102): the
      postgres:15 service block (lines 23-36), the "Run unit tests" default rail -m "not postgres"
      (lines 57-61), the "Run @pytest.mark.postgres suite" PG rail (lines 76-83, note SQLERY_TEST_PG_URL
      plus the comment that an empty collection now fails the job), and the coverage step (85-94). The
      standalone-no-django job (lines 104-159) — confirm it is import-only (it imports sqlery.core
      submodules; it does NOT run pytest), so it cannot be the home of the standalone×Postgres parity
      cell.
    - .planning/phases/11-parity-gated-tests-ci/11-02-PLAN.md and 11-01-PLAN.md — REQUIRED. They define
      the parity test files (tests/test_parity_scheduler.py, plus the PG cells in
      tests/test_atomic_scheduler.py and tests/test_core_standalone.py) and the SQLERY_FORCE_STANDALONE=1
      convention (from tests/integration/conftest.py::_run_no_django, lines 487-491) that forces compat
      to resolve the standalone backend even when Django is installed.
    - tests/integration/conftest.py (lines 47-63, 115-142) — REQUIRED. Confirms the PG-marker routing
      and the auto-skip-on-no-PG-URL collection gate, so the new step's -m postgres filter selects
      exactly the PG-marked parity cells.
  </read_first>
  <action>
    Edit .github/workflows/test.yml to guarantee all four parity matrix cells run. Two changes, both
    in the test job (which already has the postgres:15 service available):

    1. Add a new step immediately after the existing "Run @pytest.mark.postgres suite" step (after
       line 83), named "Run standalone-mode parity suite with PostgreSQL". Its env sets PYTHONPATH .,
       SQLERY_FORCE_STANDALONE "1" (forces compat to resolve the standalone backend even though Django
       is installed in the [dev] env, matching the _run_no_django convention), and SQLERY_TEST_PG_URL
       plus DATABASE_URL pointing at the postgres:15 service (copy the exact URLs from the existing PG
       step, lines 79-80). Its run invokes
       uv run pytest -m postgres -v --tb=short tests/test_parity_scheduler.py tests/test_core_standalone.py tests/test_atomic_scheduler.py
       — scoping to the parity files keeps the standalone PG run fast and focused, and the
       standalone-backend cells (pg_standalone_backend and the standalone parity cells) execute here
       under SQLERY_FORCE_STANDALONE. Do NOT add a `|| echo ...` tolerance — a failing standalone PG
       parity cell MUST fail the job (first-class gate). Treat an empty collection (pytest exit code 5)
       as failure too: mirror the existing PG step's "empty collection fails the job" intent by NOT
       using the legacy `|| echo` escape; if a pre-check is warranted, add a preceding
       uv run pytest -m postgres --co -q <files> line that asserts a non-empty collection before the
       real run.

    2. Confirm and annotate the existing "Run @pytest.mark.postgres suite" step (lines 76-83): it has
       no path filter, so it already collects tests/test_parity_scheduler.py and the new PG cells in
       tests/test_atomic_scheduler.py and tests/test_core_standalone.py under the DJANGO backend (the
       Django×Postgres parity cells). Add a `# Phase 11:` comment above its run line documenting that
       this step now also enforces the Django×Postgres parity cells. Do NOT change the run command
       itself — it already covers them.

    CLAUDE.md edit discipline (CRITICAL): do NOT delete any existing YAML line. If you must change an
    existing line, comment the original out with a `# Old:` prefix and add the replacement beside it.
    For purely-additive changes (the new step plus comments), no commenting is needed. Keep the YAML
    valid — the new step must be indented under the test job's steps: at the same level as the
    existing "Run @pytest.mark.postgres suite" step.

    Note the matrix already fans out over python-version ['3.11','3.12','3.13'], so the standalone PG
    parity step runs on every Python version — full grid coverage without a new matrix axis.
  </action>
  <verify>
    <automated>cd /Users/gabriel/Documents/GitHub/sqlery-public; uv run --active python -c "import yaml; d=yaml.safe_load(open('.github/workflows/test.yml')); names=[s.get('name','') for s in d['jobs']['test']['steps']]; assert any('standalone-mode parity' in n.lower() for n in names), 'missing standalone parity step'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parses cleanly: `uv run --active python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` exits 0.
    - The new step exists in the test job: `grep -n "Run standalone-mode parity suite with PostgreSQL" .github/workflows/test.yml` returns one line.
    - The new step forces standalone mode: `grep -n "SQLERY_FORCE_STANDALONE" .github/workflows/test.yml` returns at least one line within the new step's env block.
    - The new step runs the parity files under -m postgres: `grep -n "test_parity_scheduler.py" .github/workflows/test.yml` returns a line on the new step's run command.
    - The new step has NO `|| echo` escape (it must fail the build on failure): the new step's run line does not contain `|| echo`.
    - The Django PG rail still runs -m postgres with no path filter and now carries a `# Phase 11:` annotation: `grep -n "Phase 11" .github/workflows/test.yml` returns a line near the existing "Run @pytest.mark.postgres suite" step.
    - Edit discipline honored: any line that was changed (not purely added) has a `# Old:` commented predecessor; `git diff .github/workflows/test.yml` shows no removed non-comment lines except where a `# Old:` twin was added.
  </acceptance_criteria>
  <done>The test job runs the standalone×Postgres parity cells under SQLERY_FORCE_STANDALONE=1 with -m postgres against postgres:15, the Django PG rail collects the Django×Postgres parity cells, both SQLite cells stay on the default rail, no parity cell can pass by being skipped, and a failing cell fails the build. No line was deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → PG service | The new step connects to the postgres:15 service container via SQLERY_TEST_PG_URL / DATABASE_URL — reusing the existing service-container creds (postgres/postgres), no new secret introduced. |
| CI gate → build status | The parity grid is the acceptance gate; a skipped-but-counted cell would be a false green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | A skipped standalone×Postgres cell silently counts as passed (gate bypass) | mitigate | The new step runs the parity cells under SQLERY_FORCE_STANDALONE=1 with SQLERY_TEST_PG_URL set so they actually EXECUTE (not skip); no `|| echo` escape; empty collection (exit 5) is treated as failure so a missing cell fails the job. |
| T-11-03-02 | Tampering | Advisory (non-failing) parity step that never fails the build | mitigate | The step has no `|| echo` / `continue-on-error` tolerance — a failing parity cell fails the test job and thus the build. |
| T-11-03-03 | Information disclosure | New CI secret handling | accept | No new secret — reuses the existing postgres:15 service creds already in the workflow; the PG URL is non-sensitive local-service connection string. |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; the new step reuses the already-installed [dev] env (uv pip install -e ".[dev]" from the existing "Install dependencies" step). No legitimacy checkpoint required. |
</threat_model>

<verification>
- YAML validity: `uv run --active python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` exits 0.
- New step present and correctly wired: `grep -n "Run standalone-mode parity suite with PostgreSQL" .github/workflows/test.yml` and `grep -n "SQLERY_FORCE_STANDALONE" .github/workflows/test.yml` both return lines.
- Django PG rail annotation present: `grep -n "Phase 11" .github/workflows/test.yml`.
- Edit discipline: `git diff .github/workflows/test.yml` shows no deleted non-comment lines (only additions and any `# Old:` twins).
- Grid reasoning documented in SUMMARY: default rail covers both SQLite cells (-m "not postgres"); Django PG rail covers Django×Postgres; the new step covers standalone×Postgres — all four cells run.
</verification>

<success_criteria>
- All four parity matrix cells execute in CI: Django×SQLite + standalone×SQLite (default rail), Django×Postgres (existing PG rail), standalone×Postgres (new step under SQLERY_FORCE_STANDALONE=1).
- The standalone×Postgres cell is no longer silently skipped; a missing or skipped cell fails the job (no empty-collection tolerance, no `|| echo` escape).
- A failing parity cell fails the build — the gate is first-class, not advisory.
- No production source or test logic changes; CI YAML only; no deleted lines (CLAUDE.md edit discipline).
</success_criteria>

<output>
Create `.planning/phases/11-parity-gated-tests-ci/11-03-SUMMARY.md` when done
</output>
